openapi: 3.0.3
info:
  title: Plume Cloud — OpenSync Management API
  version: 2026-07-25
  description: "Unofficial REST API reference specification, in the style of the real\
    \ Plume Cloud\nPartner API, for managing **OpenSync** (https://opensync.io) powered\
    \ device fleets.\n\n### Background\nOpenSync devices (gateways, WiFi extenders/pods,\
    \ IoT bridges) do not speak REST/HTTP\nto the cloud natively — they replicate\
    \ state with a cloud controller via OVSDB\n(JSON-RPC) over TLS, using tables such\
    \ as `AWLAN_Node`, `Wifi_Radio_Config/State`,\n`Wifi_VIF_Config/State`, `Wifi_Associated_Clients`,\
    \ `Wifi_VIF_Neighbors`,\n`Wifi_Inet_Config/State`, `Wifi_AFC`, `Connection_Manager_Uplink`,\
    \ etc. Those OVSDB\ntables and columns are real and verified against the OpenSync\
    \ project wiki/source.\n\nPlume Cloud additionally exposes a separate **partner-facing\
    \ REST API** (distinct\nfrom the device-facing OVSDB protocol) that partners/CSPs\
    \ use to provision and manage\nsubscribers. This spec's top-level resource hierarchy\
    \ is modeled on that real API.\n\n### What is verified vs. inferred in this document\n\
    - **Verified real conventions**: the `Partner → Group → Customer → Location` resource\n\
    \  nesting and naming (e.g. `/customers/{id}`, `/customers/{id}/locations/{locationId}`)\n\
    \  are taken directly from the `ocolin/Plume` open-source PHP client\n  (https://github.com/ocolin/Plume),\
    \ which documents real endpoint shapes and the\n  `PLUME_PARTNER_ID` / `PLUME_GROUP_ID`\
    \ scoping variables used against the actual\n  Plume Cloud API.\n- **Inferred/extrapolated**:\
    \ everything below `Location` (Nodes, Radios, Networks,\n  Devices, Steering,\
    \ Telemetry, Security, Presence, VPN, Webhooks, and all their\n  fields) is **not**\
    \ sourced from Plume's actual partner API — that catalog lives\n  behind a partner-only\
    \ login (`https://gamma.noc.plume.com/explorer`) that was not\n  accessible while\
    \ authoring this spec. Those resources are instead a REST\n  translation of OpenSync's\
    \ real, public OVSDB schema, designed to plausibly match\n  how such a management\
    \ layer would be exposed. Treat this portion as a design\n  proposal to validate\
    \ against real partner API docs, not a verified mirror of it.\n\nEach resource/field\
    \ below documents which underlying OpenSync OVSDB table(s) and\ncolumns it is\
    \ derived from, so implementers can map requests to real OVSDB\ntransactions and\
    \ map replicated device state back into API responses/webhooks.\n\n### Functional\
    \ coverage\n- Customer & location (household/site) management\n- Node onboarding,\
    \ lifecycle, firmware\n- Radio (PHY) configuration: channel, power, DFS, 6GHz\
    \ AFC\n- Network (SSID/VIF) configuration incl. WPA2/WPA3/OWE, VLANs, MLO (WiFi\
    \ 7)\n- Connected device inventory, band/client steering, RRM neighbor reports\n\
    - Guest networking\n- Speedtest, stats/telemetry, diagnostics/log pulls, WAN/backhaul\
    \ uplink status\n- Security: threat protection (DPI tagging), content filtering,\
    \ parental controls,\n  captive portal\n- Presence/IoT: BLE-based room-level presence,\
    \ persons\n- VPN configuration\n- Outbound webhooks for async cloud→partner events\n"
  contact:
    name: API Platform Team
    url: https://opensync.io
  license:
    name: Apache-2.0
    url: https://www.apache.org/licenses/LICENSE-2.0
servers:
- url: https://api.plumecloud.example.com/v1
  description: Production
- url: https://api.staging.plumecloud.example.com/v1
  description: Staging
security:
- oauth2ClientCredentials: []
tags:
- name: Auth
  description: OAuth2 client-credentials token issuance
- name: Partners
  description: Top-level partner/ISP tenants (scopes Groups and Customers)
- name: Groups
  description: Sub-organizations within a Partner (e.g. regional business units, resellers)
- name: Customers
  description: Subscriber accounts belonging to a Group
- name: Locations
  description: Physical sites (homes/businesses) belonging to a customer
- name: Nodes
  description: Physical OpenSync devices (gateways, WiFi pods/extenders) — backed
    by `AWLAN_Node`
- name: Radios
  description: Per-node radio (PHY) configuration/state — backed by `Wifi_Radio_Config`/`Wifi_Radio_State`,
    `Wifi_AFC`
- name: Networks
  description: SSIDs/VIFs deployed across nodes at a location — backed by `Wifi_VIF_Config`/`Wifi_VIF_State`
- name: Devices
  description: Client devices connected to the network — backed by `Wifi_Associated_Clients`
- name: Steering
  description: Band/client steering policies and actions (RRM/BTM)
- name: Telemetry
  description: Speedtest, stats reports, diagnostics — backed by `Wifi_Stats_Config`,
    speedtest OVSDB tables
- name: Connectivity
  description: WAN/backhaul uplink status — backed by `Connection_Manager_Uplink`
- name: Security
  description: Threat protection, content filtering, parental controls, captive portal
- name: Presence
  description: BLE/IoT room-level presence and persons
- name: VPN
  description: Site-to-cloud / site-to-site VPN configuration
- name: Webhooks
  description: Outbound event subscriptions for async cloud events
- name: Deployments
  description: Named deployment environments (production, development) with tier-based
    protection rules and direct node membership
- name: Agents
  description: Third-party agent lifecycle, configuration, and status monitoring —
    configures non-OpenSync agents (speedtest daemons, IDS/IPS engines, DNS filtering
    providers, VPN clients, custom ISP agents) running alongside OpenSync on CPE devices
paths:
  /oauth/token:
    post:
      operationId: createOauthToken
      tags:
      - Auth
      summary: Obtain an access token (client credentials grant)
      description: Obtain an OAuth 2.0 access token using the client credentials grant flow for machine-to-machine API access.
      security: []
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              required:
              - grant_type
              - client_id
              - client_secret
              properties:
                grant_type:
                  type: string
                  enum:
                  - client_credentials
                client_id:
                  type: string
                client_secret:
                  type: string
                  format: password
                scope:
                  type: string
                  example: customers:read locations:write nodes:write
      responses:
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Token issued
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TokenResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /partners:
    get:
      operationId: getPartners
      tags:
      - Partners
      summary: List partners
      description: Returns a paginated list of all partners accessible to the authenticated principal.
      parameters:
      - $ref: '#/components/parameters/PageParam'
      - $ref: '#/components/parameters/PageSizeParam'
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Paginated list of partners
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/PaginatedResponse'
                - type: object
                  properties:
                    items:
                      type: array
                      items:
                        $ref: '#/components/schemas/Partner'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /partners/{partnerId}:
    parameters:
    - $ref: '#/components/parameters/PartnerId'
    get:
      operationId: getPartnersByPartnerId
      tags:
      - Partners
      summary: Get a partner
      description: Returns details for a single partner, including billing plan, status, and integration metadata.
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Partner details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Partner'
        '404':
          $ref: '#/components/responses/NotFound'
  /partners/{partnerId}/groups:
    parameters:
    - $ref: '#/components/parameters/PartnerId'
    get:
      operationId: getPartnersByPartnerIdGroups
      tags:
      - Groups
      summary: List groups under a partner
      description: Returns all organisational groups scoped under a partner, typically representing resellers or regional business units.
      parameters:
      - $ref: '#/components/parameters/PageParam'
      - $ref: '#/components/parameters/PageSizeParam'
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Paginated list of groups
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/PaginatedResponse'
                - type: object
                  properties:
                    items:
                      type: array
                      items:
                        $ref: '#/components/schemas/Group'
        '404':
          $ref: '#/components/responses/NotFound'
    post:
      operationId: createPartnersByPartnerIdGroups
      tags:
      - Groups
      summary: Create a group (e.g. a reseller or regional business unit)
      description: Creates a new organisational group (reseller, regional unit) under the partner.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GroupCreate'
      responses:
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '201':
          description: Group created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Group'
        '400':
          $ref: '#/components/responses/BadRequest'
  /groups/{groupId}:
    parameters:
    - $ref: '#/components/parameters/GroupId'
    get:
      operationId: getGroupsByGroupId
      tags:
      - Groups
      summary: Get a group
      description: Returns details for a single group, including parent partner reference and creation timestamp.
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Group details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Group'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: updateGroupsByGroupId
      tags:
      - Groups
      summary: Update a group
      description: Updates group metadata such as name or billing parent. Only supplied fields are modified.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GroupUpdate'
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Updated group
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Group'
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      operationId: deleteGroupsByGroupId
      tags:
      - Groups
      summary: Delete a group and cascade-deprovision its customers
      description: Deletes a group and cascade-deprovisions all customers, locations, and nodes. This operation is irreversible.
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '204':
          description: Deleted
        '404':
          $ref: '#/components/responses/NotFound'
  /groups/{groupId}/customers:
    parameters:
    - $ref: '#/components/parameters/GroupId'
    get:
      operationId: getGroupsByGroupIdCustomers
      tags:
      - Customers
      summary: List customers in a group
      description: Returns a paginated list of all customers (ISP subscribers) belonging to a group.
      parameters:
      - $ref: '#/components/parameters/PageParam'
      - $ref: '#/components/parameters/PageSizeParam'
      - name: externalId
        in: query
        description: Filter by partner-supplied external subscriber ID
        schema:
          type: string
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Paginated list of customers
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/PaginatedResponse'
                - type: object
                  properties:
                    items:
                      type: array
                      items:
                        $ref: '#/components/schemas/Customer'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    post:
      operationId: createGroupsByGroupIdCustomers
      tags:
      - Customers
      summary: Create a customer
      description: Creates a new customer (ISP subscriber) within the group.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CustomerCreate'
      responses:
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '201':
          description: Customer created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Customer'
        '400':
          $ref: '#/components/responses/BadRequest'
        '409':
          $ref: '#/components/responses/Conflict'
  /customers/{customerId}:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    get:
      operationId: getCustomersByCustomerId
      tags:
      - Customers
      summary: Get a customer
      description: Returns details for a single customer, including billing information, contact details, and account status.
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Customer details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Customer'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: updateCustomersByCustomerId
      tags:
      - Customers
      summary: Update a customer
      description: Updates customer metadata such as name, email, or billing address. Only supplied fields are modified.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CustomerUpdate'
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Updated customer
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Customer'
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      operationId: deleteCustomersByCustomerId
      tags:
      - Customers
      summary: Delete a customer and cascade-deprovision its locations/nodes
      description: Deletes a customer and cascade-deprovisions all locations and nodes. This operation is irreversible.
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '204':
          description: Deleted
        '404':
          $ref: '#/components/responses/NotFound'
  /customers/{customerId}/locations:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    get:
      operationId: getCustomersByCustomerIdLocations
      tags:
      - Locations
      summary: List locations for an customer
      description: Returns a paginated list of all locations (physical sites) belonging to a customer.
      parameters:
      - $ref: '#/components/parameters/PageParam'
      - $ref: '#/components/parameters/PageSizeParam'
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Paginated list of locations
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/PaginatedResponse'
                - type: object
                  properties:
                    items:
                      type: array
                      items:
                        $ref: '#/components/schemas/Location'
        '404':
          $ref: '#/components/responses/NotFound'
    post:
      operationId: createCustomersByCustomerIdLocations
      tags:
      - Locations
      summary: Create a location
      description: Creates a new location (home, office, or other site) under the customer.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LocationCreate'
      responses:
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '201':
          description: Location created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Location'
        '400':
          $ref: '#/components/responses/BadRequest'
  /customers/{customerId}/locations/{locationId}:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      operationId: getCustomersByCustomerIdLocationsByLocationId
      tags:
      - Locations
      summary: Get a location
      description: Returns details for a single location, including address, timezone, and creation metadata.
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Location details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Location'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: updateCustomersByCustomerIdLocationsByLocationId
      tags:
      - Locations
      summary: Update a location
      description: Updates location properties such as name, address, or timezone. Only supplied fields are modified.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LocationUpdate'
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Updated location
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Location'
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      operationId: deleteCustomersByCustomerIdLocationsByLocationId
      tags:
      - Locations
      summary: Delete a location and deprovision its nodes
      description: Deletes a location and deprovisions all nodes and networks. This operation is irreversible.
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '204':
          description: Deleted
        '404':
          $ref: '#/components/responses/NotFound'
  /customers/{customerId}/locations/{locationId}/nodes:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      operationId: getCustomersByCustomerIdLocationsByLocationIdNodes
      tags:
      - Nodes
      summary: List nodes at a location
      description: Returns a paginated list of all OpenSync nodes at a location, backed by the AWLAN_Node OVSDB table.
      parameters:
      - $ref: '#/components/parameters/PageParam'
      - $ref: '#/components/parameters/PageSizeParam'
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Paginated list of nodes
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/PaginatedResponse'
                - type: object
                  properties:
                    items:
                      type: array
                      items:
                        $ref: '#/components/schemas/Node'
        '404':
          $ref: '#/components/responses/NotFound'
    post:
      operationId: createCustomersByCustomerIdLocationsByLocationIdNodes
      tags:
      - Nodes
      summary: Onboard/claim a node into a location
      description: 'Registers a factory-fresh or RMA node (identified by serial/mac)
        to this

        location. Triggers the cloud controller to accept the node''s OVSDB connection,

        seed `AWLAN_Node` (redirector, cloud endpoint, mode) and push initial

        `Wifi_Radio_Config`/`Wifi_VIF_Config` rows.

        '
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NodeCreate'
      responses:
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '201':
          description: Node onboarded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Node'
        '400':
          $ref: '#/components/responses/BadRequest'
        '409':
          $ref: '#/components/responses/Conflict'
  /nodes/{nodeId}:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    get:
      operationId: getNodesByNodeId
      tags:
      - Nodes
      summary: Get a node
      description: Returns details for a single node, including hardware model, firmware version, serial number, and online status.
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Node details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Node'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: updateNodesByNodeId
      tags:
      - Nodes
      summary: Update node metadata (name, location, role)
      description: Updates node-level properties such as name, tags, or device location label. Only supplied fields are modified.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NodeUpdate'
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Updated node
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Node'
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      operationId: deleteNodesByNodeId
      tags:
      - Nodes
      summary: Deprovision a node
      description: Removes the node from the location and clears its OVSDB config
        rows. The node must be deactivated first — attempting to delete an active
        node returns a 409 Conflict.
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '204':
          description: Deprovisioned
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: Node is still active — call `POST /nodes/{nodeId}/deactivate`
            before deletion
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /nodes/{nodeId}/actions/reboot:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    post:
      operationId: createNodesByNodeIdActionsReboot
      tags:
      - Nodes
      summary: Reboot a node
      description: Issues a reboot command (`Command_Config` row insert in OVSDB).
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '202':
          $ref: '#/components/responses/Accepted'
        '404':
          $ref: '#/components/responses/NotFound'
  /nodes/{nodeId}/actions/factory-reset:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    post:
      operationId: createNodesByNodeIdActionsFactoryReset
      tags:
      - Nodes
      summary: Factory reset a node
      description: Triggers a factory reset on the node. All user data and configuration is erased and the node reboots into setup mode.
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '202':
          $ref: '#/components/responses/Accepted'
        '404':
          $ref: '#/components/responses/NotFound'
  /nodes/{nodeId}/actions/blink-led:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    post:
      operationId: createNodesByNodeIdActionsBlinkLed
      tags:
      - Nodes
      summary: Blink node LED (for physical identification)
      description: Flashes the node's LED to help physically identify the device in a rack or on-site installation.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                durationSeconds:
                  type: integer
                  default: 30
                  minimum: 1
                  maximum: 300
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '202':
          $ref: '#/components/responses/Accepted'
        '404':
          $ref: '#/components/responses/NotFound'
  /nodes/{nodeId}/led-patterns:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    get:
      operationId: getNodesByNodeIdLedPatterns
      tags:
      - Nodes
      summary: Get LED pattern configuration
      description: Returns per-state LED pattern config. Backed by the `LED_Config`
        OVSDB table with 20 named states (`idle`, `error`, `connected`, ..., `cloud`).
      responses:
        '200':
          description: LED patterns
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: '#/components/schemas/LEDPattern'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    put:
      operationId: updateNodesByNodeIdLedPatterns
      tags:
      - Nodes
      summary: Update LED pattern for one or more states
      description: Updates per-state LED pattern configuration. Pass an array of LEDPattern
        objects keyed by `name`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: '#/components/schemas/LEDPatternUpdate'
      responses:
        '200':
          description: LED patterns updated
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /nodes/{nodeId}/bluetooth:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    get:
      operationId: getNodesByNodeIdBluetooth
      tags:
      - Nodes
      summary: Get Bluetooth LE config/state
      description: Returns the BLE onboarding and diagnostic config from `AW_Bluetooth_Config`
        / `AW_Bluetooth_State`.
      responses:
        '200':
          description: Bluetooth config
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BluetoothConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    put:
      operationId: updateNodesByNodeIdBluetooth
      tags:
      - Nodes
      summary: Configure Bluetooth LE
      description: Sets BLE mode (on/off) and command (on_boarding/diagnostic/locate).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BluetoothConfig'
      responses:
        '200':
          description: Bluetooth configured
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /nodes/{nodeId}/firmware:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    get:
      operationId: getNodesByNodeIdFirmware
      tags:
      - Nodes
      summary: Get current and available firmware info
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Firmware info
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FirmwareInfo'
        '404':
          $ref: '#/components/responses/NotFound'
      description: Returns the firmware version currently running on this node and
        available upgrades. For location-level desired version policy (version matrix),
        use the location-scoped firmware endpoint.
  /nodes/{nodeId}/firmware/upgrade:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    post:
      operationId: createNodesByNodeIdFirmwareUpgrade
      tags:
      - Nodes
      summary: Trigger a firmware upgrade
      description: Sets the target `firmware_version` in `AWLAN_Node` and schedules
        OTA.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - targetVersion
              properties:
                targetVersion:
                  type: string
                  example: 5.4.2-plume
                scheduleAt:
                  type: string
                  format: date-time
                  description: Optional deferred install time; omit to upgrade immediately.
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '202':
          description: Upgrade scheduled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FirmwareUpgradeJob'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
  /nodes/{nodeId}/radios:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    get:
      operationId: getNodesByNodeIdRadios
      tags:
      - Radios
      summary: List radios on a node
      description: Reflects one row per PHY radio from `Wifi_Radio_Config`/`Wifi_Radio_State`.
        Returns live radio state from OVSDB (Wifi_Radio_State). For desired configuration,
        use the location-scoped endpoint.
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: List of radios
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: '#/components/schemas/Radio'
        '404':
          $ref: '#/components/responses/NotFound'
  /nodes/{nodeId}/radios/{radioId}:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    - $ref: '#/components/parameters/RadioId'
    get:
      operationId: getNodesByNodeIdRadiosByRadioId
      tags:
      - Radios
      summary: Get a radio's config and state
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Radio detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Radio'
        '404':
          $ref: '#/components/responses/NotFound'
      description: Returns live radio state for this specific radio from OVSDB. For
        desired config, use the location-scoped radio endpoint.
    patch:
      operationId: updateNodesByNodeIdRadiosByRadioId
      tags:
      - Radios
      summary: Update radio configuration
      description: 'Maps onto an update of the corresponding `Wifi_Radio_Config` row

        (`channel`, `ht_mode`, `tx_power`, `enabled`, `channel_mode`, `country`, `thermal_tx_chainmask`).

        '
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RadioUpdate'
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Updated radio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Radio'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
  /nodes/{nodeId}/radios/{radioId}/channel-plan:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    - $ref: '#/components/parameters/RadioId'
    get:
      operationId: getNodesByNodeIdRadiosByRadioIdChannelPlan
      tags:
      - Radios
      summary: Get automatic channel plan / DFS state
      description: Returns the current channel plan configuration for a radio, including channel and bandwidth settings.
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Channel plan
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChannelPlan'
        '404':
          $ref: '#/components/responses/NotFound'
    put:
      operationId: setNodesByNodeIdRadiosByRadioIdChannelPlan
      tags:
      - Radios
      summary: Set channel selection policy (auto vs. manual + candidate channels)
      description: Sets the channel plan for a radio. Backed by the Wifi_Radio_Config OVSDB table.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChannelPlanUpdate'
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Updated channel plan
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChannelPlan'
        '404':
          $ref: '#/components/responses/NotFound'
  /nodes/{nodeId}/radios/{radioId}/afc:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    - $ref: '#/components/parameters/RadioId'
    get:
      operationId: getNodesByNodeIdRadiosByRadioIdAfc
      tags:
      - Radios
      summary: Get 6 GHz Automated Frequency Coordination (AFC) status
      description: Backed by `Wifi_AFC` and `Wifi_Transmit_Power_Constraints` OVSDB
        tables. Returns per-radio AFC spectrum inquiry status. For location-wide AFC
        overview, use the network-scoped AFC endpoint.
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: AFC status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AfcStatus'
        '404':
          $ref: '#/components/responses/NotFound'
    put:
      operationId: setNodesByNodeIdRadiosByRadioIdAfc
      tags:
      - Radios
      summary: Configure AFC server settings for a 6 GHz radio
      description: Configures Automated Frequency Coordination (AFC) settings for a 6 GHz radio.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AfcConfigUpdate'
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Updated AFC config
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AfcStatus'
        '404':
          $ref: '#/components/responses/NotFound'
  /nodes/{nodeId}/radios/{radioId}/neighbors:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    - $ref: '#/components/parameters/RadioId'
    get:
      operationId: getNodesByNodeIdRadiosByRadioIdNeighbors
      tags:
      - Radios
      summary: List RRM neighbor reports
      description: Backed by `Wifi_VIF_Neighbors` — used for 802.11k neighbor reports
        and mesh topology.
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Neighbor list
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: '#/components/schemas/NeighborReport'
        '404':
          $ref: '#/components/responses/NotFound'
  /customers/{customerId}/locations/{locationId}/networks:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      operationId: getCustomersByCustomerIdLocationsByLocationIdNetworks
      tags:
      - Networks
      summary: List networks (SSIDs) configured at a location
      description: Returns a paginated list of all SSID/VIF networks deployed at a location, backed by Wifi_VIF_Config OVSDB table.
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: List of networks
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: '#/components/schemas/Network'
        '404':
          $ref: '#/components/responses/NotFound'
    post:
      operationId: createCustomersByCustomerIdLocationsByLocationIdNetworks
      tags:
      - Networks
      summary: Create a network (SSID) and fan it out to all eligible nodes/radios
      description: 'Creates the logical SSID definition; the controller inserts a
        `Wifi_VIF_Config`

        row per applicable radio/node and references it from `Wifi_Radio_Config.vif_configs`.

        '
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NetworkCreate'
      responses:
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '201':
          description: Network created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Network'
        '400':
          $ref: '#/components/responses/BadRequest'
  /networks/{networkId}:
    parameters:
    - $ref: '#/components/parameters/NetworkId'
    get:
      operationId: getNetworksByNetworkId
      tags:
      - Networks
      summary: Get a network
      description: Returns details for a single SSID/VIF network, including security mode, VLAN tag, and enabled radios.
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Network detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Network'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: updateNetworksByNetworkId
      tags:
      - Networks
      summary: Update a network (SSID, security, VLAN, MLO settings)
      description: Updates a network (SSID) configuration such as WPA mode, passphrase, or VLAN. Only supplied fields are modified.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NetworkUpdate'
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Updated network
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Network'
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      operationId: deleteNetworksByNetworkId
      tags:
      - Networks
      summary: Delete a network from all nodes
      description: Deletes a network (SSID) and removes it from all radios where it was deployed.
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '204':
          description: Deleted
        '404':
          $ref: '#/components/responses/NotFound'
  /nodes/{nodeId}/radios/{radioId}/vifs:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    - $ref: '#/components/parameters/RadioId'
    get:
      operationId: getNodesByNodeIdRadiosByRadioIdVifs
      tags:
      - Networks
      summary: List per-node VIF deployment state for a radio
      description: Raw per-node/per-radio view of `Wifi_VIF_Config`/`Wifi_VIF_State`,
        one row per deployed network.
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: List of VIFs
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: '#/components/schemas/Vif'
        '404':
          $ref: '#/components/responses/NotFound'
  /nodes/{nodeId}/radios/{radioId}/vifs/{vifId}:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    - $ref: '#/components/parameters/RadioId'
    - $ref: '#/components/parameters/VifId'
    get:
      operationId: getNodesByNodeIdRadiosByRadioIdVifsByVifId
      tags:
      - Networks
      summary: Get per-node VIF deployment detail (incl. MLO link info)
      description: Returns the configuration and operational state of a single VIF on a radio.
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: VIF detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Vif'
        '404':
          $ref: '#/components/responses/NotFound'
  /customers/{customerId}/locations/{locationId}/guest-network:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      operationId: getCustomersByCustomerIdLocationsByLocationIdGuestNetwork
      tags:
      - Networks
      summary: Get guest network configuration
      description: Returns the guest WiFi network configuration for a location, including SSID and access constraints.
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Guest network config
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GuestNetwork'
        '404':
          $ref: '#/components/responses/NotFound'
    put:
      operationId: setCustomersByCustomerIdLocationsByLocationIdGuestNetwork
      tags:
      - Networks
      summary: Enable/configure the guest network
      description: Configures or updates the guest WiFi network for a location.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GuestNetworkUpdate'
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Updated guest network config
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GuestNetwork'
        '404':
          $ref: '#/components/responses/NotFound'
  /customers/{customerId}/locations/{locationId}/devices:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      operationId: getCustomersByCustomerIdLocationsByLocationIdDevices
      tags:
      - Devices
      summary: List connected/known devices at a location
      description: Aggregates `Wifi_Associated_Clients` across all nodes plus historical
        fingerprinting data. Returns connected client devices (by MAC) seen at this
        location. For hardware inventory by serial number, use /inventory/devices.
      parameters:
      - $ref: '#/components/parameters/PageParam'
      - $ref: '#/components/parameters/PageSizeParam'
      - name: connected
        in: query
        description: Filter by current connection state
        schema:
          type: boolean
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Paginated list of devices
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/PaginatedResponse'
                - type: object
                  properties:
                    items:
                      type: array
                      items:
                        $ref: '#/components/schemas/Device'
        '404':
          $ref: '#/components/responses/NotFound'
  /devices/{deviceId}:
    parameters:
    - $ref: '#/components/parameters/DeviceId'
    get:
      operationId: getDevicesByDeviceId
      tags:
      - Devices
      summary: Get a device
      description: Returns details for a single client device, including connection history, data usage, and identity associations.
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Device detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Device'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: updateDevicesByDeviceId
      tags:
      - Devices
      summary: Update device metadata (nickname, device group)
      description: Updates device properties such as friendly name, device type label, or grouping tags.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DeviceUpdate'
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Updated device
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Device'
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      operationId: deleteDevicesByDeviceId
      tags:
      - Devices
      summary: Forget a device (remove from inventory/history)
      description: Deletes a client device record from the cloud but does not prevent the device from reconnecting.
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '204':
          description: Forgotten
        '404':
          $ref: '#/components/responses/NotFound'
  /devices/{deviceId}/actions/block:
    parameters:
    - $ref: '#/components/parameters/DeviceId'
    post:
      operationId: createDevicesByDeviceIdActionsBlock
      tags:
      - Devices
      summary: Block a device from the network
      description: Applies an access-control deny entry (MAC-based) and deauthenticates
        active associations.
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '202':
          $ref: '#/components/responses/Accepted'
        '404':
          $ref: '#/components/responses/NotFound'
  /devices/{deviceId}/actions/unblock:
    parameters:
    - $ref: '#/components/parameters/DeviceId'
    post:
      operationId: createDevicesByDeviceIdActionsUnblock
      tags:
      - Devices
      summary: Unblock a previously blocked device
      description: Removes a block on a client device, allowing it to reconnect.
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '202':
          $ref: '#/components/responses/Accepted'
        '404':
          $ref: '#/components/responses/NotFound'
  /devices/{deviceId}/steering:
    parameters:
    - $ref: '#/components/parameters/DeviceId'
    get:
      operationId: getDevicesByDeviceIdSteering
      tags:
      - Steering
      summary: Get per-device steering state/history
      description: Returns the steering history and current recommendations for a specific client device.
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Steering state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeviceSteeringState'
        '404':
          $ref: '#/components/responses/NotFound'
  /devices/{deviceId}/actions/steer:
    parameters:
    - $ref: '#/components/parameters/DeviceId'
    post:
      operationId: createDevicesByDeviceIdActionsSteer
      tags:
      - Steering
      summary: Trigger an immediate steering attempt (802.11v BTM request)
      description: Sends a BSS Transition Management (BTM) request to steer the device to a better AP or band.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                targetNodeId:
                  type: string
                targetBand:
                  $ref: '#/components/schemas/Band'
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '202':
          description: Steering attempt scheduled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SteeringJob'
        '404':
          $ref: '#/components/responses/NotFound'
  /customers/{customerId}/locations/{locationId}/steering-policy:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      operationId: getCustomersByCustomerIdLocationsByLocationIdSteeringPolicy
      tags:
      - Steering
      summary: Get the location's band/client steering policy (RRM)
      description: Returns the current band/client steering policy for a location.
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Steering policy
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SteeringPolicy'
        '404':
          $ref: '#/components/responses/NotFound'
    put:
      operationId: setCustomersByCustomerIdLocationsByLocationIdSteeringPolicy
      tags:
      - Steering
      summary: Update the location's steering policy
      description: Configures band and client steering policy for a location, including RSSI thresholds and band preference.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SteeringPolicyUpdate'
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Updated policy
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SteeringPolicy'
        '404':
          $ref: '#/components/responses/NotFound'
  /nodes/{nodeId}/speedtest:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    get:
      operationId: getNodesByNodeIdSpeedtest
      tags:
      - Telemetry
      summary: Get the most recent speedtest result
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Latest speedtest result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SpeedTestResult'
        '404':
          $ref: '#/components/responses/NotFound'
      description: Triggers and returns per-node speed test results. For aggregated
        speed test history across the location, use the location-scoped speedtest/history
        endpoint.
    post:
      operationId: createNodesByNodeIdSpeedtest
      tags:
      - Telemetry
      summary: Trigger a new speedtest
      description: Triggers a new speedtest on the node. Use the optional `providerId`
        query parameter to target a specific speedtest provider configured at `/speedtest/providers`.
        Without `providerId`, the built-in default provider is used.
      parameters:
      - name: providerId
        in: query
        schema:
          type: string
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '202':
          description: Speedtest scheduled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SpeedTestJob'
        '404':
          $ref: '#/components/responses/NotFound'
  /nodes/{nodeId}/speedtest/{testId}:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    - name: testId
      in: path
      required: true
      schema:
        type: string
    get:
      operationId: getNodesByNodeIdSpeedtestByTestId
      tags:
      - Telemetry
      summary: Get a specific speedtest result by ID
      description: Returns the result of a single speedtest, including upload/download throughput, latency, and jitter.
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Speedtest result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SpeedTestResult'
        '404':
          $ref: '#/components/responses/NotFound'
  /nodes/{nodeId}/speedtest/providers:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    get:
      tags:
      - Agents
      summary: List configured speedtest providers
      description: Returns all speedtest provider configurations for this node. Multiple
        providers of different types (built-in, Ookla, iPerf3, custom) can coexist,
        each with independent schedules, servers, and thresholds. Use paginated response
        when many providers are configured.
      parameters:
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: per_page
        in: query
        schema:
          type: integer
          default: 50
          maximum: 200
      responses:
        '200':
          description: Paginated list of speedtest provider configurations
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/PaginatedResponse'
                - type: object
                  properties:
                    items:
                      type: array
                      items:
                        $ref: '#/components/schemas/SpeedtestProviderConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      tags:
      - Agents
      summary: Register a speedtest provider
      description: Adds a speedtest provider configuration for this node (e.g., Ookla
        on server-A every hour, iPerf3 on server-B every 4 hours). Multiple providers
        of different types can coexist; each gets its own schedule, server, thresholds,
        and callback.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SpeedtestProviderConfig'
      responses:
        '201':
          description: Speedtest provider registered
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SpeedtestProviderConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /nodes/{nodeId}/speedtest/providers/{providerId}:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    - name: providerId
      in: path
      required: true
      schema:
        type: string
    get:
      tags:
      - Agents
      summary: Get a speedtest provider configuration
      description: Returns the configuration for a specific speedtest provider on
        this node, identified by its provider ID.
      responses:
        '200':
          description: Speedtest provider configuration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SpeedtestProviderConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Provider not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    put:
      tags:
      - Agents
      summary: Update a speedtest provider configuration
      description: Updates the configuration for a specific speedtest provider, including
        schedule, server URL, thresholds, and interface binding.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SpeedtestProviderConfig'
      responses:
        '200':
          description: Speedtest provider configuration updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SpeedtestProviderConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Provider not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    delete:
      tags:
      - Agents
      summary: Remove a speedtest provider
      description: Removes a speedtest provider configuration. Any pending or scheduled
        tests for this provider are cancelled. Historical results from this provider
        remain accessible.
      responses:
        '204':
          description: Speedtest provider removed
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Provider not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /nodes/{nodeId}/stats:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    get:
      operationId: getNodesByNodeIdStats
      tags:
      - Telemetry
      summary: Get recent stats/telemetry samples
      description: 'Aggregates periodic MQTT/protobuf telemetry reports driven by
        `Wifi_Stats_Config`

        (client, survey, capacity, neighbor stats) into a normalized time series.

        '
      parameters:
      - name: from
        in: query
        schema:
          type: string
          format: date-time
      - name: to
        in: query
        schema:
          type: string
          format: date-time
      - name: metric
        in: query
        schema:
          type: string
          enum:
          - cpu
          - memory
          - uptime
          - tx_bytes
          - rx_bytes
          - tx_rate
          - rx_rate
          - channel_utilization
          - noise_floor
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Stats samples
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: '#/components/schemas/StatsSample'
        '404':
          $ref: '#/components/responses/NotFound'
  /nodes/{nodeId}/diagnostics:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    get:
      operationId: getNodesByNodeIdDiagnostics
      tags:
      - Telemetry
      summary: Get current diagnostic/health summary
      description: Returns the current diagnostic status and available test results for a node.
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Diagnostics summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DiagnosticsSummary'
        '404':
          $ref: '#/components/responses/NotFound'
  /nodes/{nodeId}/diagnostics/logs:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    post:
      operationId: createNodesByNodeIdDiagnosticsLogs
      tags:
      - Telemetry
      summary: Request an on-demand log/crash-report pull from the node
      description: Triggers a log pull from the node for cloud-based analysis.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                includeCrashReports:
                  type: boolean
                  default: true
                includeOvsdbDump:
                  type: boolean
                  default: false
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '202':
          description: Log pull scheduled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LogPullJob'
        '404':
          $ref: '#/components/responses/NotFound'
  /nodes/{nodeId}/diagnostics/crashes:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    get:
      operationId: getNodesByNodeIdDiagnosticsCrashes
      tags:
      - Telemetry
      summary: List recent crash/restart reports for a node
      description: Returns crash reports and full OpenSync restart reports received
        from the node on the `Crash.Reports` MQTT topic. These are published by the
        Device Manager (DM) when a process crashes or when OpenSync restarts.
      parameters:
      - $ref: '#/components/parameters/PageParam'
      - $ref: '#/components/parameters/PageSizeParam'
      responses:
        '200':
          description: Paginated crash reports
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/PaginatedResponse'
                - type: object
                  properties:
                    items:
                      type: array
                      items:
                        $ref: '#/components/schemas/CrashReport'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /nodes/{nodeId}/uplink:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    get:
      operationId: getNodesByNodeIdUplink
      tags:
      - Connectivity
      summary: Get WAN/backhaul uplink status
      description: Backed by `Connection_Manager_Uplink` (per-interface priority,
        `is_used`, `has_L2/L3` connectivity).
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Uplink status
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: '#/components/schemas/UplinkStatus'
        '404':
          $ref: '#/components/responses/NotFound'
  /nodes/{nodeId}/backhaul:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    get:
      operationId: getNodesByNodeIdBackhaul
      tags:
      - Connectivity
      summary: Get mesh backhaul link info (for extenders/pods)
      description: Returns the current backhaul connection status and link metrics for a mesh node.
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Backhaul link info
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BackhaulLink'
        '404':
          $ref: '#/components/responses/NotFound'
  /customers/{customerId}/locations/{locationId}/security/policy:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      operationId: getCustomersByCustomerIdLocationsByLocationIdSecurityPolicy
      tags:
      - Security
      summary: Get threat protection / DPI tagging policy
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Security policy
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SecurityPolicy'
        '404':
          $ref: '#/components/responses/NotFound'
      description: Returns threat protection / DPI tagging policy only. For full security
        configuration including content filtering and parental controls, use /security.
    put:
      operationId: setCustomersByCustomerIdLocationsByLocationIdSecurityPolicy
      tags:
      - Security
      summary: Update threat protection policy
      description: Updates the security policy for a location, configuring threat detection and containment.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SecurityPolicyUpdate'
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Updated policy
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SecurityPolicy'
        '404':
          $ref: '#/components/responses/NotFound'
  /customers/{customerId}/locations/{locationId}/security/threats:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      operationId: getCustomersByCustomerIdLocationsByLocationIdSecurityThreats
      tags:
      - Security
      summary: List detected threat events
      description: Returns a paginated list of detected security threats at a location.
      parameters:
      - $ref: '#/components/parameters/PageParam'
      - $ref: '#/components/parameters/PageSizeParam'
      - name: severity
        in: query
        schema:
          type: string
          enum:
          - low
          - medium
          - high
          - critical
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Paginated threat events
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/PaginatedResponse'
                - type: object
                  properties:
                    items:
                      type: array
                      items:
                        $ref: '#/components/schemas/ThreatEvent'
        '404':
          $ref: '#/components/responses/NotFound'
  /customers/{customerId}/locations/{locationId}/content-filtering:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      operationId: getCustomersByCustomerIdLocationsByLocationIdContentFiltering
      tags:
      - Security
      summary: Get content filtering (DNS category blocking) config
      description: Returns the current content filtering configuration for a location.
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Content filtering config
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContentFilteringConfig'
        '404':
          $ref: '#/components/responses/NotFound'
    put:
      operationId: setCustomersByCustomerIdLocationsByLocationIdContentFiltering
      tags:
      - Security
      summary: Update content filtering config
      description: Configures content filtering for a location, including blocked categories and allow/deny lists.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ContentFilteringConfigUpdate'
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Updated config
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContentFilteringConfig'
        '404':
          $ref: '#/components/responses/NotFound'
  /customers/{customerId}/locations/{locationId}/content-filtering/provider:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Agents
      summary: Get content filtering provider configuration
      description: Returns the content filtering provider selection and settings.
        Controls which DNS filtering backend is used for DNS-based content filtering
        at this location.
      responses:
        '200':
          description: Content filtering provider configuration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContentFilteringProviderConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    put:
      tags:
      - Agents
      summary: Update content filtering provider configuration
      description: Sets the content filtering provider and its backend-specific settings
        (provider type, server URL, API key, profile ID, category lists). The `apiKey`
        field is write-only and never returned in responses.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ContentFilteringProviderConfig'
      responses:
        '200':
          description: Content filtering provider configuration updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContentFilteringProviderConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/parental-controls:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      operationId: getCustomersByCustomerIdLocationsByLocationIdParentalControls
      tags:
      - Security
      summary: List parental control profiles
      description: Returns a paginated list of parental control profiles at a location.
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: List of profiles
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: '#/components/schemas/ParentalControlProfile'
        '404':
          $ref: '#/components/responses/NotFound'
    post:
      operationId: createCustomersByCustomerIdLocationsByLocationIdParentalControls
      tags:
      - Security
      summary: Create a parental control profile
      description: Creates a new parental control profile with content filtering and time scheduling.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ParentalControlProfileCreate'
      responses:
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '201':
          description: Profile created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ParentalControlProfile'
        '400':
          $ref: '#/components/responses/BadRequest'
  /customers/{customerId}/locations/{locationId}/parental-controls/{profileId}:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    - name: profileId
      in: path
      required: true
      schema:
        type: string
    get:
      operationId: getCustomersByCustomerIdLocationsByLocationIdParentalControlsByProfileId
      tags:
      - Security
      summary: Get a parental control profile
      description: Returns a single parental control profile with device assignments and schedules.
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Profile detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ParentalControlProfile'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: updateCustomersByCustomerIdLocationsByLocationIdParentalControlsByProfileId
      tags:
      - Security
      summary: Update a parental control profile
      description: Updates a parental control profile. Only supplied fields are modified.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ParentalControlProfileUpdate'
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Updated profile
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ParentalControlProfile'
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      operationId: deleteCustomersByCustomerIdLocationsByLocationIdParentalControlsByProfileId
      tags:
      - Security
      summary: Delete a parental control profile
      description: Deletes a parental control profile and removes it from all assigned devices.
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '204':
          description: Deleted
        '404':
          $ref: '#/components/responses/NotFound'
  /customers/{customerId}/locations/{locationId}/captive-portal:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      operationId: getCustomersByCustomerIdLocationsByLocationIdCaptivePortal
      tags:
      - Security
      summary: Get captive portal configuration
      description: Returns the current captive portal configuration for a location.
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Captive portal config
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CaptivePortalConfig'
        '404':
          $ref: '#/components/responses/NotFound'
    put:
      operationId: setCustomersByCustomerIdLocationsByLocationIdCaptivePortal
      tags:
      - Security
      summary: Configure captive portal (e.g. for guest/onboarding networks)
      description: Configures the captive portal for a location, including splash page and authentication.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CaptivePortalConfigUpdate'
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Updated config
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CaptivePortalConfig'
        '404':
          $ref: '#/components/responses/NotFound'
  /customers/{customerId}/locations/{locationId}/persons:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      operationId: getCustomersByCustomerIdLocationsByLocationIdPersons
      tags:
      - Presence
      summary: List persons registered at a location
      description: Returns a paginated list of persons (household members) at a location.
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: List of persons
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: '#/components/schemas/Person'
        '404':
          $ref: '#/components/responses/NotFound'
    post:
      operationId: createCustomersByCustomerIdLocationsByLocationIdPersons
      tags:
      - Presence
      summary: Register a person (and associate devices/BLE tokens)
      description: Creates a new person (household member) at a location.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PersonCreate'
      responses:
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '201':
          description: Person created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Person'
        '400':
          $ref: '#/components/responses/BadRequest'
  /customers/{customerId}/locations/{locationId}/persons/{personId}:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    - $ref: '#/components/parameters/PersonId'
    get:
      operationId: getPersonsByPersonId
      tags:
      - Presence
      summary: Get a person
      description: Returns details for a single person, including associated devices and presence history.
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Person detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Person'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: updatePersonsByPersonId
      tags:
      - Presence
      summary: Update a person
      description: Updates person details such as name or avatar. Only supplied fields are modified.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PersonUpdate'
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Updated person
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Person'
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      operationId: deletePersonsByPersonId
      tags:
      - Presence
      summary: Delete a person
      description: Deletes a person record. Associated devices remain in the system.
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '204':
          description: Deleted
        '404':
          $ref: '#/components/responses/NotFound'
  /customers/{customerId}/locations/{locationId}/rooms:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      operationId: getCustomersByCustomerIdLocationsByLocationIdRooms
      tags:
      - Presence
      summary: List rooms/zones inferred or defined for BLE presence
      description: Returns a paginated list of rooms defined at a location for presence tracking and device grouping.
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: List of rooms
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: '#/components/schemas/Room'
        '404':
          $ref: '#/components/responses/NotFound'
  /customers/{customerId}/locations/{locationId}/presence-events:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      operationId: getCustomersByCustomerIdLocationsByLocationIdPresenceEvents
      tags:
      - Presence
      summary: List recent room-level presence events (BLE-based)
      description: Returns recent BLE-based room-level presence events for persons at a location.
      parameters:
      - $ref: '#/components/parameters/PageParam'
      - $ref: '#/components/parameters/PageSizeParam'
      - name: personId
        in: query
        schema:
          type: string
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Paginated presence events
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/PaginatedResponse'
                - type: object
                  properties:
                    items:
                      type: array
                      items:
                        $ref: '#/components/schemas/PresenceEvent'
        '404':
          $ref: '#/components/responses/NotFound'
  /customers/{customerId}/locations/{locationId}/vpn:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      operationId: getCustomersByCustomerIdLocationsByLocationIdVpn
      tags:
      - VPN
      summary: Get VPN configuration for a location
      description: Returns the VPN configuration for a location, including site-to-site and client VPN settings.
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: VPN config
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VpnConfig'
        '404':
          $ref: '#/components/responses/NotFound'
    put:
      operationId: setCustomersByCustomerIdLocationsByLocationIdVpn
      tags:
      - VPN
      summary: Configure/enable VPN (client or site-to-site tunnel)
      description: Configures VPN for a location, including IPSec tunnels and WireGuard peers.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VpnConfigUpdate'
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Updated VPN config
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VpnConfig'
        '404':
          $ref: '#/components/responses/NotFound'
  /customers/{customerId}/webhooks:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    get:
      operationId: getCustomersByCustomerIdWebhooks
      tags:
      - Webhooks
      summary: List webhook subscriptions for an customer
      description: Returns a paginated list of outbound webhook subscriptions for a customer.
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: List of webhooks
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: '#/components/schemas/Webhook'
        '404':
          $ref: '#/components/responses/NotFound'
    post:
      operationId: createCustomersByCustomerIdWebhooks
      tags:
      - Webhooks
      summary: Create a webhook subscription
      description: Creates a new outbound webhook subscription for async cloud-to-partner event notifications.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WebhookCreate'
      responses:
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '201':
          description: Webhook created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Webhook'
        '400':
          $ref: '#/components/responses/BadRequest'
  /webhooks/{webhookId}:
    parameters:
    - name: webhookId
      in: path
      required: true
      schema:
        type: string
    delete:
      operationId: deleteWebhooksByWebhookId
      tags:
      - Webhooks
      summary: Delete a webhook subscription
      description: Deletes a webhook subscription and stops event delivery to the configured endpoint.
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '204':
          description: Deleted
        '404':
          $ref: '#/components/responses/NotFound'
  /customers/{customerId}/locations/{locationId}/events:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      operationId: getCustomersByCustomerIdLocationsByLocationIdEvents
      tags:
      - Webhooks
      summary: List recent cloud events for a location (polling alternative to webhooks)
      description: Returns a paginated list of recent events at a location.
      parameters:
      - $ref: '#/components/parameters/PageParam'
      - $ref: '#/components/parameters/PageSizeParam'
      - name: type
        in: query
        schema:
          $ref: '#/components/schemas/EventType'
      responses:
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '200':
          description: Paginated events
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/PaginatedResponse'
                - type: object
                  properties:
                    items:
                      type: array
                      items:
                        $ref: '#/components/schemas/CloudEvent'
        '404':
          $ref: '#/components/responses/NotFound'
  /customers:
    get:
      tags:
      - Customers
      summary: List customers
      parameters:
      - name: status
        in: query
        schema:
          type: string
          enum:
          - active
          - suspended
          - cancelled
      - name: search
        in: query
        description: Search by name, email, phone, or account_id
        schema:
          type: string
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: per_page
        in: query
        schema:
          type: integer
          default: 50
          maximum: 200
      responses:
        '200':
          description: Customer list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/Customer'
                  meta:
                    $ref: '#/components/schemas/Pagination'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Returns a paginated list of all customers in the ISP tenant. Supports
        filtering by status, free-text search across name/email/phone/account_id,
        and pagination.
    post:
      tags:
      - Customers
      summary: Create customer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CustomerCreate'
      responses:
        '201':
          description: Customer created
          headers:
            Location:
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/Customer'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Creates a new customer account. The customer is provisioned in
        the default cloud shard unless overridden. Returns 201 with a Location header
        pointing to the new resource.
  /customers/{customerId}/cloud:
    get:
      tags:
      - Customers
      summary: Get customer's cloud assignment
      description: Returns which Plume cloud shard/environment the customer is assigned
        to.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Cloud assignment
          content:
            application/json:
              schema:
                type: object
                properties:
                  cloud_id:
                    type: string
                  region:
                    type: string
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/onboarding:
    get:
      tags:
      - Customers
      summary: Get onboarding status
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Onboarding status
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Returns the current onboarding status for a customer. Indicates
        whether the customer has completed the initial setup workflow. Returns customer-level
        onboarding wizard status and steps. For per-node onboarding/activation status,
        use the node-scoped onboarding endpoint.
    post:
      tags:
      - Customers
      summary: Complete onboarding
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                auto_approve:
                  type: boolean
                  default: true
      responses:
        '200':
          description: Onboarding completed
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Completes the onboarding workflow for a customer. When auto_approve
        is true (default), the customer is automatically approved and activated.
  /customers/{customerId}/migration:
    get:
      tags:
      - Customers
      summary: Get migration status
      description: Check whether a customer has pending or in-progress migration between
        clouds.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Migration status
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    enum:
                    - none
                    - pending
                    - in_progress
                    - completed
                    - failed
                  target_cloud:
                    type: string
                  started_at:
                    type: string
                    format: date-time
                  completed_at:
                    type: string
                    format: date-time
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      tags:
      - Customers
      summary: Migrate customer to another cloud
      description: Initiates migration of a customer to a different Plume cloud shard.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - target_cloud
              properties:
                target_cloud:
                  type: string
                force:
                  type: boolean
                  default: false
      responses:
        '202':
          description: Migration initiated
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/clients-by-ssid:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Networks
      summary: List clients grouped by SSID
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: SSID client breakdown with per-client connectivity details
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/ConnectedClient'
                  meta:
                    $ref: '#/components/schemas/Pagination'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Returns a breakdown of connected clients grouped by SSID. Useful
        for understanding per-SSID usage and planning capacity.
  /customers/{customerId}/locations/{locationId}/guest-sessions:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Networks
      summary: List active guest sessions
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Guest session list
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Lists all active guest WiFi sessions including client MAC, IP,
        login time, and remaining session duration.
    delete:
      tags:
      - Networks
      summary: Revoke all guest sessions
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '204':
          description: Sessions revoked
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Immediately revokes all active guest WiFi sessions. All currently
        connected guest clients are disconnected.
  /customers/{customerId}/locations/{locationId}/profiles:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Devices
      summary: List device profiles
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Profile list
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Lists all device profiles for the location. Profiles group devices
        for content filtering, time limits, and access controls.
    post:
      tags:
      - Devices
      summary: Create profile
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProfileCreate'
      responses:
        '201':
          description: Profile created
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Creates a new device profile with optional content filtering categories,
        SafeSearch, and daily time limits with bedtime schedule.
  /customers/{customerId}/locations/{locationId}/profiles/{profileId}:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Devices
      summary: Get profile details
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: ProfileID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Profile details
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Returns the full profile configuration including assigned devices,
        content filtering rules, and time limits.
    patch:
      tags:
      - Devices
      summary: Update profile
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: ProfileID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProfileUpdate'
      responses:
        '200':
          description: Profile updated
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Updates a profile's name, icon, color, content filtering rules,
        or time limits.
    delete:
      tags:
      - Devices
      summary: Delete profile
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: ProfileID
        in: path
        required: true
        schema:
          type: string
      responses:
        '204':
          description: Profile deleted
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Deletes a profile. Devices previously assigned to this profile
        return to the default (unfiltered) state.
  /customers/{customerId}/locations/{locationId}/profiles/{profileId}/devices:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Devices
      summary: List devices assigned to a profile
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: ProfileID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Device list
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Lists all client devices currently assigned to this profile by
        MAC address.
    put:
      tags:
      - Devices
      summary: Assign devices to profile
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: ProfileID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                device_macs:
                  type: array
                  items:
                    type: string
      responses:
        '200':
          description: Devices assigned
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Assigns a list of client devices (by MAC) to a profile. Replaces
        any previous device assignments for this profile.
  /customers/{customerId}/locations/{locationId}/controls/focus:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Networks
      summary: List focus schedules
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Focus schedules
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Lists all focus mode schedules. Focus mode blocks specified app
        categories for designated profiles/devices during scheduled periods.
    post:
      tags:
      - Networks
      summary: Create focus schedule
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FocusSchedule'
      responses:
        '201':
          description: Focus schedule created
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Creates a focus mode schedule. Specify the profile or devices,
        schedule rules, and which app categories to block.
  /customers/{customerId}/locations/{locationId}/controls/focus/{scheduleId}:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    delete:
      tags:
      - Networks
      summary: Remove focus schedule
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: ScheduleID
        in: path
        required: true
        schema:
          type: string
      responses:
        '204':
          description: Focus schedule removed
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Removes a focus mode schedule. The affected profiles/devices resume
        normal unfiltered access.
  /customers/{customerId}/locations/{locationId}/controls/timeout:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    post:
      tags:
      - Networks
      summary: Pause connectivity for a profile or device
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                profile_id:
                  type: string
                device_mac:
                  type: string
                duration_min:
                  type: integer
                  default: 60
      responses:
        '200':
          description: Timeout applied
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Pauses internet connectivity for a specified profile or device
        for the given duration. Use DELETE to resume connectivity early.
    delete:
      tags:
      - Networks
      summary: Remove timeout / resume connectivity
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '204':
          description: Timeout removed
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Immediately resumes internet connectivity for any paused profile
        or device (Timeout).
  /customers/{customerId}/locations/{locationId}/controls/boost:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Networks
      summary: List active traffic boosts
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Active boosts
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Lists all active traffic boost rules. Traffic boost gives priority
        (high/realtime) to specified profiles or devices.
    post:
      tags:
      - Networks
      summary: Create traffic boost
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TrafficBoost'
      responses:
        '201':
          description: Traffic boost created
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Creates a traffic boost rule for a profile or device. Boosting
        gives higher QoS priority for gaming, streaming, or video calls.
  /customers/{customerId}/locations/{locationId}/controls/home-away:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Networks
      summary: Get home/away configuration
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Home/away config
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Returns the home/away configuration: presence detection, automatic
        security arming, and security device prioritization.'
    put:
      tags:
      - Networks
      summary: Configure home/away behavior
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                presence_detection:
                  type: boolean
                auto_arm_security:
                  type: boolean
                prioritize_security_devices:
                  type: boolean
      responses:
        '200':
          description: Home/away updated
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Configures home/away behavior. When location is detected as 'away',
        the system can auto-arm security, pause devices, or prioritize security sensors.
  /customers/{customerId}/locations/{locationId}/client-freeze:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      operationId: getCustomersByCustomerIdLocationsByLocationIdClientFreeze
      tags:
      - Networks
      summary: Get client freeze configuration
      description: Returns configured client connectivity freeze schedules from `Client_Freeze_Config`.
        A freeze blocks specified devices from the network during scheduled periods.
      responses:
        '200':
          description: Client freeze config
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClientFreezeConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    put:
      operationId: updateCustomersByCustomerIdLocationsByLocationIdClientFreeze
      tags:
      - Networks
      summary: Configure client freeze
      description: Sets a client connectivity freeze — schedule or always-on block
        for specified MAC addresses. Backed by `Client_Freeze_Config`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ClientFreezeConfig'
      responses:
        '200':
          description: Freeze configured
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/iot-network:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Networks
      summary: Get IoT network configuration
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: IoT network config
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/IoTNetworkConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Returns the IoT network configuration: SSID, security, isolation
        from primary network, VLAN ID, Zigbee coordinator status, and client limits.'
    put:
      tags:
      - Networks
      summary: Configure IoT network
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/IoTNetworkConfig'
      responses:
        '200':
          description: IoT network configured
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Configures the dedicated IoT network. IoT devices are isolated
        on a separate SSID with optional VLAN tagging for network segmentation.
  /customers/{customerId}/locations/{locationId}/iot-devices:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Networks
      summary: List IoT devices
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: IoT device list
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Lists all registered IoT devices on the IoT network including type,
        connection status, and signal metrics.
  /customers/{customerId}/locations/{locationId}/zigbee:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Networks
      summary: Get Zigbee coordinator status
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Zigbee status
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Returns the Zigbee coordinator status: enabled state, channel,
        and current permit-join window status.'
    post:
      tags:
      - Networks
      summary: Permit Zigbee device joining
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                duration_sec:
                  type: integer
                  default: 180
      responses:
        '200':
          description: Joining enabled
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Opens the Zigbee network for device joining for the specified duration
        in seconds. New Zigbee devices can pair during this window.
  /customers/{customerId}/locations/{locationId}/lte:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Connectivity
      summary: Get LTE backup status and config
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: LTE status
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/LTEConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Returns LTE backup configuration: failover mode (automatic/manual/disabled),
        APN settings, active SIM slot, data limits, and notification thresholds.'
    put:
      tags:
      - Connectivity
      summary: Configure LTE backup
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LTEConfig'
      responses:
        '200':
          description: LTE configured
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Configures LTE cellular backup for WAN failover. Sets APN, authentication,
        failover mode, data caps, and force-LTE override.
  /customers/{customerId}/locations/{locationId}/lte/modem:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Connectivity
      summary: Get LTE modem details
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Modem details
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Returns detailed LTE modem information: signal strength (RSRP/RSRQ/SINR),
        connection status, network operator, IMEI, and IMSI.'
  /customers/{customerId}/locations/{locationId}/lte/usage:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Connectivity
      summary: Get LTE data usage
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Data usage
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Returns LTE data usage for the current billing period including
        download, upload, total GB, and percentage of data cap consumed.
  /customers/{customerId}/locations/{locationId}/lte/test:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    post:
      tags:
      - Connectivity
      summary: Test LTE failover
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '202':
          description: Failover test initiated
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Triggers an LTE failover test. Forces the gateway to switch to
        LTE for a brief period and reports success/failure. Returns 202 Accepted.
  /customers/{customerId}/locations/{locationId}/lte/esim:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Connectivity
      summary: Get eSIM profile status
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: eSIM status
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Returns the current eSIM profile status: installed profiles, activation
        state, and carrier information.'
    post:
      tags:
      - Connectivity
      summary: Activate eSIM profile
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                activation_code:
                  type: string
                profile_name:
                  type: string
      responses:
        '200':
          description: eSIM activated
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Activates a new eSIM profile using an activation code. Supports
        remote provisioning without physical SIM card swaps.
  /customers/{customerId}/locations/{locationId}/captive-portal/analytics:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Security
      summary: Get portal login analytics
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: period
        in: query
        schema:
          type: string
          enum:
          - day
          - week
          - month
          default: day
      responses:
        '200':
          description: Portal analytics
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Returns portal login analytics: total sessions, unique users,
        login methods used, and session duration distribution for the specified period.'
  /customers/{customerId}/locations/{locationId}/employee-access:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Networks
      summary: Get employee access configuration
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Employee access config
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Returns employee WiFi network configuration: SSID, RADIUS server
        settings, and per-employee VLAN/bandwidth/role/schedule assignments.'
    put:
      tags:
      - Networks
      summary: Configure employee network
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                ssid:
                  type: string
                auth_mode:
                  type: string
                  enum:
                  - radius
                  - psk
                radius:
                  type: object
                  properties:
                    primary_ip:
                      type: string
                    primary_port:
                      type: integer
                      default: 1812
                    primary_secret:
                      type: string
                    accounting_enabled:
                      type: boolean
                employees:
                  type: array
                  items:
                    type: object
                    properties:
                      name:
                        type: string
                      username:
                        type: string
                      role:
                        type: string
                        enum:
                        - staff
                        - manager
                        - admin
                      vlan_id:
                        type: integer
                      bandwidth_kbps:
                        type: integer
                      schedule:
                        type: string
                      enabled:
                        type: boolean
      responses:
        '200':
          description: Employee access configured
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Configures the employee WiFi network with RADIUS (802.1X) authentication.
        Define the RADIUS server, per-employee access levels, VLAN assignment, and
        bandwidth limits.
  /customers/{customerId}/locations/{locationId}/activity:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Telemetry
      summary: Get network activity feed
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: since
        in: query
        schema:
          type: string
          format: date-time
      - name: limit
        in: query
        schema:
          type: integer
          default: 50
      responses:
        '200':
          description: Activity feed
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Returns the network activity feed — a chronological log of events
        such as client connections, disconnections, speed tests, threats, and configuration
        changes.
  /customers/{customerId}/locations/{locationId}/activity/motion:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Telemetry
      summary: Get motion/sense events
      description: Plume Sense motion detection events (if enabled).
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: since
        in: query
        schema:
          type: string
          format: date-time
      responses:
        '200':
          description: Motion events
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/radios:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Radios
      summary: List radios
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Radio list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/Radio'
                  meta:
                    $ref: '#/components/schemas/Pagination'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Lists all WiFi radios across all nodes at the location with their
        band, channel, mode, TX power, DFS status, and current utilization. Returns
        desired radio configuration. For live OVSDB state per node, use the node-scoped
        radios endpoint.
  /customers/{customerId}/locations/{locationId}/radios/{radioId}:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Radios
      summary: Get radio details
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: RadioName
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Radio details
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/Radio'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Returns detailed radio configuration and state for a specific radio
        interface (e.g., 'wifi0', 'wifi1', 'wifi2'). Returns desired radio configuration
        for this radio. For live OVSDB state, use the node-scoped radio endpoint.
    patch:
      tags:
      - Radios
      summary: Configure radio
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: RadioName
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RadioUpdate'
      responses:
        '200':
          description: Radio configured
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Configures a specific radio: enable/disable, channel plan (auto/manual/cloud/ACS),
        channel width (HT20-320), TX power, country code, and thermal limits.'
  /customers/{customerId}/locations/{locationId}/radios/{radioId}/scan:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    post:
      tags:
      - Radios
      summary: Trigger channel scan
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: RadioName
        in: path
        required: true
        schema:
          type: string
      responses:
        '202':
          description: Scan initiated
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Triggers an on-demand channel scan for the specified radio. Results
        are reflected in the radio's state after scan completion. Returns 202 Accepted.
  /customers/{customerId}/locations/{locationId}/radios/dfs-events:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Radios
      summary: Get DFS radar events
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: since
        in: query
        schema:
          type: string
          format: date-time
      responses:
        '200':
          description: DFS events
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Returns DFS (Dynamic Frequency Selection) radar detection events.
        Shows when radar was detected and the resulting channel change. Filterable
        by time range.
  /customers/{customerId}/locations/{locationId}/security:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Security
      summary: Get security configuration
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Security config
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/SecurityConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Returns the full security configuration: Plume Shield threat protection
        (malware/phishing/botnet/intrusion), IoT protection with auto-isolation and
        VLAN. Returns the full security configuration for the location, including
        threat protection policy, content filtering, and parental controls. Use /security/policy
        for threat protection settings only.'
    put:
      tags:
      - Security
      summary: Set security configuration
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SecurityConfig'
      responses:
        '200':
          description: Security config updated
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Configures network security. Enable/disable Shield protection,
        set protection level, and configure IoT device automatic isolation with VLAN
        assignment.
  /customers/{customerId}/locations/{locationId}/security/threats/{threatId}:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    delete:
      tags:
      - Security
      summary: Dismiss threat
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: ThreatID
        in: path
        required: true
        schema:
          type: string
      responses:
        '204':
          description: Threat dismissed
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Dismisses a threat event. The threat is marked as acknowledged
        and removed from the active threats list.
  /customers/{customerId}/locations/{locationId}/dhcp:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Connectivity
      summary: Get DHCP configuration
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: DHCP config
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/DHCPConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Returns the DHCP server configuration: enabled state, subnet,
        address pool range, lease time, DNS servers, gateway address, and custom DHCP
        options.'
    put:
      tags:
      - Connectivity
      summary: Update DHCP configuration
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DHCPConfig'
      responses:
        '200':
          description: DHCP config updated
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Configures the DHCP server on the gateway node. Set the LAN subnet,
        IP pool, lease duration, DNS servers, gateway, and custom DHCP options.
  /customers/{customerId}/locations/{locationId}/dhcp/leases:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Connectivity
      summary: List active DHCP leases
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Active DHCP leases with client IP, MAC, hostname, and expiry
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/DHCPLease'
                  meta:
                    $ref: '#/components/schemas/Pagination'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Lists all active DHCP leases: client hostname, MAC address, assigned
        IP, lease expiry time, and whether the lease is static or dynamic.'
  /customers/{customerId}/locations/{locationId}/dhcp/reservations:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Connectivity
      summary: List DHCP reservations
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: DHCP static reservations
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/DHCPReservation'
                  meta:
                    $ref: '#/components/schemas/Pagination'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Lists all DHCP reservations (static IP assignments). Each reservation
        maps a MAC address to a fixed IP.
    post:
      tags:
      - Connectivity
      summary: Create DHCP reservation
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DHCPReservationCreate'
      responses:
        '201':
          description: Reservation created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DHCPReservation'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Creates a DHCP reservation to assign a fixed IP address to a specific
        device by MAC address. The device will always receive the same IP.
  /customers/{customerId}/locations/{locationId}/dhcp/reservations/{reservationId}:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    delete:
      tags:
      - Connectivity
      summary: Remove DHCP reservation
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: ReservationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '204':
          description: Reservation removed
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Removes a DHCP reservation. The device will receive a dynamic IP
        on its next DHCP lease renewal.
  /customers/{customerId}/locations/{locationId}/dhcp/port-forwarding:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Connectivity
      summary: List port forwarding rules
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Port forwarding rules
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Lists all port forwarding rules: name, protocol (TCP/UDP), external
        port, internal IP and port, NAT loopback, and enabled state.'
    post:
      tags:
      - Connectivity
      summary: Create port forwarding rule
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PortForwardCreate'
      responses:
        '201':
          description: Rule created
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Creates a port forwarding rule to expose an internal service to
        the internet. Supports NAT loopback for accessing the service from inside
        the LAN using the public IP.
  /customers/{customerId}/locations/{locationId}/dhcp/port-forwarding/{ruleId}:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    patch:
      tags:
      - Connectivity
      summary: Update port forwarding rule
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: RuleID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PortForwardUpdate'
      responses:
        '200':
          description: Rule updated
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Updates an existing port forwarding rule: change external port,
        internal IP, internal port, or toggle NAT loopback.'
    delete:
      tags:
      - Connectivity
      summary: Delete port forwarding rule
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: RuleID
        in: path
        required: true
        schema:
          type: string
      responses:
        '204':
          description: Rule deleted
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Deletes a port forwarding rule. The internal service is no longer
        accessible from the internet.
  /customers/{customerId}/locations/{locationId}/routing/static:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Connectivity
      summary: List static routes
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: family
        in: query
        schema:
          type: string
          enum:
          - ipv4
          - ipv6
      responses:
        '200':
          description: Route list
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Lists all static routes (IPv4 or IPv6). Filterable by address family.
        Each route specifies destination, netmask, gateway, interface, and metric.
    post:
      tags:
      - Connectivity
      summary: Add static route
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StaticRoute'
      responses:
        '201':
          description: Route added
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Adds a static route to the routing table. Specify destination network,
        netmask, gateway, optional interface binding, and metric for priority.
  /customers/{customerId}/locations/{locationId}/routing/static/{routeId}:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    delete:
      tags:
      - Connectivity
      summary: Remove static route
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: RouteID
        in: path
        required: true
        schema:
          type: string
      responses:
        '204':
          description: Route removed
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Removes a static route. Traffic to the destination network will
        follow the default route thereafter.
  /customers/{customerId}/locations/{locationId}/routing/policy:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Connectivity
      summary: Get PBR rules
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: PBR rules
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Returns all policy-based routing rules. PBR rules route traffic
        based on source/destination prefix, interface, or fwmark rather than destination
        only.
    put:
      tags:
      - Connectivity
      summary: Set PBR rules
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: '#/components/schemas/PolicyRouteRule'
      responses:
        '200':
          description: PBR updated
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Sets the complete policy-based routing table. Replaces all existing
        PBR rules. Use for advanced routing such as source-based routing or multi-WAN
        traffic steering.
  /customers/{customerId}/locations/{locationId}/vpn/tunnels:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - VPN
      summary: List VPN tunnels
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: VPN tunnel list
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Lists all VPN tunnels (IPSec and WireGuard). Each tunnel shows
        name, type, remote endpoint, status, and connected subnets.
    post:
      tags:
      - VPN
      summary: Create VPN tunnel
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VPNCreate'
      responses:
        '201':
          description: VPN tunnel created
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Creates a VPN tunnel. Supports IPSec (with IKE/ESP crypto params)
        and WireGuard. Required: name, remote endpoint, and remote subnets. Optional:
        pre-shared key, healthcheck IP.'
  /customers/{customerId}/locations/{locationId}/vpn/tunnels/{tunnelId}:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - VPN
      summary: Get VPN tunnel status
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: TunnelName
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: VPN tunnel details
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Returns detailed status for a specific VPN tunnel including uptime,
        traffic counters, and last healthcheck result.
    patch:
      tags:
      - VPN
      summary: Update VPN tunnel
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: TunnelName
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VPNUpdate'
      responses:
        '200':
          description: VPN updated
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Updates a VPN tunnel: enable/disable, change remote endpoint or
        subnets, or update the healthcheck target IP.'
    delete:
      tags:
      - VPN
      summary: Remove VPN tunnel
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: TunnelName
        in: path
        required: true
        schema:
          type: string
      responses:
        '204':
          description: VPN tunnel removed
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Removes a VPN tunnel and tears down the connection. Routed traffic
        to remote subnets is dropped until an alternative path is configured.
  /customers/{customerId}/locations/{locationId}/qos:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Telemetry
      summary: Get QoS configuration
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: QoS config
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/QoSConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Returns QoS configuration: mode (adaptive/static/disabled), adaptive
        QoS parameters (latency measurement, pingers, reflectors), and static queue
        definitions with DSCP marking.'
    put:
      tags:
      - Telemetry
      summary: Set QoS configuration
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/QoSConfig'
      responses:
        '200':
          description: QoS updated
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Configures Quality of Service. Adaptive QoS measures available
        bandwidth and dynamically shapes traffic. Static QoS uses fixed queue priorities
        and bandwidth limits with DSCP marking.
  /customers/{customerId}/locations/{locationId}/speedtest/history:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Telemetry
      summary: Get speed test history
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: limit
        in: query
        schema:
          type: integer
          default: 20
          maximum: 100
      responses:
        '200':
          description: Speed test history
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Returns speed test history for the location. Paginated, configurable
        limit (max 100). Useful for tracking connection quality over time. Returns
        aggregated speed test history across all nodes at this location. For per-node
        speed test execution and results, use the node-scoped speedtest endpoint.
  /customers/{customerId}/locations/{locationId}/diagnostics/logging:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Telemetry
      summary: Get logging configuration
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Logging config
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Returns the logging configuration: per-module log severity levels
        and remote syslog server settings.'
    put:
      tags:
      - Telemetry
      summary: Set debug logging levels
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoggingConfig'
      responses:
        '200':
          description: Logging config updated
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Sets debug logging levels for specific modules (e.g., 'Wifi_Radio_Config'
        -> 'debug'). Also configures remote syslog forwarding to a central log server.
  /customers/{customerId}/locations/{locationId}/diagnostics/remote-ssh:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Telemetry
      summary: Get reverse SSH tunnel status
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: RevSSH status
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Returns the current reverse SSH tunnel status. When active, shows
        the tunnel server, port, and idle timeout.
    post:
      tags:
      - Telemetry
      summary: Establish reverse SSH tunnel
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RevSSHRequest'
      responses:
        '202':
          description: Tunnel request initiated
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Establishes a reverse SSH tunnel from the node to a specified server.
        Enables remote CLI access for advanced troubleshooting. Returns 202 Accepted.
  /customers/{customerId}/locations/{locationId}/diagnostics/reboot-history:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Telemetry
      summary: Get reboot history
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Node reboot history with timestamps and reboot types
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/RebootEventData'
                  meta:
                    $ref: '#/components/schemas/Pagination'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Returns reboot history for all nodes at the location: timestamps,
        reason codes (user-initiated, crash, firmware upgrade, power loss), and node
        serial.'
  /customers/{customerId}/locations/{locationId}/diagnostics/alarms:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Telemetry
      summary: List active alarms
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: severity
        in: query
        schema:
          type: string
          enum:
          - warning
          - critical
      responses:
        '200':
          description: Alarm list
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Lists active alarms for the location. Filterable by severity (warning/critical).
        Alarms include: node offline, high CPU, low memory, WAN down, LTE failover,
        etc.'
  /customers/{customerId}/locations/{locationId}/telemetry/config:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Telemetry
      summary: Get telemetry collection config
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Telemetry config
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Returns telemetry collection configuration: stats collection enable/disable,
        reporting intervals, sampling intervals, stat types, and MQTT reporting settings.'
    put:
      tags:
      - Telemetry
      summary: Configure telemetry collection
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TelemetryConfig'
      responses:
        '200':
          description: Telemetry config updated
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Configures telemetry data collection. Select which stats to collect
        (latency, neighbor, survey, client, capacity, radio, quality, device, RSSI,
        steering) and set collection/interval timing.
  /customers/{customerId}/locations/{locationId}/telemetry/reports:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Telemetry
      summary: List telemetry reports
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Report list
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Lists all configured telemetry reports. Each report specifies a
        name, collection interval, output format, MQTT topic, and optional data filters.
    post:
      tags:
      - Telemetry
      summary: Create telemetry report
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TelemetryReportConfig'
      responses:
        '201':
          description: Report created
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Creates a telemetry report configuration. Define what data to collect,
        at what interval, in what format (JSON/CSV/Protobuf), and where to publish
        (MQTT topic) with optional MAC/IP/app filters.
  /customers/{customerId}/locations/{locationId}/telemetry/reports/{reportId}:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Telemetry
      summary: Get telemetry report data
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: ReportName
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Report data
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Returns the latest data for a specific telemetry report. Data format
        depends on the report's configuration.
    delete:
      tags:
      - Telemetry
      summary: Remove telemetry report
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: ReportName
        in: path
        required: true
        schema:
          type: string
      responses:
        '204':
          description: Report removed
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Removes a telemetry report configuration. Data collection for this
        report stops.
  /customers/{customerId}/locations/{locationId}/services:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Nodes
      summary: List node services
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Service list
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Lists all configurable node services (e.g., 'wifi', 'zigbee', 'thread',
        'lte') and their current enabled/disabled state.
    put:
      tags:
      - Nodes
      summary: Enable or disable services
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: array
              items:
                type: object
                required:
                - service
                - enabled
                properties:
                  service:
                    type: string
                  enabled:
                    type: boolean
      responses:
        '200':
          description: Services updated
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Enables or disables specific node services. Provide an array of
        service name + enabled boolean pairs.
  /customers/{customerId}/locations/{locationId}/firmware:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Nodes
      summary: Get firmware status
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Firmware status
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Returns firmware status for all nodes at the location: current
        version, available update, upgrade status code, and last check timestamp.
        Returns the firmware version matrix (desired target versions per device model)
        for this location. For per-node current firmware info, use the node-scoped
        firmware endpoint.'
  /customers/{customerId}/locations/{locationId}/firmware/version-matrix:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Nodes
      summary: Get version compatibility matrix
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Version matrix
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Returns the version compatibility matrix for the location — a mapping
        of device models to allowed firmware versions.
    put:
      tags:
      - Nodes
      summary: Set version matrix
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties:
                type: string
      responses:
        '200':
          description: Version matrix updated
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Sets the version compatibility matrix. Specify which firmware version
        each device model at the location should run.
  /customers/{customerId}/locations/{locationId}/certificates:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Customers
      summary: Get certificate enrollment status
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Certificate status
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Returns certificate enrollment status for all nodes at the location.
        Shows current certificate validity, enrollment state, and expiry dates.
    post:
      tags:
      - Customers
      summary: Trigger certificate enrollment or renewal
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                label:
                  type: string
                server_url:
                  type: string
                  format: uri
                renew:
                  type: boolean
                  default: false
      responses:
        '202':
          description: Enrollment initiated
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Triggers certificate enrollment or renewal via EST (Enrollment
        over Secure Transport). Specify the EST server URL and optional label. Returns
        202 Accepted.
  /customers/{customerId}/locations/{locationId}/provision:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    post:
      tags:
      - Nodes
      summary: Provision location
      description: 'Applies a declarative configuration profile to a location. The
        cloud translates the profile into the necessary OVSDB operations.

        '
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProvisionProfile'
      responses:
        '200':
          description: Location provisioned
        '503':
          description: Node offline — provisioning deferred
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /provisioning/templates:
    get:
      tags:
      - Nodes
      summary: List provisioning templates
      responses:
        '200':
          description: Template list
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Lists all provisioning templates. Templates are reusable configuration
        profiles that can be applied to multiple locations.
    post:
      tags:
      - Nodes
      summary: Create provisioning template
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProvisionTemplate'
      responses:
        '201':
          description: Template created
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Creates a provisioning template with a name, description, full
        provision profile, and optional tags for categorization.
  /provisioning/templates/{templateId}:
    get:
      tags:
      - Nodes
      summary: Get template
      parameters:
      - name: TemplateID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Template details
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Returns a specific provisioning template with its full configuration
        profile.
    put:
      tags:
      - Nodes
      summary: Update template
      parameters:
      - name: TemplateID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProvisionTemplate'
      responses:
        '200':
          description: Template updated
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Updates a provisioning template. All locations using this template
        will receive the updated configuration on their next provision cycle.
    delete:
      tags:
      - Nodes
      summary: Delete template
      parameters:
      - name: TemplateID
        in: path
        required: true
        schema:
          type: string
      responses:
        '204':
          description: Template deleted
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Deletes a provisioning template. Locations previously provisioned
        with this template retain their current configuration.
  /customers/{customerId}/locations/{locationId}/thread:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Networks
      summary: Get Thread network config
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Thread config
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Returns the Thread IoT network configuration: enabled state, network
        name, PAN ID, channel, mesh local prefix, and commissioning settings.'
    put:
      tags:
      - Networks
      summary: Configure Thread network
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ThreadConfig'
      responses:
        '200':
          description: Thread configured
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Configures the Thread border router. Set the Thread network parameters
        including PAN ID, network key (write-only), channel, mesh prefix, and commissioning
        PSK.
  /customers/{customerId}/locations/{locationId}/thread/devices:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Networks
      summary: List Thread devices
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Thread device list
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Lists all Thread/Matter devices on the network: device type, IPv6
        address, RLOC16, parent node, and connection quality.'
  /customers/{customerId}/locations/{locationId}/passpoint:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Networks
      summary: Get Passpoint / Hotspot 2.0 config
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Passpoint config
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Returns Passpoint (Hotspot 2.0) configuration: HESSID, domain
        name list, roaming consortium OIs, venue info, operator name, and OSU provider
        details.'
    put:
      tags:
      - Networks
      summary: Configure Passpoint
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PasspointConfig'
      responses:
        '200':
          description: Passpoint configured
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Configures Passpoint / Hotspot 2.0 for carrier WiFi offload. Define
        roaming consortium, venue information, OSU SSID, and online sign-up providers
        for seamless authentication.
  /reference/schema:
    get:
      tags:
      - Partners
      summary: Get OVSDB schema catalog
      description: 'Returns the OpenSync OVSDB schema catalog grouped by domain. Informational
        reference only — all configuration must use the feature-level REST endpoints.

        '
      responses:
        '200':
          description: Schema catalog
          content:
            application/json:
              schema:
                type: object
                properties:
                  name:
                    type: string
                    example: Open_vSwitch
                  version:
                    type: string
                    example: 7.11.426
                  table_count:
                    type: integer
                    example: 137
                  tables:
                    type: array
                    items:
                      $ref: '#/components/schemas/TableReference'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /reference/health:
    get:
      tags:
      - Partners
      summary: API health check
      responses:
        '200':
          description: Healthy
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    enum:
                    - ok
                    - degraded
                  version:
                    type: string
                  shard:
                    type: string
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: API health check endpoint. Returns status (ok/degraded), API version,
        and current shard identifier. Use for monitoring and load balancer health
        checks.
  /admin/portals:
    get:
      tags:
      - Partners
      summary: Get admin portal URLs
      description: Returns Frontline Tier 1 and Tier 2&3 portal URLs.
      parameters:
      - name: customerId
        in: query
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Portal URLs
          content:
            application/json:
              schema:
                type: object
                properties:
                  frontline_tier1:
                    type: string
                    format: uri
                    frontline_tier23:
                      type: string
                      format: uri
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/wan:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Connectivity
      summary: Get WAN interface configuration
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: WAN configuration
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/WANConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Returns the primary WAN interface configuration: connection mode
        (DHCP/static/PPPoE/bridge/LTE), IP settings, MTU, DNS servers, VLAN tagging,
        PPPoE credentials, UPnP mode, and MAC cloning.'
    put:
      tags:
      - Connectivity
      summary: Configure WAN interface
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WANConfig'
      responses:
        '200':
          description: WAN configured
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Configures the WAN interface. Supports DHCP client, static IP,
        PPPoE (with credentials), bridge mode, or LTE-only. Includes VLAN tagging
        for ISP-specific WAN VLANs and MAC address cloning.
  /customers/{customerId}/locations/{locationId}/wan/uplinks:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Connectivity
      summary: List WAN uplinks and health
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Uplink list with health
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/UplinkHealth'
                  meta:
                    $ref: '#/components/schemas/Pagination'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Lists all WAN uplinks and their health status: interface type,
        priority, active state, L2/L3 connectivity, IPv4/IPv6 status, unreachable
        counters (link/router/internet/cloud), link speed, and NTP sync.'
  /customers/{customerId}/locations/{locationId}/wan/multi-wan:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Connectivity
      summary: Get multi-WAN configuration
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Multi-WAN config
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/MultiWANConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Returns multi-WAN configuration: enabled state, uplink definitions
        (name, type, priority, per-uplink WAN config), and failover policy (mode,
        healthcheck IP, interval, thresholds).'
    put:
      tags:
      - Connectivity
      summary: Configure multi-WAN
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MultiWANConfig'
      responses:
        '200':
          description: Multi-WAN configured
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Configures multi-WAN with multiple uplinks. Define each uplink
        (eth/PPPoE/VLAN/LTE/GRE) with priority and per-uplink config. Set failover
        mode (active-standby/load-balance/round-robin) with health checking.
  /customers/{customerId}/locations/{locationId}/wan/igmpproxy:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Connectivity
      summary: Get IGMP/MLD proxy configuration
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: IGMP/MLD proxy config
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/IGMPProxyConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Returns IGMP/MLD proxy configuration for IPTV multicast: IGMP
        version, MLD version, upstream/downstream interfaces, fast-leave, and query
        interval.'
    put:
      tags:
      - Connectivity
      summary: Configure IGMP/MLD proxy
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/IGMPProxyConfig'
      responses:
        '200':
          description: IGMP/MLD configured
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Configures IGMP/MLD multicast proxy for IPTV services. Set the
        upstream WAN interface, downstream LAN interfaces, IGMP/MLD protocol versions,
        and fast-leave behavior.
  /customers/{customerId}/locations/{locationId}/wan/map:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Connectivity
      summary: Get MAP-T/MAP-E IPv6 transition config
      description: Returns the MAP-T (RFC 7597) or MAP-E (RFC 7599) IPv6 transition
        configuration for the WAN interface.
      responses:
        '200':
          description: MAP IPv6 transition config
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MAPConfig'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    put:
      tags:
      - Connectivity
      summary: Configure MAP-T/MAP-E IPv6 transition
      description: Configures MAP-T or MAP-E IPv6 transition parameters. Requires
        the ISP to supply Border Relay address and mapping rule parameters per RFC
        7597/7599.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MAPConfig'
      responses:
        '200':
          description: MAP config updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MAPConfig'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/dns:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Connectivity
      summary: Get DNS configuration
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: DNS config
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/DNSConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Returns DNS configuration: mode (auto/manual/ISP), primary/secondary
        servers, DNS-over-TLS settings, local domain, rebind protection, cache size,
        and static host entries.'
    put:
      tags:
      - Connectivity
      summary: Configure DNS
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DNSConfig'
      responses:
        '200':
          description: DNS configured
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Configures DNS resolution. Set manual DNS servers, enable DNS-over-TLS
        for encrypted queries, configure local domain name, enable rebind attack protection,
        and add static host mappings.
  /customers/{customerId}/locations/{locationId}/dns/ddns:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Connectivity
      summary: Get dynamic DNS configuration
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: DDNS config
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/DDNSConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Returns dynamic DNS configuration: enabled state, provider (dyndns/noip/cloudflare/duckdns/afraid/custom),
        hostname, credentials, update interval, and custom update URL.'
    put:
      tags:
      - Connectivity
      summary: Configure dynamic DNS
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DDNSConfig'
      responses:
        '200':
          description: DDNS configured
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Configures dynamic DNS. Keeps a hostname updated with the gateway's
        current public IP. Supports major DDNS providers and custom update URLs.
  /customers/{customerId}/locations/{locationId}/ntp:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Connectivity
      summary: Get NTP configuration
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: NTP config
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/NTPConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Returns NTP configuration: server list, sync interval, and timezone.'
    put:
      tags:
      - Connectivity
      summary: Configure NTP
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NTPConfig'
      responses:
        '200':
          description: NTP configured
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Configures NTP time synchronization. Set NTP server list (defaults
        to pool.ntp.org), sync interval, and the location's timezone.
  /customers/{customerId}/locations/{locationId}/topology:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Connectivity
      summary: Get network topology
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: at
        in: query
        schema:
          type: string
          format: date-time
        description: Return topology as it was at this timestamp. Omit for current
          live topology.
      responses:
        '200':
          description: Network topology graph
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/NetworkTopology'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Returns the full network topology graph showing all nodes and the
        links between them. Each link includes type (ethernet/wifi-backhaul), RSSI,
        PHY rate, channel, and band. Use the `at` query parameter to retrieve historical
        topology snapshots.
  /customers/{customerId}/locations/{locationId}/topology/mesh:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Connectivity
      summary: Get mesh backhaul topology
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: at
        in: query
        schema:
          type: string
          format: date-time
        description: Return topology as it was at this timestamp. Omit for current
          live topology.
      responses:
        '200':
          description: Mesh topology with link metrics
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/MeshTopology'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Returns the mesh backhaul topology: gateway ID, satellite nodes
        with parent relationships, hop counts, backhaul type and metrics (RSSI, PHY
        rate, channel), connected client counts, firmware, and uptime. Use the `at`
        query parameter to retrieve historical topology snapshots.'
  /customers/{customerId}/locations/{locationId}/topology/elements:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Connectivity
      summary: Get LLDP/CDP neighbors per node
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: node_id
        in: query
        schema:
          type: string
      - name: at
        in: query
        schema:
          type: string
          format: date-time
        description: Return topology as it was at this timestamp. Omit for current
          live topology.
      responses:
        '200':
          description: Neighbor discovery results
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/LinkLayerNeighbor'
                  meta:
                    $ref: '#/components/schemas/Pagination'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Returns LLDP/CDP neighbor discovery results per node. Shows which
        devices are connected to each node's Ethernet ports — useful for mapping wired
        topology in business deployments. Use the `at` query parameter to retrieve
        historical topology snapshots.
  /bulk/locations:
    post:
      tags:
      - Partners
      summary: Bulk create or update locations
      description: Accepts up to 1000 locations per request. Returns an operation
        ID for async status polling.
      parameters:
      - name: customer_id
        in: query
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                locations:
                  type: array
                  maxItems: 1000
                  items:
                    $ref: '#/components/schemas/LocationCreate'
                mode:
                  type: string
                  enum:
                  - create
                  - update
                  - upsert
                  default: upsert
                match_key:
                  type: string
                  enum:
                  - name
                  - id
                  default: name
      responses:
        '202':
          description: Bulk operation accepted
          content:
            application/json:
              schema:
                type: object
                properties:
                  operation_id:
                    type: string
                  status:
                    type: string
                    enum:
                    - queued
                    - processing
                    - completed
                    - failed
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /bulk/nodes:
    post:
      tags:
      - Partners
      summary: Bulk claim or update nodes
      description: Accepts up to 5000 nodes per request.
      parameters:
      - name: customer_id
        in: query
        required: true
        schema:
          type: string
      - name: location_id
        in: query
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BulkNodesRequest'
      responses:
        '202':
          description: Bulk operation accepted
          content:
            application/json:
              schema:
                type: object
                properties:
                  operation_id:
                    type: string
                  status:
                    type: string
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /bulk/operations/{operationId}:
    get:
      tags:
      - Partners
      summary: Get bulk operation status
      parameters:
      - name: OperationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Operation status and results
          content:
            application/json:
              schema:
                type: object
                properties:
                  operation_id:
                    type: string
                  status:
                    type: string
                    enum:
                    - queued
                    - processing
                    - completed
                    - failed
                    - cancelled
                  progress_pct:
                    type: integer
                  total_items:
                    type: integer
                  succeeded:
                    type: integer
                  failed:
                    type: integer
                  errors:
                    type: array
                    items:
                      type: object
                      properties:
                        item_index:
                          type: integer
                        error:
                          type: string
                  created_at:
                    type: string
                    format: date-time
                  completed_at:
                    type: string
                    format: date-time
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Returns the status of a bulk operation: queued/processing/completed/failed/cancelled,
        progress percentage, total/succeeded/failed item counts, per-item error details,
        and timestamps.'
  /bulk/import/csv:
    post:
      tags:
      - Partners
      summary: Import locations/nodes from CSV
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
              - file
              - type
              properties:
                file:
                  type: string
                  format: binary
                type:
                  type: string
                  enum:
                  - locations
                  - nodes
                  - ssids
                customer_id:
                  type: string
      responses:
        '202':
          description: Import accepted
          content:
            application/json:
              schema:
                type: object
                properties:
                  operation_id:
                    type: string
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Imports locations, nodes, or SSIDs from a CSV file upload. Specify
        the import type and target customer. Returns an operation ID for async processing.
  /bulk/export/csv:
    post:
      tags:
      - Partners
      summary: Export locations/nodes to CSV
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - type
              properties:
                type:
                  type: string
                  enum:
                  - locations
                  - nodes
                  - clients
                  - ssids
                customer_id:
                  type: string
                filters:
                  type: object
      responses:
        '202':
          description: Export initiated
          content:
            application/json:
              schema:
                type: object
                properties:
                  operation_id:
                    type: string
                  download_url:
                    type: string
                    format: uri
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Exports locations, nodes, clients, or SSIDs to CSV. Specify the
        type, customer, and optional filters. Returns an operation ID and a download
        URL when complete.
  /bulk/config-push:
    post:
      tags:
      - Partners
      summary: Push configuration to multiple locations
      description: Apply a provisioning template or partial config to multiple locations
        at once.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - location_ids
              - config
              properties:
                location_ids:
                  type: array
                  maxItems: 500
                  items:
                    type: string
                config:
                  $ref: '#/components/schemas/ProvisionProfile'
                continue_on_error:
                  type: boolean
                  default: true
      responses:
        '202':
          description: Config push accepted
          content:
            application/json:
              schema:
                type: object
                properties:
                  operation_id:
                    type: string
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /analytics/isp/dashboard:
    get:
      tags:
      - Telemetry
      summary: Get ISP-wide dashboard summary
      parameters:
      - name: period
        in: query
        schema:
          type: string
          enum:
          - day
          - week
          - month
          - quarter
          default: week
      responses:
        '200':
          description: ISP dashboard data
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/ISPDashboard'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Returns aggregated ISP-wide dashboard data for the specified period:
        total/active customers, online locations, node/client counts, average health
        score, threats blocked, speed test averages, bandwidth utilization, top devices
        and applications, and customer growth trends.'
  /customers/{customerId}/locations/{locationId}/analytics/health:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Telemetry
      summary: Get network health score
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: period
        in: query
        schema:
          type: string
          enum:
          - day
          - week
          - month
          default: day
      responses:
        '200':
          description: Network health score
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/NetworkHealthScore'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Returns the network health score (0-100) for the location broken
        down by category: connectivity, WiFi performance, security, latency, and throughput.
        Includes identified issues with severity and recommendations.'
  /customers/{customerId}/locations/{locationId}/analytics/bandwidth:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Telemetry
      summary: Get bandwidth utilization report
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: period
        in: query
        schema:
          type: string
          enum:
          - hour
          - day
          - week
          - month
          default: day
      - name: resolution
        in: query
        schema:
          type: string
          enum:
          - 5min
          - 15min
          - 1hour
          - 1day
          default: 15min
      responses:
        '200':
          description: Bandwidth report
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/BandwidthReport'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Returns bandwidth utilization report: total download/upload GB,
        peak speeds, time-series data at configurable resolution (5min/15min/1hour/1day),
        and breakdowns by SSID and by band.'
  /customers/{customerId}/locations/{locationId}/analytics/clients:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Telemetry
      summary: Get client device analytics
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: period
        in: query
        schema:
          type: string
          enum:
          - day
          - week
          - month
          default: week
      responses:
        '200':
          description: Client analytics
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/ClientAnalytics'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Returns client device analytics: unique clients, average daily
        count, new clients, and breakdowns by operating system, device type, vendor,
        and signal strength distribution.'
  /customers/{customerId}/locations/{locationId}/analytics/wifi:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Telemetry
      summary: Get WiFi performance analytics
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: period
        in: query
        schema:
          type: string
          enum:
          - day
          - week
          - month
          default: day
      responses:
        '200':
          description: WiFi analytics
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/WiFiAnalytics'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Returns WiFi performance analytics: average channel utilization,
        noise levels, channel change count, DFS events, per-radio metrics (utilization,
        noise, clients, airtime efficiency), and interference score.'
  /customers/{customerId}/locations/{locationId}/analytics/apps:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Telemetry
      summary: Get application traffic breakdown
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: period
        in: query
        schema:
          type: string
          enum:
          - hour
          - day
          - week
          default: day
      - name: top_n
        in: query
        schema:
          type: integer
          default: 10
      responses:
        '200':
          description: Application analytics
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/AppAnalytics'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Returns application traffic breakdown: top-N applications by bandwidth
        usage, per-app metrics (download/upload bytes, active clients, bandwidth percentage),
        and per-category bandwidth totals.'
  /customers/{customerId}/locations/{locationId}/audit/log:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Partners
      summary: Get configuration change audit log
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: since
        in: query
        schema:
          type: string
          format: date-time
      - name: actor
        in: query
        schema:
          type: string
      - name: resource
        in: query
        schema:
          type: string
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: per_page
        in: query
        schema:
          type: integer
          default: 50
          maximum: 500
      responses:
        '200':
          description: Audit log entries
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/AuditLogEntry'
                  meta:
                    $ref: '#/components/schemas/Pagination'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Returns the configuration change audit log for the location. Filterable
        by time range, actor, and resource type. Paginated. Each entry shows who changed
        what, when, old/new values, IP address, and user agent.
  /customers/{customerId}/locations/{locationId}/compliance:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Partners
      summary: Get regulatory compliance status
      description: Returns compliance with country-specific WiFi regulations (channels,
        power, DFS).
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Compliance status
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/ComplianceStatus'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/compliance/data-residency:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Partners
      summary: Get data residency configuration
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Data residency settings
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/DataResidencyConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Returns data residency configuration: storage region, telemetry
        residency rules, PII exclusion status, retention period, and GDPR compliance
        flag.'
    put:
      tags:
      - Partners
      summary: Configure data residency
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DataResidencyConfig'
      responses:
        '200':
          description: Data residency updated
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Configures data residency settings. Set the primary storage region
        (US/EU/APAC/LATAM/MEA), separate telemetry region if needed, PII exclusion,
        data retention days, and GDPR compliance mode.
  /admin/tenants:
    get:
      tags:
      - Partners
      summary: List ISP tenant accounts
      description: For platform operators only — manage multiple ISP tenants.
      responses:
        '200':
          description: Tenant list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/Tenant'
                  meta:
                    $ref: '#/components/schemas/Pagination'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      tags:
      - Partners
      summary: Create ISP tenant
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TenantCreate'
      responses:
        '201':
          description: Tenant created
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Creates a new ISP tenant account. Specify the tenant name, domain,
        tier (sandbox/starter/growth/enterprise/unlimited), and contact information.
  /admin/tenants/{tenantId}:
    get:
      tags:
      - Partners
      summary: Get tenant details
      parameters:
      - name: TenantID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Tenant details
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/Tenant'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Returns detailed tenant information including status, tier, creation
        date, and resource limits (max customers, locations, nodes, API keys, rate
        limit).
    patch:
      tags:
      - Partners
      summary: Update tenant
      parameters:
      - name: TenantID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TenantUpdate'
      responses:
        '200':
          description: Tenant updated
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Updates a tenant's name, status (active/suspended/cancelled), or
        tier. Changing tier updates resource limits.
    delete:
      tags:
      - Partners
      summary: Delete tenant
      parameters:
      - name: TenantID
        in: path
        required: true
        schema:
          type: string
      responses:
        '204':
          description: Tenant deleted
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Deletes a tenant and all associated data — customers, locations,
        nodes, and configuration. Irreversible.
  /admin/api-keys:
    get:
      tags:
      - Partners
      summary: List API keys
      parameters:
      - name: tenant_id
        in: query
        schema:
          type: string
      responses:
        '200':
          description: API key list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/ApiKey'
                  meta:
                    $ref: '#/components/schemas/Pagination'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Lists API keys for the authenticated tenant. Keys show name, prefix
        (first 4 chars), roles, scopes, last used, and expiry.
    post:
      tags:
      - Partners
      summary: Create API key
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ApiKeyCreate'
      responses:
        '201':
          description: API key created (secret shown once)
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Creates a new API key for M2M automation. The full key secret is
        returned only once in the creation response. Store it securely.
  /admin/api-keys/{keyId}:
    delete:
      tags:
      - Partners
      summary: Revoke API key
      parameters:
      - name: KeyID
        in: path
        required: true
        schema:
          type: string
      responses:
        '204':
          description: API key revoked
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Revokes an API key. All requests using this key will immediately
        receive 401 Unauthorized.
  /admin/rate-limits:
    get:
      tags:
      - Partners
      summary: Get rate limits configuration
      parameters:
      - name: tenant_id
        in: query
        schema:
          type: string
      responses:
        '200':
          description: Rate limits
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/RateLimitConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Returns current rate limit configuration: per-second and per-minute
        limits, burst multiplier, and per-endpoint overrides.'
    put:
      tags:
      - Partners
      summary: Configure rate limits
      parameters:
      - name: tenant_id
        in: query
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RateLimitConfig'
      responses:
        '200':
          description: Rate limits updated
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Configures rate limits for the tenant. Set global per-second/per-minute
        limits and per-endpoint fine-grained limits. Includes notification threshold
        for limit warnings.
  /admin/sub-accounts:
    get:
      tags:
      - Partners
      summary: List sub-accounts (NOC/support roles)
      parameters:
      - name: tenant_id
        in: query
        schema:
          type: string
      responses:
        '200':
          description: Sub-account list
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Lists sub-accounts (NOC/support staff) for the tenant. Sub-accounts
        have role-based access scoped to specific customers.
    post:
      tags:
      - Partners
      summary: Create sub-account
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SubAccountCreate'
      responses:
        '201':
          description: Sub-account created
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Creates a sub-account with specific roles (admin/read_only/noc/support_tier1/support_tier2/billing/provisioning).
        Optionally scoped to specific customer IDs.
  /app/auth/login:
    post:
      tags:
      - Customers
      summary: End-user login
      description: Authenticate an end-user for the HomePass/WorkPass mobile app.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - email
              - password
              properties:
                email:
                  type: string
                  format: email
                password:
                  type: string
                mfa_code:
                  type: string
      responses:
        '200':
          description: Login successful
          content:
            application/json:
              schema:
                type: object
                properties:
                  access_token:
                    type: string
                  refresh_token:
                    type: string
                  expires_in:
                    type: integer
                  user:
                    $ref: '#/components/schemas/AppUser'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /app/auth/refresh:
    post:
      tags:
      - Customers
      summary: Refresh access token
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - refresh_token
              properties:
                refresh_token:
                  type: string
      responses:
        '200':
          description: Token refreshed
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Refreshes an expired access token using a valid refresh token.
        Returns a new access token without requiring re-authentication.
  /app/user/me:
    get:
      tags:
      - Customers
      summary: Get current user profile
      responses:
        '200':
          description: User profile
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/AppUser'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Returns the authenticated end-user''s profile: ID, email, name,
        phone, MFA status, and list of associated locations with roles.'
  /app/user/locations:
    get:
      tags:
      - Customers
      summary: List user's locations (simplified)
      responses:
        '200':
          description: User's locations
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/AppLocation'
                  meta:
                    $ref: '#/components/schemas/Pagination'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Returns a simplified list of the user''s locations suitable for
        mobile display: ID, name, type (home/business), online status, node counts,
        client count, and thumbnail.'
  /app/locations/{locationId}/status:
    get:
      tags:
      - Customers
      summary: Get location status summary
      description: Simplified status view for mobile app (online/offline, client count,
        speed).
      parameters:
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Location status
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/AppLocationStatus'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /app/locations/{locationId}/notifications:
    get:
      tags:
      - Customers
      summary: Get push notification preferences
      parameters:
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Notification settings
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/AppNotificationPrefs'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Returns the user''s push notification preferences for the location:
        platform (iOS/Android), push token, and toggle preferences for each notification
        type.'
    put:
      tags:
      - Customers
      summary: Update notification preferences
      parameters:
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AppNotificationPrefs'
      responses:
        '200':
          description: Preferences updated
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Updates push notification preferences. Set the push token and
        toggle which events trigger push notifications: new device connect, security
        threats, node offline, firmware updates, speed test results, guest logins,
        and weekly reports.'
  /ovsdb/transact:
    post:
      tags:
      - Nodes
      summary: Execute raw OVSDB transaction
      description: 'Direct OVSDB transact (RFC 7047 §5.2) for power users. The cloud
        validates the operations

        against the schema and applies authorization scoping per tenant. Each operation
        within the

        transaction maps to an OVSDB table row insert/update/delete/mutate.

        '
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - operations
              properties:
                operations:
                  type: array
                  items:
                    type: object
                    properties:
                      op:
                        type: string
                        enum:
                        - insert
                        - update
                        - delete
                        - mutate
                        - select
                        - wait
                      table:
                        type: string
                      where:
                        type: array
                        items:
                          type: array
                      row:
                        type: object
                      columns:
                        type: array
                        items:
                          type: string
                      mutations:
                        type: array
                        items:
                          type: array
                      until:
                        type: string
                        enum:
                        - equal
                        - '!='
                        - '>'
                        - '>='
                        - <
                        - <=
                      rows:
                        type: array
                      timeout:
                        type: integer
                scope:
                  type: string
                  enum:
                  - tenant
                  - customer
                  - location
                  - node
                  default: tenant
                customer_id:
                  type: string
                location_id:
                  type: string
      responses:
        '200':
          description: Transaction result (array of operation results)
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /ovsdb/monitor:
    post:
      tags:
      - Nodes
      summary: Create OVSDB monitor (WebSocket upgrade)
      description: 'Creates an OVSDB monitor session via WebSocket upgrade. The response
        is a 101 Switching

        Protocols with a WebSocket stream of OVSDB monitor updates (RFC 7047 §4.1.5).

        Client must include OAuth bearer token in the upgrade request.

        '
      parameters:
      - name: upgrade
        in: header
        required: true
        schema:
          type: string
          enum:
          - websocket
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - table
              - columns
              properties:
                table:
                  type: string
                  description: OVSDB table name to monitor
                columns:
                  type: array
                  items:
                    type: string
                select:
                  type: object
                  properties:
                    initial:
                      type: boolean
                      default: true
                    insert:
                      type: boolean
                      default: true
                    delete:
                      type: boolean
                      default: true
                    modify:
                      type: boolean
                      default: true
                scope:
                  type: string
                  enum:
                  - tenant
                  - customer
                  - location
                  - node
                  default: tenant
                customer_id:
                  type: string
                location_id:
                  type: string
      responses:
        '101':
          description: WebSocket upgrade to OVSDB monitor stream
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /ovsdb/schema:
    get:
      tags:
      - Nodes
      summary: Get full OVSDB schema
      description: Returns the complete OpenSync OVSDB schema (all 137 tables with
        columns, types, constraints).
      responses:
        '200':
          description: Full OVSDB schema JSON
          content:
            application/json:
              schema:
                type: object
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /ovsdb/schema/{tableName}:
    get:
      tags:
      - Nodes
      summary: Get single OVSDB table schema
      parameters:
      - name: TableName
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Table schema
          content:
            application/json:
              schema:
                type: object
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Returns the schema for a single OVSDB table by name. Includes column
        definitions, types, enum values, min/max constraints, and whether the table
        is a root table.
  /customers/{customerId}/locations/{locationId}/tr069:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Connectivity
      summary: Get TR-069 bridge status
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: TR-069 bridge status
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/TR069BridgeStatus'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Returns the TR-069/TR-181 bridge status: enabled state, ACS URL,
        connection status, last inform timestamp, inform interval, CPE ID, and provisioning
        code.'
    put:
      tags:
      - Connectivity
      summary: Configure TR-069 bridge
      description: Configure the Exchange Manager (XM) TR-069/TR-181 connector bridge.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TR069BridgeConfig'
      responses:
        '200':
          description: TR-069 bridge configured
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/network/fast-roaming:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Networks
      summary: Get 802.11r/k/v fast roaming configuration
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Fast roaming config
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/FastRoamingConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Returns the fast roaming configuration: 802.11r (FT), 802.11k
        (neighbor reports), 802.11v (BSS transition), PMK caching, and OKC settings.'
    put:
      tags:
      - Networks
      summary: Configure fast roaming
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FastRoamingConfig'
      responses:
        '200':
          description: Fast roaming configured
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Configures fast roaming (802.11r/k/v) for seamless client transitions
        between access points. Essential for VoIP, video calls, and high-density deployments.
  /customers/{customerId}/locations/{locationId}/wifi-schedule:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Networks
      summary: List WiFi on/off schedules per SSID
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: WiFi schedules
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/WiFiSchedule'
                  meta:
                    $ref: '#/components/schemas/Pagination'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Lists all WiFi on/off schedules. Each schedule defines SSID, timezone,
        and time rules for when the SSID should be active.
    post:
      tags:
      - Networks
      summary: Create WiFi schedule
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WiFiSchedule'
      responses:
        '201':
          description: Schedule created
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Creates a WiFi schedule to automatically enable/disable a specific
        SSID on a recurring schedule. Useful for parental controls or energy saving.
  /customers/{customerId}/locations/{locationId}/wifi-schedule/{scheduleId}:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    delete:
      tags:
      - Networks
      summary: Remove WiFi schedule
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: ScheduleID
        in: path
        required: true
        schema:
          type: string
      responses:
        '204':
          description: Schedule removed
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Removes a WiFi schedule. The SSID will return to its default always-on
        state.
  /customers/{customerId}/locations/{locationId}/network/mlo:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Networks
      summary: Get WiFi 7 Multi-Link Operation configuration
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: MLO config
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/MLOConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Returns WiFi 7 Multi-Link Operation configuration: enabled state,
        mode (STR/EMLSR/NSTR), participating bands, and max simultaneous links.'
    put:
      tags:
      - Networks
      summary: Configure Multi-Link Operation
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MLOConfig'
      responses:
        '200':
          description: MLO configured
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Configures WiFi 7 Multi-Link Operation for supported hardware.
        MLO allows clients to simultaneously use multiple bands for higher throughput
        and lower latency.
  /customers/{customerId}/locations/{locationId}/network/afc:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Networks
      summary: Get AFC (Automated Frequency Coordination) status for 6GHz
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: AFC status and channel grants
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/AfcStatus'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Returns Automated Frequency Coordination status for 6GHz operation.
        Shows granted channels, max power, expiry time, and the AFC server URL. Returns
        location-wide AFC status across all radios. For per-radio AFC detail, use
        the radio-scoped AFC endpoint.
  /customers/{customerId}/locations/{locationId}/network/min-rssi:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Networks
      summary: Get minimum RSSI threshold config
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Min RSSI config
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/MinRSSIConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Returns the minimum RSSI threshold configuration for each band.
        Clients below the threshold are steered to closer APs or disconnected.
    put:
      tags:
      - Networks
      summary: Configure minimum RSSI threshold
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MinRSSIConfig'
      responses:
        '200':
          description: Min RSSI configured
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Sets minimum RSSI thresholds per band. Prevents 'sticky clients'
        that remain associated to distant APs, degrading overall network performance.
  /customers/{customerId}/locations/{locationId}/client-isolation:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Networks
      summary: Get client isolation config per SSID
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Client isolation config
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/ClientIsolationConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Returns per-SSID client isolation settings: L2 client isolation,
        isolation from primary network, inter-SSID isolation, and VLAN assignment.'
    put:
      tags:
      - Networks
      summary: Configure client isolation
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ClientIsolationConfig'
      responses:
        '200':
          description: Client isolation configured
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Configures client isolation per SSID. Use to prevent client-to-client
        communication for guest networks, IoT devices, or multi-tenant deployments.
  /provisioning/policies:
    get:
      tags:
      - Nodes
      summary: List OUI/MAC-based provisioning policies
      responses:
        '200':
          description: Provisioning policies
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/ProvisioningPolicy'
                  meta:
                    $ref: '#/components/schemas/Pagination'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Lists all OUI/MAC-based automatic provisioning policies. Policies
        auto-assign nodes matching OUI prefixes, serial patterns, or models to customers,
        locations, and templates.
    post:
      tags:
      - Nodes
      summary: Create OUI-based provisioning policy
      description: Auto-assign nodes matching OUI or serial pattern to customer/location/template.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProvisioningPolicyCreate'
      responses:
        '201':
          description: Policy created
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /provisioning/policies/{policyId}:
    get:
      tags:
      - Nodes
      summary: Get provisioning policy
      parameters:
      - name: PolicyID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Policy details
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Returns a specific provisioning policy: match criteria, action,
        target customer/location/template, enabled state, and priority.'
    patch:
      tags:
      - Nodes
      summary: Update provisioning policy
      parameters:
      - name: PolicyID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProvisioningPolicyUpdate'
      responses:
        '200':
          description: Policy updated
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Updates a provisioning policy's match criteria, target assignment,
        enabled state, or priority.
    delete:
      tags:
      - Nodes
      summary: Delete provisioning policy
      parameters:
      - name: PolicyID
        in: path
        required: true
        schema:
          type: string
      responses:
        '204':
          description: Policy deleted
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Deletes a provisioning policy. New nodes matching this policy's
        criteria will no longer be auto-assigned.
  /customers/{customerId}/locations/{locationId}/monitoring/traffic:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Telemetry
      summary: Get real-time traffic stats
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: interface
        in: query
        schema:
          type: string
      responses:
        '200':
          description: Real-time traffic data
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/TrafficStats'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Returns real-time traffic statistics per interface: RX/TX bytes
        per second, packets per second, error counts, and drop counts.'
  /customers/{customerId}/locations/{locationId}/monitoring/packet-capture:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    post:
      tags:
      - Telemetry
      summary: Start packet capture
      description: Triggers remote packet capture on the gateway node.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                interface:
                  type: string
                  default: any
                filter:
                  type: string
                  example: tcp port 80
                duration_sec:
                  type: integer
                  default: 60
                  maximum: 300
                max_packets:
                  type: integer
                  default: 10000
      responses:
        '202':
          description: Packet capture started
          content:
            application/json:
              schema:
                type: object
                properties:
                  capture_id:
                    type: string
                  download_url:
                    type: string
                    format: uri
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/monitoring/anomalies:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Telemetry
      summary: List anomaly detection events
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: type
        in: query
        schema:
          type: string
          enum:
          - throughput_drop
          - latency_spike
          - client_disconnect
          - channel_congestion
          - reboot_loop
      - name: since
        in: query
        schema:
          type: string
          format: date-time
      responses:
        '200':
          description: Anomaly events
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/AnomalyEvent'
                  meta:
                    $ref: '#/components/schemas/Pagination'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: 'Lists detected network anomalies: throughput drops, latency spikes,
        client disconnects, channel congestion, reboot loops. Each anomaly includes
        type, severity, metrics, and recommendation.'
  /customers/{customerId}/billing:
    get:
      tags:
      - Customers
      summary: Get billing integration status
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Billing status
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/BillingStatus'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
      description: Returns billing integration status including subscription state,
        plan, billing cycle, data usage vs cap, and active location/node counts.
  /customers/{customerId}/locations/{locationId}/device-groups:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Devices
      summary: List device groups
      description: Returns all device groups (labels) for the location.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Device groups
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/DeviceGroup'
                  meta:
                    $ref: '#/components/schemas/Pagination'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      tags:
      - Devices
      summary: Create device group
      description: Creates a new device group with optional device MACs, node IDs,
        and tags.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DeviceGroupCreate'
      responses:
        '201':
          description: Group created
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/device-groups/{deviceGroupId}:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Devices
      summary: Get device group
      description: Returns a single device group, including assigned client MACs,
        node IDs, labels, tags, and any group-level policy metadata for the location.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: GroupID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Group details
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/DeviceGroup'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    patch:
      tags:
      - Devices
      summary: Update device group
      description: Updates the device group membership and metadata. Use this to rename
        the group, change tags, or replace the assigned client MAC and node ID lists.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: GroupID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DeviceGroupCreate'
      responses:
        '200':
          description: Group updated
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    delete:
      tags:
      - Devices
      summary: Delete device group
      description: Deletes the device group from the location. Assigned devices remain
        in the location and only lose this group association.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: GroupID
        in: path
        required: true
        schema:
          type: string
      responses:
        '204':
          description: Group deleted
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /admin/service-tiers:
    get:
      tags:
      - Partners
      summary: List service tiers
      description: Returns all configurable service tiers with feature bundles and
        resource limits.
      responses:
        '200':
          description: Service tiers
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/ServiceTier'
                  meta:
                    $ref: '#/components/schemas/Pagination'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      tags:
      - Partners
      summary: Create service tier
      description: Creates a new feature bundle tier. Assign to customers to control
        feature access.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ServiceTierCreate'
      responses:
        '201':
          description: Tier created
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /admin/service-tiers/{tierId}:
    get:
      tags:
      - Partners
      summary: Get service tier
      description: Returns the service tier definition, including enabled feature
        bundle, resource limits, quotas, and provisioning defaults.
      parameters:
      - name: TierID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Tier details
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/ServiceTier'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    patch:
      tags:
      - Partners
      summary: Update service tier
      description: Updates the service tier definition. Changes affect future entitlement
        checks and may be applied to assigned customers according to tenant policy.
      parameters:
      - name: TierID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ServiceTierCreate'
      responses:
        '200':
          description: Tier updated
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    delete:
      tags:
      - Partners
      summary: Delete service tier
      description: Deletes an unused service tier. Implementations should reject deletion
        when customers or locations are still assigned to the tier.
      parameters:
      - name: TierID
        in: path
        required: true
        schema:
          type: string
      responses:
        '204':
          description: Tier deleted
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/radius:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Networks
      summary: Get RADIUS/AAA configuration
      description: Returns advanced RADIUS/AAA server config for ISP-level 802.1X
        authentication, accounting, and CoA.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: RADIUS config
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/RADIUSConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    put:
      tags:
      - Networks
      summary: Configure RADIUS/AAA
      description: Configures advanced RADIUS/AAA for WPA2/WPA3-Enterprise, captive
        portal auth, and employee access. Supports multiple servers with failover,
        CoA for dynamic policy updates, and session accounting.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RADIUSConfig'
      responses:
        '200':
          description: RADIUS configured
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/radius/stats:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      operationId: getCustomersByCustomerIdLocationsByLocationIdRadiusStats
      tags:
      - Networks
      summary: Get RADIUS server statistics per VIF
      description: Returns RADIUS server stats (requests, accepts, rejects, retransmissions,
        timeouts, round-trip time) per VIF. Backed by the `radius_stats` telemetry
        stat type from `Wifi_Stats_Config` and the upstream `RADIUS` table.
      parameters:
      - $ref: '#/components/parameters/PageParam'
      - $ref: '#/components/parameters/PageSizeParam'
      responses:
        '200':
          description: RADIUS statistics
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/PaginatedResponse'
                - type: object
                  properties:
                    items:
                      type: array
                      items:
                        $ref: '#/components/schemas/RadiusStatsRecord'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/flow:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Networks
      summary: Get Flow guest marketing config
      description: 'Returns Flow config: capture method, CRM integrations (Mailchimp/HubSpot/Salesforce),
        consent settings.'
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Flow config
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/FlowConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    put:
      tags:
      - Networks
      summary: Configure Flow guest marketing
      description: 'Configures guest marketing: contact capture method, CRM integration
        settings, data collection consent, and privacy policy.'
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FlowConfig'
      responses:
        '200':
          description: Flow configured
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/flow/campaigns:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Networks
      summary: List marketing campaigns
      description: Returns guest marketing campaigns configured for the WorkPass location,
        including date ranges, offers, enabled channels, and current campaign status.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Campaigns
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/FlowCampaign'
                  meta:
                    $ref: '#/components/schemas/Pagination'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      tags:
      - Networks
      summary: Create marketing campaign
      description: Creates a guest marketing campaign with offer and date range. Captured
        contacts are synced to integrated CRM systems.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FlowCampaign'
      responses:
        '201':
          description: Campaign created
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/flow/campaigns/{campaignId}:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Networks
      summary: Get campaign results
      description: 'Returns campaign performance: captured contacts, conversion rate.'
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: CampaignID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Campaign results
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/FlowCampaign'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    delete:
      tags:
      - Networks
      summary: Delete campaign
      description: Deletes a guest marketing campaign and stops future delivery. Historical
        analytics and captured contacts remain available according to the location
        retention policy.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: CampaignID
        in: path
        required: true
        schema:
          type: string
      responses:
        '204':
          description: Campaign deleted
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/analytics/business:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Telemetry
      summary: Get business analytics (footfall, retention, demographics)
      description: 'Returns comprehensive business analytics for WorkPass locations:
        visitor footfall, session metrics, device breakdown, and engagement.'
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: period
        in: query
        schema:
          type: string
          enum:
          - day
          - week
          - month
          - quarter
          default: week
      responses:
        '200':
          description: Business analytics
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/BusinessAnalytics'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/analytics/footfall:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Telemetry
      summary: Get visitor footfall data
      description: 'Returns visitor footfall: unique visitors, daily averages, peak
        times, return rates.'
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: period
        in: query
        schema:
          type: string
          enum:
          - day
          - week
          - month
          default: week
      responses:
        '200':
          description: Footfall data
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/analytics/export:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Telemetry
      summary: Export analytics report
      description: Exports business analytics as CSV or PDF.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: format
        in: query
        schema:
          type: string
          enum:
          - csv
          - pdf
          default: csv
      - name: period
        in: query
        schema:
          type: string
          enum:
          - week
          - month
          - quarter
          default: month
      responses:
        '200':
          description: Report file
          content:
            application/json:
              schema:
                type: string
                format: binary
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/captive-portal/design:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    post:
      tags:
      - Security
      summary: Upload custom portal branding design
      description: Uploads a custom HTML/CSS/asset bundle for the captive portal splash
        page.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
                type:
                  type: string
                  enum:
                  - html
                  - image
                  - css
      responses:
        '200':
          description: Design uploaded
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/captive-portal/test:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    post:
      tags:
      - Security
      summary: Generate portal preview URL
      description: Generates a temporary preview URL for testing the captive portal
        before going live.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Preview generated
          content:
            application/json:
              schema:
                type: object
                properties:
                  preview_url:
                    type: string
                    format: uri
                  expires_at:
                    type: string
                    format: date-time
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/topology/mesh/optimize:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    post:
      tags:
      - Connectivity
      summary: Trigger mesh topology optimization
      description: 'Analyzes mesh topology and recommends/applies optimizations: backhaul
        reassignment, channel changes, satellite re-parenting.'
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                auto_apply:
                  type: boolean
                  default: false
                dry_run:
                  type: boolean
                  default: true
      responses:
        '202':
          description: Optimization started
          content:
            application/json:
              schema:
                type: object
                properties:
                  optimization_id:
                    type: string
                  recommendations:
                    type: array
                    items:
                      type: object
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/topology/mesh/optimize/{optimizationId}:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    - $ref: '#/components/parameters/OptimizationId'
    get:
      operationId: getCustomersByCustomerIdLocationsByLocationIdTopologyMeshOptimizeByOptimizationId
      tags:
      - Connectivity
      summary: Get optimization status/result
      description: Returns the status and recommendations (or errors) for a mesh topology
        optimization job previously triggered via `POST /locations/{locationId}/topology/mesh/optimize`.
      responses:
        '200':
          description: Optimization status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OptimizationStatus'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/topology/mesh/placement:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Connectivity
      summary: Get satellite placement guidance
      description: Returns placement recommendations for satellite pods based on signal
        strength, backhaul quality, and coverage gaps.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Placement guidance
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        node_id:
                          type: string
                        current_rssi_dbm:
                          type: integer
                        recommendation:
                          type: string
                        suggested_position:
                          type: string
                        coverage_gap:
                          type: boolean
                  meta:
                    $ref: '#/components/schemas/Pagination'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /admin/mac-pools:
    get:
      tags:
      - Partners
      summary: List MAC address pools
      description: Lists MAC address pools for device inventory and auto-provisioning.
      responses:
        '200':
          description: MAC pools
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/MACPool'
                  meta:
                    $ref: '#/components/schemas/Pagination'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      tags:
      - Partners
      summary: Create MAC address pool
      description: Creates a MAC address pool for bulk device provisioning.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MACPoolCreate'
      responses:
        '201':
          description: Pool created
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /admin/mac-pools/{poolId}:
    get:
      tags:
      - Partners
      summary: Get MAC pool details
      description: Returns a MAC address pool, including allocation range, current
        usage, reserved prefixes, and auto-provisioning assignment metadata.
      parameters:
      - name: PoolID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Pool details
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/MACPool'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    delete:
      tags:
      - Partners
      summary: Delete MAC pool
      description: Deletes an unused MAC address pool. Implementations should reject
        deletion when addresses from the pool are allocated to existing inventory
        or provisioning policies.
      parameters:
      - name: PoolID
        in: path
        required: true
        schema:
          type: string
      responses:
        '204':
          description: Pool deleted
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /partners/{partnerId}/commission:
    get:
      tags:
      - Partners
      summary: Get commission rules
      description: Returns commission rules for a partner — per-plan share percentage,
        flat-rate templates, and settlement schedule.
      parameters:
      - name: PartnerID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Commission rules
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/CommissionRule'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    patch:
      tags:
      - Partners
      summary: Update commission rules
      description: Updates the commission rules for a partner — revenue share percentage,
        flat-rate per plan, settlement frequency, and minimum payout threshold.
      parameters:
      - name: PartnerID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CommissionRuleUpdate'
      responses:
        '200':
          description: Commission rules updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/CommissionRule'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /partners/{partnerId}/wallet:
    get:
      tags:
      - Partners
      summary: Get partner wallet
      description: Returns the partner's wallet ledger — current balance, transaction
        history (credits from commissions and debits from payouts), and pending settlements.
      parameters:
      - name: PartnerID
        in: path
        required: true
        schema:
          type: string
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: per_page
        in: query
        schema:
          type: integer
          default: 50
          maximum: 200
      responses:
        '200':
          description: Partner wallet
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/PartnerWallet'
                  meta:
                    $ref: '#/components/schemas/Pagination'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customer-groups:
    get:
      tags:
      - Groups
      summary: List customer groups
      description: Returns a paginated list of customer groups — hierarchical containers
        grouping customers (and their devices/nodes) for fleet-wide policy, config,
        firmware, and RBAC.
      parameters:
      - name: parent_id
        in: query
        description: Filter by parent group ID (returns immediate children)
        schema:
          type: string
      - name: member_type
        in: query
        schema:
          type: string
          enum:
          - customer
          - location
          - node
      - name: search
        in: query
        schema:
          type: string
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: per_page
        in: query
        schema:
          type: integer
          default: 50
          maximum: 200
      responses:
        '200':
          description: Customer group list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/CustomerGroup'
                  meta:
                    $ref: '#/components/schemas/Pagination'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      tags:
      - Groups
      summary: Create customer group
      description: Creates a new customer group for fleet-wide policy and config management.
        Groups form a hierarchy via parent_group_id and members are added via tags
        or query selectors.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CustomerGroupCreate'
      responses:
        '201':
          description: Group created
          headers:
            Location:
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/CustomerGroup'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customer-groups/{deviceGroupId}/members:
    get:
      tags:
      - Groups
      summary: List group members
      description: Returns the resolved member set of a customer group — the customers,
        locations, and nodes matching the group's tag selectors or direct assignments.
      parameters:
      - name: GroupID
        in: path
        required: true
        schema:
          type: string
      - name: member_type
        in: query
        schema:
          type: string
          enum:
          - customer
          - location
          - node
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: per_page
        in: query
        schema:
          type: integer
          default: 100
          maximum: 500
      responses:
        '200':
          description: Group members
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/GroupMember'
                  meta:
                    $ref: '#/components/schemas/Pagination'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      tags:
      - Groups
      summary: Add members to group
      description: Adds members (customers, locations, or nodes) to a customer group
        by ID or tag-based selection. Supports bulk addition for large fleet operations.
      parameters:
      - name: GroupID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GroupMemberAdd'
      responses:
        '200':
          description: Members added
          content:
            application/json:
              schema:
                type: object
                properties:
                  added:
                    type: integer
                  skipped:
                    type: integer
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customer-groups/{deviceGroupId}/members/{memberId}:
    delete:
      tags:
      - Groups
      summary: Remove member from group
      description: Removes a single member from a customer group. The member's config
        reverts to the next inheritance level.
      parameters:
      - name: GroupID
        in: path
        required: true
        schema:
          type: string
      - name: memberId
        in: path
        required: true
        schema:
          type: string
      responses:
        '204':
          description: Member removed
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customer-groups/{deviceGroupId}/config:
    get:
      tags:
      - Customers
      summary: Get group-level config
      description: Returns the configuration override set defined at the group scope.
        When queried with `?effective=true` the resolved effective config is returned
        by merging upwards through the inheritance chain (node → group → location
        → customer).
      parameters:
      - name: GroupID
        in: path
        required: true
        schema:
          type: string
      - name: effective
        in: query
        description: Return the fully-resolved effective config (merge chain)
        schema:
          type: boolean
          default: false
      responses:
        '200':
          description: Group config
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/InheritableConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    patch:
      tags:
      - Customers
      summary: Update group-level config
      description: Sets or clears configuration overrides at the group scope. These
        override values flow down to member customers/nodes unless overridden at a
        more specific scope.
      parameters:
      - name: GroupID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/InheritableConfigUpdate'
      responses:
        '200':
          description: Group config updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/InheritableConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customer-groups/{deviceGroupId}/stats:
    get:
      tags:
      - Groups
      summary: Get group aggregate stats
      description: Returns aggregated statistics for all members in a customer group
        — total nodes online, total traffic volume, average health score, and device
        counts by type.
      parameters:
      - name: GroupID
        in: path
        required: true
        schema:
          type: string
      - name: period
        in: query
        schema:
          type: string
          default: 24h
          enum:
          - 1h
          - 24h
          - 7d
          - 30d
      responses:
        '200':
          description: Group stats
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/GroupStats'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/config:
    get:
      tags:
      - Customers
      summary: Get customer-level config
      description: Returns the configuration override set at the customer scope. With
        `?effective=true` the resolved effective config merges location and node overrides
        into the customer baseline.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: effective
        in: query
        description: Return the fully-resolved effective config (merge chain)
        schema:
          type: boolean
          default: false
      responses:
        '200':
          description: Customer config
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/InheritableConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    patch:
      tags:
      - Customers
      summary: Update customer-level config
      description: Sets or clears configuration overrides at the customer scope. Override
        values propagate to all locations and nodes under the customer unless overridden
        at a more specific tier.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/InheritableConfigUpdate'
      responses:
        '200':
          description: Customer config updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/InheritableConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/config:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Customers
      summary: Get location-level config
      description: Returns the configuration override set at the location scope. Supports
        `?effective=true` for merged resolution.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: effective
        in: query
        description: Return the fully-resolved effective config (merge chain)
        schema:
          type: boolean
          default: false
      responses:
        '200':
          description: Location config
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/InheritableConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    patch:
      tags:
      - Customers
      summary: Update location-level config
      description: Sets or clears configuration overrides at the location scope.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/InheritableConfigUpdate'
      responses:
        '200':
          description: Location config updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/InheritableConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /inventory/devices:
    get:
      tags:
      - Partners
      summary: List inventory devices
      description: Returns a paginated list of devices in the ISP's inventory — serial/MAC
        tracking across the entire lifecycle from received through installed to RMA/written-off.
        Returns hardware inventory devices by serial number (manufacturing lifecycle).
        For connected client devices by MAC, use the location-scoped devices endpoint.
      parameters:
      - name: status
        in: query
        schema:
          type: string
          enum:
          - received
          - reserved
          - dispatched
          - installed
          - recovered
          - returned
          - written_off
          - lost
      - name: model
        in: query
        schema:
          type: string
      - name: manufacturer
        in: query
        schema:
          type: string
      - name: serial
        in: query
        schema:
          type: string
      - name: warehouse_id
        in: query
        schema:
          type: string
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: per_page
        in: query
        schema:
          type: integer
          default: 50
          maximum: 200
      responses:
        '200':
          description: Inventory device list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/InventoryDevice'
                  meta:
                    $ref: '#/components/schemas/Pagination'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      tags:
      - Partners
      summary: Register inventory device
      description: Registers a new device in inventory by serial number and MAC. The
        device enters in `received` status and may be associated with a purchase order
        and warehouse location.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/InventoryDeviceCreate'
      responses:
        '201':
          description: Device registered
          headers:
            Location:
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/InventoryDevice'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /inventory/devices/{serialNumber}:
    get:
      tags:
      - Partners
      summary: Get device by serial
      description: Returns the full lifecycle record for a device — current status,
        location/customer binding, RMA history, and chain-of-custody log.
      parameters:
      - name: serialNumber
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Device details
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/InventoryDevice'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    patch:
      tags:
      - Partners
      summary: Update device inventory record
      description: Updates an inventory device record — status change, location assignment,
        or ownership transfer. Status transitions trigger chain-of-custody log entries.
      parameters:
      - name: serialNumber
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/InventoryDeviceUpdate'
      responses:
        '200':
          description: Device updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/InventoryDevice'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /inventory/orders:
    get:
      tags:
      - Partners
      summary: List purchase orders
      description: Returns a paginated list of purchase orders — supplier, status,
        line items, and associated goods receipt notes.
      parameters:
      - name: status
        in: query
        schema:
          type: string
          enum:
          - draft
          - submitted
          - partially_received
          - received
          - cancelled
      - name: supplier
        in: query
        schema:
          type: string
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: per_page
        in: query
        schema:
          type: integer
          default: 50
          maximum: 200
      responses:
        '200':
          description: Purchase order list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/PurchaseOrder'
                  meta:
                    $ref: '#/components/schemas/Pagination'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      tags:
      - Partners
      summary: Create purchase order
      description: Creates a new purchase order with supplier details and line items.
        The PO starts in `draft` status and can be submitted for supplier fulfillment.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PurchaseOrderCreate'
      responses:
        '201':
          description: PO created
          headers:
            Location:
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/PurchaseOrder'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /inventory/orders/{orderId}:
    get:
      tags:
      - Partners
      summary: Get purchase order details
      description: Returns a purchase order including line items, goods receipt notes,
        and fulfilment status.
      parameters:
      - name: OrderID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Purchase order details
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/PurchaseOrder'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    patch:
      tags:
      - Partners
      summary: Update purchase order
      description: Updates a purchase order — submit to supplier, record goods receipt
        with serial numbers, or cancel. Goods receipt creates inventory device records.
      parameters:
      - name: OrderID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PurchaseOrderUpdate'
      responses:
        '200':
          description: Purchase order updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/PurchaseOrder'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /inventory/rma:
    get:
      tags:
      - Partners
      summary: List RMA cases
      description: Returns a paginated list of RMA (Return Merchandise Authorization)
        cases with status, original/replacement device serials, and vendor disposition.
      parameters:
      - name: status
        in: query
        schema:
          type: string
          enum:
          - pending
          - shipped
          - received
          - investigating
          - approved
          - rejected
          - completed
      - name: serial
        in: query
        schema:
          type: string
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: per_page
        in: query
        schema:
          type: integer
          default: 50
          maximum: 200
      responses:
        '200':
          description: RMA list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/RMACase'
                  meta:
                    $ref: '#/components/schemas/Pagination'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      tags:
      - Partners
      summary: Create RMA case
      description: Opens a new RMA case for a device — records the original serial,
        issue description, and initiates the return-to-vendor or replacement workflow.
        Links to the replacement device if available.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RMACaseCreate'
      responses:
        '201':
          description: RMA case created
          headers:
            Location:
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/RMACase'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/wips:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Security
      summary: Get WIPS configuration
      description: Returns the Wireless Intrusion Prevention System configuration
        for a location — scanning policy, detection classes (rogue AP, evil-twin,
        deauth flood, BLE bridge, etc.), thresholds, and containment rules.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: WIPS configuration
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/WIPSConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    patch:
      tags:
      - Security
      summary: Update WIPS configuration
      description: Configures WIPS scanning policy, detection class toggles and thresholds,
        containment mode (off/manual/auto), sensor AP assignments, and friendly MAC/SSID
        allowlists.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WIPSConfigUpdate'
      responses:
        '200':
          description: WIPS configuration updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/WIPSConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/wips/alarms:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Security
      summary: List WIPS alarms
      description: Returns a paginated list of WIPS intrusion detection alarms — rogue
        APs, evil-twins, deauth floods, MAC spoofs, and other wireless threats detected
        at the location.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: class
        in: query
        schema:
          type: string
          enum:
          - rogue_ap
          - rogue_client
          - evil_twin
          - deauth_flood
          - disassoc_flood
          - mac_spoof
          - auth_failure_flood
          - beacon_flood
          - adhoc_network
          - ble_bridge
      - name: severity
        in: query
        schema:
          type: string
          enum:
          - critical
          - major
          - minor
          - warning
          - info
      - name: state
        in: query
        schema:
          type: string
          enum:
          - active
          - acknowledged
          - resolved
          - suppressed
      - name: since
        in: query
        schema:
          type: string
          format: date-time
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: per_page
        in: query
        schema:
          type: integer
          default: 50
          maximum: 200
      responses:
        '200':
          description: WIPS alarm list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/WIPSAlarm'
                  meta:
                    $ref: '#/components/schemas/Pagination'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/wips/alarms/{alarmId}:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Security
      summary: Get WIPS alarm details
      description: Returns detailed information for a specific WIPS alarm including
        source/destination MACs, BSSID, channel, RSSI, classification, containment
        state, and forensic capture metadata.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: AlarmID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: WIPS alarm details
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/WIPSAlarm'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    patch:
      tags:
      - Security
      summary: Update WIPS alarm state
      description: Updates the state of a WIPS alarm — acknowledge, resolve, or suppress.
        Manual containment can also be triggered or stopped via this endpoint.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: AlarmID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - state
              properties:
                state:
                  type: string
                  enum:
                  - acknowledged
                  - resolved
                  - suppressed
                containment_action:
                  type: string
                  enum:
                  - contain
                  - stop
                suppress_ttl_hours:
                  type: integer
                  minimum: 1
                  maximum: 8760
                note:
                  type: string
                  maxLength: 500
      responses:
        '200':
          description: WIPS alarm updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/WIPSAlarm'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/wips/suppress:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    post:
      tags:
      - Security
      summary: Create suppression rule
      description: Creates a time-bound suppression rule to silence WIPS alarms matching
        a pattern (MAC, SSID, or regex). Useful for suppressing known-friendly devices
        or planned maintenance.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - match_type
              - match_value
              - ttl_hours
              properties:
                match_type:
                  type: string
                  enum:
                  - mac
                  - ssid
                  - regex
                match_value:
                  type: string
                ttl_hours:
                  type: integer
                  minimum: 1
                  maximum: 8760
                  default: 24
                note:
                  type: string
                  maxLength: 500
      responses:
        '200':
          description: Suppression rule created
          content:
            application/json:
              schema:
                type: object
                properties:
                  suppression_id:
                    type: string
                  expires_at:
                    type: string
                    format: date-time
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/security/ids:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Agents
      summary: Get IDS/IPS engine configuration
      description: Returns the intrusion detection and prevention engine configuration
        for the location. Supports built-in WIPS plus third-party engines such as
        Snort, Suricata, and Zeek. Selection of a third-party engine requires the
        corresponding agent to be available on node firmware and configured via `/nodes/{nodeId}/agents`.
      responses:
        '200':
          description: IDS/IPS engine configuration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IDSConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    put:
      tags:
      - Agents
      summary: Update IDS/IPS engine configuration
      description: Configures the intrusion detection and prevention engine for the
        location, including engine type (wips/snort/suricata/zeek/none), IDS/IPS mode,
        ruleset source, monitored interfaces, alert webhook destination, and performance
        profile.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/IDSConfig'
      responses:
        '200':
          description: IDS/IPS engine configuration updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IDSConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/agents/policies:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Agents
      summary: List location-scoped agent policies
      description: Returns all agent policies configured at the location scope. Policies
        define desired agent provider, config, and enablement state that propagates
        to nodes at the location. Node-level agent config records serve as overrides.
        Uses paginated response.
      parameters:
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: per_page
        in: query
        schema:
          type: integer
          default: 50
          maximum: 200
      responses:
        '200':
          description: Paginated list of agent policies
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/PaginatedResponse'
                - type: object
                  properties:
                    items:
                      type: array
                      items:
                        $ref: '#/components/schemas/AgentPolicy'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      tags:
      - Agents
      summary: Create a location-scoped agent policy
      description: Creates an agent policy at the location scope. Policies propagate
        to nodes at the location unless overridden by node-level agent config records.
        Use `inheritable` to control whether the policy cascades to child scopes (future
        groups/customer defaults).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AgentPolicy'
      responses:
        '201':
          description: Agent policy created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentPolicy'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/agents/policies/{policyId}:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    - name: policyId
      in: path
      required: true
      schema:
        type: string
    get:
      tags:
      - Agents
      summary: Get a location-scoped agent policy
      description: Returns a single agent policy by ID, including its desired config,
        provider, priority, and inheritance settings.
      responses:
        '200':
          description: Agent policy
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentPolicy'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Policy not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    patch:
      tags:
      - Agents
      summary: Update a location-scoped agent policy
      description: Updates an existing agent policy. Omitted fields are left unchanged.
        Updating `priority` can change the policy resolution order for overlapping
        agent keys at the same scope.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AgentPolicy'
      responses:
        '200':
          description: Agent policy updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentPolicy'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Policy not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    delete:
      tags:
      - Agents
      summary: Delete a location-scoped agent policy
      description: Removes an agent policy. Nodes previously governed by this policy
        fall back to their own agent config records or any remaining lower-priority
        policies at the same scope.
      responses:
        '204':
          description: Agent policy deleted
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Policy not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/dpp/bootstrap:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    post:
      tags:
      - Networks
      summary: Register DPP bootstrap URI
      description: Registers a Wi-Fi Easy Connect bootstrap URI (QR or NFC payload)
        for an enrollee device. The controller parses the URI to extract the bootstrapping
        public key and MAC address, then queues the device for DPP configuration.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DPPBootstrapRequest'
      responses:
        '201':
          description: Bootstrap URI registered
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/DPPEnrollee'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/dpp/sessions:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Networks
      summary: List DPP sessions
      description: Returns a list of active and recent DPP configuration sessions
        with enrollee state, target SSID, and completion status.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: status
        in: query
        schema:
          type: string
          enum:
          - pending
          - in_progress
          - enrolled
          - failed
      responses:
        '200':
          description: DPP session list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/DPPSession'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      tags:
      - Networks
      summary: Start DPP configuration session
      description: Starts a Wi-Fi Easy Connect configurator session for a target enrollee.
        Specifies the network credential (SSID + passphrase) to push to the device.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DPPSessionCreate'
      responses:
        '201':
          description: DPP session started
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/DPPSession'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/dpp/sessions/{sessionId}:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Networks
      summary: Get DPP session details
      description: Polls the state of a DPP configuration session — auth-init, config-sent,
        enrolled, or failed — with error details if enrollment did not complete.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: SessionID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: DPP session details
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/DPPSession'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/dpp/qr:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    post:
      tags:
      - Networks
      summary: Generate DPP QR code
      description: Generates a Wi-Fi Easy Connect QR code image or bootstrapping URI
        for the gateway acting as an enrollee or for shareable guest authentication.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - type
              - ssid
              properties:
                type:
                  type: string
                  enum:
                  - enrollee
                  - configurator
                ssid:
                  type: string
                passphrase:
                  type: string
                expiry_hours:
                  type: integer
                  default: 24
      responses:
        '200':
          description: QR code generated
          content:
            application/json:
              schema:
                type: object
                properties:
                  uri:
                    type: string
                  qr_base64:
                    type: string
                  expires_at:
                    type: string
                    format: date-time
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/matter/fabrics:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Networks
      summary: List Matter fabrics
      description: Returns all Matter fabrics configured on the Thread border router
        at this location, including fabric IDs, root CA certificates, and node counts.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Fabric list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/MatterFabric'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      tags:
      - Networks
      summary: Create Matter fabric
      description: Creates a new Matter fabric by provisioning a root CA certificate
        and operational key pair on the border router. Each fabric is a distinct trust
        domain for Matter devices.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MatterFabricCreate'
      responses:
        '201':
          description: Fabric created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/MatterFabric'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/matter/commissioning:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    post:
      tags:
      - Networks
      summary: Commission Matter device
      description: Initiates Matter commissioning for a device. The setup payload
        (manual pairing code, QR payload, or BLE discovery) is provided and the border
        router arms the fail-safe timer, performs PASE handshake over BLE/IP, provisions
        credentials, and issues a Node Operational Certificate.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MatterCommissioningRequest'
      responses:
        '201':
          description: Commissioning initiated
          content:
            application/json:
              schema:
                type: object
                properties:
                  session_id:
                    type: string
                  state:
                    type: string
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/matter/nodes:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Networks
      summary: List Matter nodes
      description: Returns all commissioned Matter nodes on the Thread fabric — node
        ID, vendor/product IDs, reachability, software version, and current CASE session.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: fabric_id
        in: query
        schema:
          type: string
      - name: state
        in: query
        schema:
          type: string
          enum:
          - online
          - offline
          - commissioning
          - decommissioned
      responses:
        '200':
          description: Matter node list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/MatterNode'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/matter/nodes/{nodeId}:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Networks
      summary: Get Matter node details
      description: Returns details for a commissioned Matter node — endpoints, clusters,
        attributes, ACL grants, and commissioning history.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: nodeId
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Matter node details
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/MatterNode'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    delete:
      tags:
      - Networks
      summary: Decommission Matter node
      description: Removes a Matter node from the fabric by revoking its Node Operational
        Certificate and sending a decommissioning command.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: nodeId
        in: path
        required: true
        schema:
          type: string
      responses:
        '204':
          description: Node decommissioned
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/tenants:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Networks
      summary: List tenant networks
      description: Returns all tenant networks (secondary isolated networks for MDU/hotel/HaaS)
        configured at the location. Each tenant has its own credentials, VLAN, bandwidth
        policy, and isolation rules.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Tenant network list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/TenantNetwork'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      tags:
      - Networks
      summary: Create tenant network
      description: Creates a new isolated tenant network with a dedicated SSID or
        ePSK credential on a shared SSID, assigned VLAN, bandwidth quota, and isolation
        policies.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TenantNetworkCreate'
      responses:
        '201':
          description: Tenant network created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/TenantNetwork'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/tenants/{tenantId}:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Networks
      summary: Get tenant network details
      description: Returns details for a tenant network including credential state,
        bandwidth usage, connected devices, and egress ACL rules.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: TenantID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Tenant network details
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/TenantNetwork'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    patch:
      tags:
      - Networks
      summary: Update tenant network
      description: Updates a tenant network — bandwidth quota, isolation level, egress
        rules, or expiry.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: TenantID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TenantNetworkUpdate'
      responses:
        '200':
          description: Tenant network updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/TenantNetwork'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    delete:
      tags:
      - Networks
      summary: Delete tenant network
      description: Deletes a tenant network and revokes all associated credentials.
        Active clients are disconnected and the VLAN/bandwidth resources are released.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: TenantID
        in: path
        required: true
        schema:
          type: string
      responses:
        '204':
          description: Tenant network deleted
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/dpi/config:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Security
      summary: Get DPI engine configuration
      description: Returns the Deep Packet Inspection configuration including enabled
        engines, signature DB version, sampling rate, excluded devices, and NFQUEUE/Netfilter
        plugin settings.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: DPI configuration
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/DPIConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    patch:
      tags:
      - Security
      summary: Update DPI engine configuration
      description: Configures the DPI engine — enable/disable handlers, set sampling
        rate, add excluded devices from inspection, or refresh the signature database
        version.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DPIConfigUpdate'
      responses:
        '200':
          description: DPI configuration updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/DPIConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/dpi/policies:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Security
      summary: List DPI policies
      description: Returns all DPI classification and action policies — ordered rules
        that match flows by app, category, domain, MAC and apply actions (block, shape,
        log, redirect, or gatekeeper).
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: DPI policy list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/DPIPolicy'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      tags:
      - Security
      summary: Create DPI policy
      description: Creates a new DPI policy rule for L7 flow classification. Rules
        are ordered by index and can chain via the next field.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DPIPolicyCreate'
      responses:
        '201':
          description: DPI policy created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/DPIPolicy'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/dpi/policies/{policyId}:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Security
      summary: Get DPI policy details
      description: Returns a single DPI policy rule including its match criteria,
        action, index, and chain next pointer.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: PolicyID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: DPI policy details
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/DPIPolicy'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    patch:
      tags:
      - Security
      summary: Update DPI policy
      description: Updates a DPI policy rule — match criteria, action, index ordering,
        or chain next pointer.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: PolicyID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DPIPolicyUpdate'
      responses:
        '200':
          description: DPI policy updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/DPIPolicy'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    delete:
      tags:
      - Security
      summary: Delete DPI policy
      description: Deletes a DPI policy rule. Chain ordering is automatically adjusted
        for remaining rules.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: PolicyID
        in: path
        required: true
        schema:
          type: string
      responses:
        '204':
          description: DPI policy deleted
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/dpi/flows:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Security
      summary: Get live DPI flow stats
      description: Returns the latest DPI-classified flow table — source MAC, app
        ID, category, traffic volume, action taken, and risk level.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: mac
        in: query
        schema:
          type: string
      - name: app_id
        in: query
        schema:
          type: string
      - name: category
        in: query
        schema:
          type: string
      - name: limit
        in: query
        schema:
          type: integer
          default: 100
          maximum: 500
      responses:
        '200':
          description: DPI flow stats
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/DPIFlow'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/dpi/usage:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Security
      summary: Get DPI usage report
      description: Returns per-app data volume breakdown for a time window — app,
        category, bytes up/down, and percentage of total traffic.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: period
        in: query
        schema:
          type: string
          default: 24h
          enum:
          - 1h
          - 24h
          - 7d
          - 30d
      - name: mac
        in: query
        schema:
          type: string
      - name: top_n
        in: query
        schema:
          type: integer
          default: 20
          maximum: 100
      responses:
        '200':
          description: DPI usage report
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/DPIUsageReport'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/parental-controls/profiles:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Security
      summary: List parental control profiles
      description: Returns all parental control profiles — per-child device groupings
        with content filtering rules, screen-time allowances, bedtime schedules, and
        reward state.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Parental control profile list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/ParentalControlProfile'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      tags:
      - Security
      summary: Create parental control profile
      description: Creates a new parental control profile with name, avatar, age band,
        and optional initial device list.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ParentalControlProfileCreate'
      responses:
        '201':
          description: Profile created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/ParentalControlProfile'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/parental-controls/{profileId}/devices:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    post:
      tags:
      - Security
      summary: Assign devices to profile
      description: Assigns one or more devices by MAC to a parental control profile.
        Filtering and screen-time rules are immediately enforced.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: ProfileID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - macs
              properties:
                macs:
                  type: array
                  items:
                    type: string
                    pattern: ^([0-9A-Fa-f]{2}:){5}[0-9A-Fa-f]{2}$
                  minItems: 1
                  maxItems: 50
      responses:
        '200':
          description: Devices assigned
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    delete:
      tags:
      - Security
      summary: Remove devices from profile
      description: Removes specified devices from a parental control profile.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: ProfileID
        in: path
        required: true
        schema:
          type: string
      - name: macs
        in: query
        required: true
        description: Comma-separated MAC addresses
        schema:
          type: string
      responses:
        '200':
          description: Devices removed
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/parental-controls/{profileId}/rewards:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    post:
      tags:
      - Security
      summary: Grant reward
      description: Grants a reward to a profile — extends daily time limit, disables
        off-time blocking, or allows a later bedtime. Rewards expire at midnight by
        default.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: ProfileID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RewardGrant'
      responses:
        '201':
          description: Reward granted
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/parental-controls/usage:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Security
      summary: Get parental control usage
      description: Returns per-device, per-app, and per-category usage minutes for
        all profiles, with daily totals and remaining allowances.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: profile_id
        in: query
        schema:
          type: string
      - name: from
        in: query
        schema:
          type: string
          format: date
      - name: to
        in: query
        schema:
          type: string
          format: date
      - name: granularity
        in: query
        schema:
          type: string
          enum:
          - daily
          - hourly
        default: daily
      responses:
        '200':
          description: Usage report
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/ParentalUsageReport'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /admin/alert-rules:
    get:
      tags:
      - Partners
      summary: List alert rules
      description: Returns all alert rules — conditions and thresholds that generate
        alerts, attached notification channels, severity mapping, and inhibition rules.
      parameters:
      - name: severity
        in: query
        schema:
          type: string
          enum:
          - critical
          - warning
          - info
      - name: enabled
        in: query
        schema:
          type: boolean
      responses:
        '200':
          description: Alert rule list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/AlertRule'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      tags:
      - Partners
      summary: Create alert rule
      description: Creates a new alert rule — condition expression, severity, labels,
        annotations, runbook link, evaluation interval, and notification channel routing.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AlertRuleCreate'
      responses:
        '201':
          description: Alert rule created
          headers:
            Location:
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/AlertRule'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /admin/alert-rules/{ruleId}:
    get:
      tags:
      - Partners
      summary: Get alert rule details
      description: Returns an alert rule including current firing state, channel mappings,
        and recent alert history.
      parameters:
      - name: RuleID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Alert rule details
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/AlertRule'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    patch:
      tags:
      - Partners
      summary: Update alert rule
      description: Updates an alert rule — condition, severity, channel bindings,
        or enable/disable.
      parameters:
      - name: RuleID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AlertRuleUpdate'
      responses:
        '200':
          description: Alert rule updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/AlertRule'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    delete:
      tags:
      - Partners
      summary: Delete alert rule
      description: Deletes an alert rule. Fired alerts from this rule are preserved
        but no new alerts will fire.
      parameters:
      - name: RuleID
        in: path
        required: true
        schema:
          type: string
      responses:
        '204':
          description: Alert rule deleted
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /admin/alerts:
    get:
      tags:
      - Partners
      summary: List alerts
      description: Returns a paginated list of alert instances — firing, acknowledged,
        resolved, and silenced, filtered by severity, status, labels, and time range.
      parameters:
      - name: status
        in: query
        schema:
          type: string
          enum:
          - firing
          - acknowledged
          - resolved
          - silenced
      - name: severity
        in: query
        schema:
          type: string
          enum:
          - critical
          - warning
          - info
      - name: rule_id
        in: query
        schema:
          type: string
      - name: since
        in: query
        schema:
          type: string
          format: date-time
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: per_page
        in: query
        schema:
          type: integer
          default: 50
          maximum: 200
      responses:
        '200':
          description: Alert list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/Alert'
                  meta:
                    $ref: '#/components/schemas/Pagination'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /admin/alerts/{alertId}:
    get:
      tags:
      - Partners
      summary: Get alert details
      description: Returns a specific alert instance — labels, annotations, values,
        firing duration, and notification delivery log.
      parameters:
      - name: AlertID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Alert details
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/Alert'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    patch:
      tags:
      - Partners
      summary: Update alert state
      description: Updates the state of an alert — acknowledge (stop reminders), resolve,
        or silence for a specified duration.
      parameters:
      - name: AlertID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - status
              properties:
                status:
                  type: string
                  enum:
                  - acknowledged
                  - resolved
                  - silenced
                silence_hours:
                  type: integer
                  minimum: 1
                  maximum: 8760
                comment:
                  type: string
                  maxLength: 1000
      responses:
        '200':
          description: Alert state updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/Alert'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/routing/dynamic:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Connectivity
      summary: Get dynamic routing configuration
      description: Returns the dynamic routing protocol configuration — enabled protocols
        per interface, RIPv2/RIPng/OSPFv2/v3 settings, redistribution policies, and
        passive-interface assignments.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Dynamic routing configuration
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/DynamicRoutingConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    patch:
      tags:
      - Connectivity
      summary: Update dynamic routing configuration
      description: Configures dynamic routing — enable/disable RIPv2, RIPng, OSPFv2/v3,
        set timers, split-horizon, redistribute static/connected routes, and assign
        passive interfaces.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DynamicRoutingConfigUpdate'
      responses:
        '200':
          description: Dynamic routing configuration updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/DynamicRoutingConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/routing/dynamic/routes:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Connectivity
      summary: Get learned dynamic routes
      description: Returns the current RIB/FIB of routes learned via dynamic routing
        protocols — destination, next-hop, metric, protocol, and interface.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: protocol
        in: query
        schema:
          type: string
          enum:
          - rip
          - ripng
          - ospf
          - ospfv3
          - bgp
      responses:
        '200':
          description: Learned routes
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/LearnedRoute'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/vlans:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Networks
      summary: List VLANs
      description: Returns the VLAN database — all configured VLANs with bridge assignments,
        tagged/untagged port memberships, PVID, and SSID mappings.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: bridge
        in: query
        schema:
          type: string
          enum:
          - br-home
          - br-wan
          - br-guest
          - br-iot
      responses:
        '200':
          description: VLAN database
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/VLANConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      tags:
      - Networks
      summary: Create VLAN
      description: Creates a new VLAN with a VLAN ID, bridge assignment, and tagged/untagged
        port memberships.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VLANConfigCreate'
      responses:
        '201':
          description: VLAN created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/VLANConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/vlans/{vlanId}:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Networks
      summary: Get VLAN details
      description: Returns a single VLAN entry with full tagged/untagged port membership,
        PVID, and SSID bindings.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: VLANID
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
          maximum: 4094
      responses:
        '200':
          description: VLAN details
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/VLANConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    patch:
      tags:
      - Networks
      summary: Update VLAN
      description: Updates a VLAN — change bridge, add/remove tagged or untagged ports,
        set PVID, or map SSIDs to the VLAN.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: VLANID
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
          maximum: 4094
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VLANConfigUpdate'
      responses:
        '200':
          description: VLAN updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/VLANConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    delete:
      tags:
      - Networks
      summary: Delete VLAN
      description: Deletes a VLAN. All port memberships and SSID bindings for this
        VLAN are released.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: VLANID
        in: path
        required: true
        schema:
          type: integer
          minimum: 1
          maximum: 4094
      responses:
        '204':
          description: VLAN deleted
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/power/config:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Nodes
      summary: Get power management configuration
      description: Returns power management settings — radio enabled/disabled state
        per band, TX power, LED state and pattern, eco/ATX idle mode, PoE budget,
        and thermal throttle thresholds.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Power management configuration
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/PowerConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    patch:
      tags:
      - Nodes
      summary: Update power management configuration
      description: Updates power management — enable/disable radios per band, set
        TX power, configure LED state and patterns, set eco mode/wake schedule, and
        adjust thermal throttle thresholds.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PowerConfigUpdate'
      responses:
        '200':
          description: Power management configuration updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/PowerConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/power/schedule:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Nodes
      summary: Get power schedule
      description: Returns the power/radio on-off schedule — time windows per band
        per day for automatic radio deactivation to save energy.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Power schedule
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/PowerSchedule'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    patch:
      tags:
      - Nodes
      summary: Update power schedule
      description: Sets a power schedule — time windows when radios should be off
        to conserve energy.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PowerScheduleUpdate'
      responses:
        '200':
          description: Power schedule updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/PowerSchedule'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/management/config:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Locations
      summary: Get configuration snapshot
      description: Returns a snapshot of the current running configuration — all configurable
        parameters formatted for backup or manual inspection.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: format
        in: query
        schema:
          type: string
          enum:
          - json
          - yaml
        default: json
      responses:
        '200':
          description: Configuration snapshot
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/ConfigSnapshot'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      tags:
      - Locations
      summary: Restore configuration
      description: Restores a previously backed-up configuration to the node. The
        restore is applied transactionally and can auto-revert if connectivity is
        lost.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConfigRestore'
      responses:
        '200':
          description: Configuration restored
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/management/maintenance:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Locations
      summary: Get maintenance state
      description: Returns the current maintenance mode state — active, scheduled,
        or inactive.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Maintenance state
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/MaintenanceState'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    patch:
      tags:
      - Locations
      summary: Update maintenance state
      description: Toggle maintenance mode — schedule a maintenance window, activate
        maintenance mode with a banner, or deactivate and resume normal operations.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MaintenanceStateUpdate'
      responses:
        '200':
          description: Maintenance state updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/MaintenanceState'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/management/sync:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Locations
      summary: Check configuration drift
      description: Compares the node's running configuration against the cloud's last-known-good
        configuration and returns any differences (drift).
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: node_id
        in: query
        description: Specific node to check (defaults to gateway)
        schema:
          type: string
      responses:
        '200':
          description: Configuration drift report
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/ConfigDriftReport'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      tags:
      - Locations
      summary: Sync configuration
      description: Pushes the cloud's last-known-good configuration to the node to
        resolve drift. Triggers a graceful configuration reload on the node.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Configuration synced
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/gre/tunnels:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Connectivity
      summary: List GRE tunnels
      description: Returns all GRE tunnels configured at the location — tunnel interfaces
        with source/destination, GRE key, MTU, checksum, keepalive, and live status.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: GRE tunnel list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/GRETunnel'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      tags:
      - Connectivity
      summary: Create GRE tunnel
      description: Creates a new GRE tunnel (or GRE6/ip6tnl) with source/destination
        IPs, optional GRE key, checksum, sequence numbers, MTU, and keepalive settings.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GRETunnelCreate'
      responses:
        '201':
          description: GRE tunnel created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/GRETunnel'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/gre/tunnels/{tunnelId}:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Connectivity
      summary: Get GRE tunnel details
      description: Returns detailed status for a GRE tunnel — interface up/down, keepalive
        state, packet counters, peer registration (mGRE/NHRP), and MTU probe results.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: TunnelID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: GRE tunnel details
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/GRETunnel'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    patch:
      tags:
      - Connectivity
      summary: Update GRE tunnel
      description: Updates a GRE tunnel — change key, MTU, toggle checksum/seqnum,
        adjust keepalive, or enable/disable the tunnel interface.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: TunnelID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GRETunnelUpdate'
      responses:
        '200':
          description: GRE tunnel updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/GRETunnel'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    delete:
      tags:
      - Connectivity
      summary: Delete GRE tunnel
      description: Deletes a GRE tunnel interface and releases its IP resources.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: TunnelID
        in: path
        required: true
        schema:
          type: string
      responses:
        '204':
          description: GRE tunnel deleted
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/qoe/rules:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Telemetry
      summary: List QoE rules
      description: Returns all QoE (Quality of Experience) prioritization rules —
        per-app or per-device priority assignments with DSCP marking, WME access category,
        reserved bandwidth, and schedule.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: QoE rule list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/QoERule'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      tags:
      - Telemetry
      summary: Create QoE rule
      description: Creates a new QoE prioritization rule — select an app or device,
        assign a priority (high/normal/low/blocked), set DSCP mark, WME access category,
        and optionally reserve bandwidth.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/QoERuleCreate'
      responses:
        '201':
          description: QoE rule created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/QoERule'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/qoe/rules/{ruleId}:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Telemetry
      summary: Get QoE rule details
      description: Returns a single QoE rule including priority, DSCP mark, WME class,
        reserved bandwidth, and active schedule.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: RuleID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: QoE rule details
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/QoERule'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    patch:
      tags:
      - Telemetry
      summary: Update QoE rule
      description: Updates a QoE rule — change priority level, DSCP marking, reserved
        bandwidth, schedule, or enable/disable.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: RuleID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/QoERuleUpdate'
      responses:
        '200':
          description: QoE rule updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/QoERule'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    delete:
      tags:
      - Telemetry
      summary: Delete QoE rule
      description: Deletes a QoE prioritization rule. Traffic reverts to default QoS
        handling.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: RuleID
        in: path
        required: true
        schema:
          type: string
      responses:
        '204':
          description: QoE rule deleted
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/guest-invitations:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Networks
      summary: List guest invitations
      description: Returns all guest WiFi invitations — pending, active, redeemed,
        and expired time-bound shareable access tokens with QR codes and status.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: status
        in: query
        schema:
          type: string
          enum:
          - pending
          - active
          - redeemed
          - expired
          - revoked
      responses:
        '200':
          description: Guest invitation list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/GuestInvitation'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      tags:
      - Networks
      summary: Create guest invitation
      description: Creates a time-bound guest WiFi invitation — generates a shareable
        link and QR code for the guest SSID with bandwidth limits, max devices, and
        automatic expiry.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GuestInvitationCreate'
      responses:
        '201':
          description: Invitation created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/GuestInvitation'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/guest-invitations/{invitationId}:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Networks
      summary: Get guest invitation details
      description: Returns details for a guest invitation — token, QR URL, expiry,
        bandwidth caps, redemption count, and status.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: InvitationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Guest invitation details
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/GuestInvitation'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    patch:
      tags:
      - Networks
      summary: Update guest invitation
      description: Extends or updates a guest invitation — change expiry, bandwidth
        limits, or max devices.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: InvitationID
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GuestInvitationUpdate'
      responses:
        '200':
          description: Guest invitation updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/GuestInvitation'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    delete:
      tags:
      - Networks
      summary: Revoke guest invitation
      description: Revokes a guest invitation — immediately deactivates the link/QR
        and disconnects any active sessions using this invitation.
      parameters:
      - name: ID
        in: path
        required: true
        schema:
          type: string
      - name: LocationID
        in: path
        required: true
        schema:
          type: string
      - name: InvitationID
        in: path
        required: true
        schema:
          type: string
      responses:
        '204':
          description: Invitation revoked
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /deployments:
    get:
      tags:
      - Deployments
      summary: List deployments
      description: Returns a paginated list of named deployments. Each deployment
        is both an environment classification and a device group — nodes belong directly
        to a deployment (no separate fleet concept). The tier determines protection
        level (1=production, protected; 2=development, unrestricted).
      parameters:
      - name: tier
        in: query
        schema:
          type: integer
          enum:
          - 1
          - 2
        description: Filter by deployment tier (1=production, 2=development)
      - name: is_protected
        in: query
        schema:
          type: boolean
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: per_page
        in: query
        schema:
          type: integer
          default: 50
          maximum: 200
      responses:
        '200':
          description: Deployment list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/Deployment'
                  meta:
                    $ref: '#/components/schemas/Pagination'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      tags:
      - Deployments
      summary: Create deployment
      description: Creates a new named deployment. The tier determines the protection
        rules — tier 1 (production) devices cannot be reassigned to tier 2 (development)
        deployments. Tier 2 devices can be promoted to tier 1.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DeploymentCreate'
      responses:
        '201':
          description: Deployment created
          headers:
            Location:
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/Deployment'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '409':
          description: Conflict — a deployment with this name already exists
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /deployments/{deploymentId}:
    parameters:
    - $ref: '#/components/parameters/DeploymentId'
    get:
      tags:
      - Deployments
      summary: Get deployment
      description: Returns details for a single deployment, including node count.
      responses:
        '200':
          description: Deployment details
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/Deployment'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Deployment not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    patch:
      tags:
      - Deployments
      summary: Update deployment
      description: Updates deployment metadata. The tier cannot be changed while nodes
        are assigned, as that would violate protection rules.
      parameters:
      - name: deploymentId
        in: path
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DeploymentUpdate'
      responses:
        '200':
          description: Deployment updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/Deployment'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Deployment not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '422':
          description: Cannot change tier while nodes are assigned
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    delete:
      tags:
      - Deployments
      summary: Delete deployment
      description: Deletes a deployment. All member nodes must be removed first. Protected
        deployments (tier 1) require a `confirm=true` query parameter.
      parameters:
      - name: deploymentId
        in: path
        required: true
        schema:
          type: string
      - name: confirm
        in: query
        schema:
          type: boolean
        description: Required to be true when deleting a protected (tier 1) deployment
      responses:
        '204':
          description: Deployment deleted
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Deployment not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '409':
          description: Deployment still contains nodes — remove all members first
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '422':
          description: Confirmation required for protected deployment deletion
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /deployments/{deploymentId}/members:
    parameters:
    - $ref: '#/components/parameters/DeploymentId'
    get:
      tags:
      - Deployments
      summary: List deployment members
      description: Returns a paginated list of nodes assigned to this deployment.
        Each node belongs to exactly one deployment at a time.
      parameters:
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: per_page
        in: query
        schema:
          type: integer
          default: 50
          maximum: 200
      responses:
        '200':
          description: Deployment member list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/DeploymentMember'
                  meta:
                    $ref: '#/components/schemas/Pagination'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Deployment not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      tags:
      - Deployments
      summary: Add members to deployment
      description: Assigns nodes to this deployment. Supports bulk addition for large
        fleet operations. **Protection rule** — nodes whose current deployment is
        tier 1 (production) cannot be reassigned to a tier 2 (development) deployment.
        Promotion from tier 2 to tier 1 is always allowed. Use `forceAssignment` to
        override this safeguard.
      parameters:
      - name: deploymentId
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DeploymentMemberAdd'
      responses:
        '201':
          description: Members added
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/DeploymentMember'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Deployment not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '422':
          description: Protection rule violation — production-tier nodes cannot be
            reassigned to a development deployment
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /deployments/{deploymentId}/members/{memberId}:
    parameters:
    - $ref: '#/components/parameters/DeploymentId'
    delete:
      tags:
      - Deployments
      summary: Remove member from deployment
      description: Removes a single node from the deployment. The node becomes unassigned
        and can be onboarded into a different deployment.
      parameters:
      - name: memberId
        in: path
        required: true
        schema:
          type: string
      responses:
        '204':
          description: Member removed
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Deployment or member not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /nodes/{nodeId}/onboarding:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    get:
      tags:
      - Nodes
      summary: Get node onboarding status
      description: Returns the current onboarding status for a node, including its
        deployment assignment if onboarding is complete. Returns per-node onboarding
        and activation status. For customer-level onboarding wizard, use the customer-scoped
        onboarding endpoint.
      responses:
        '200':
          description: Node onboarding status
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/NodeOnboardingStatus'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Node not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      tags:
      - Nodes
      summary: Onboard node
      description: Onboards a node into a deployment. This is the primary entry point
        for assigning a provisioned node to its operational environment. **Protection
        rule** — nodes whose inventory record shows current membership in a tier 1
        (production) deployment cannot be onboarded into a tier 2 (development) deployment.
        Promotion from tier 2 to tier 1 is always allowed. Use `forceAssignment` with
        appropriate privileges to override.
      parameters:
      - name: nodeId
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NodeOnboardingRequest'
      responses:
        '202':
          description: Onboarding initiated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/NodeOnboardingStatus'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Node or deployment not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '409':
          description: Node is already onboarded to a different deployment
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '422':
          description: Protection rule violation — production-tier devices cannot
            be onboarded into a development deployment — use forceAssignment to override
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /inventory/devices/{serialNumber}/deployment:
    parameters:
    - $ref: '#/components/parameters/SerialNumber'
    get:
      tags:
      - Partners
      summary: Get device deployment assignment
      description: Returns the current deployment assignment for an inventory device.
        Shows which deployment the device belongs to and whether it is in a protected
        tier.
      responses:
        '200':
          description: Device deployment assignment
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/DeviceDeploymentAssignment'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Device not found in inventory
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      tags:
      - Partners
      summary: Assign device to deployment
      description: Assigns an inventory device to a deployment. This updates the device's
        inventory record and cascades to any node linked to this serial number. **Protection
        rule** — devices currently assigned to a tier 1 (production) deployment cannot
        be reassigned to a tier 2 (development) deployment. Promotion upward is always
        allowed. Use `forceAssignment` to override.
      parameters:
      - name: serialNumber
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DeviceDeploymentAssignmentCreate'
      responses:
        '200':
          description: Device assigned to deployment
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/DeviceDeploymentAssignment'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Device or deployment not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '422':
          description: Protection rule violation — production-tier devices cannot
            join a development deployment
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /nodes/{nodeId}/deactivate:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    post:
      tags:
      - Nodes
      summary: Deactivate node
      description: Suspends configuration updates for a node without deleting it.
        The node remains accessible for monitoring and diagnostics but configuration
        changes are not pushed. Deactivation is required before deletion — attempting
        to delete an active node returns a 409 Conflict.
      responses:
        '200':
          description: Node deactivated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/Node'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Node not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '409':
          description: Node is already deactivated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /nodes/{nodeId}/activate:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    post:
      tags:
      - Nodes
      summary: Activate node
      description: Re-activates a previously deactivated node. Configuration updates
        resume immediately. The node's last known configuration is reconciled before
        new changes are pushed.
      responses:
        '200':
          description: Node activated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/Node'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Node not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '409':
          description: Node is already active
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /bulk/nodes/preview:
    post:
      tags:
      - Partners
      summary: Dry-run bulk node operation
      description: Mirrors the `/bulk/nodes` request body and returns the list of
        nodes that would be affected without executing the operation. Supports the
        same `mode` values (claim, update, move). Use this to preview the impact of
        a bulk operation before committing.
      parameters:
      - name: customer_id
        in: query
        required: true
        schema:
          type: string
      - name: location_id
        in: query
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BulkNodesRequest'
      responses:
        '200':
          description: Dry-run result — nodes that would be affected
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/BulkOperationPreview'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /networks/{networkId}/client-psk:
    parameters:
    - $ref: '#/components/parameters/NetworkId'
    get:
      tags:
      - Networks
      summary: List per-client PSK entries
      description: Returns all per-client pre-shared key entries for this SSID. Each
        entry maps a client MAC address to a unique password and optional VLAN assignment.
        This enables a single SSID to assign different VLANs based on the password
        used, without requiring separate SSIDs (PPSK/MPSK).
      parameters:
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: per_page
        in: query
        schema:
          type: integer
          default: 50
          maximum: 200
      responses:
        '200':
          description: Client PSK list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/ClientPsk'
                  meta:
                    $ref: '#/components/schemas/Pagination'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Network not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      tags:
      - Networks
      summary: Create per-client PSK entry
      description: Adds a new per-client PSK entry. The client MAC address is paired
        with a unique passphrase and an optional VLAN ID. When a client authenticates
        with this passphrase on the SSID, it is assigned to the specified VLAN. Supports
        bulk creation for fleet-wide client provisioning.
      parameters:
      - name: networkId
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ClientPskCreate'
      responses:
        '201':
          description: Client PSK entry created
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/ClientPsk'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Network not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '409':
          description: A PSK entry for this MAC already exists on this network
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /networks/{networkId}/client-psk/{pskId}:
    parameters:
    - $ref: '#/components/parameters/NetworkId'
    get:
      tags:
      - Networks
      summary: Get per-client PSK entry
      description: Returns the details of a single per-client PSK entry. The passphrase
        is not returned — only the MAC, VLAN ID, and metadata are visible.
      parameters:
      - name: pskId
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Client PSK entry details
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/ClientPsk'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: PSK entry or network not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    delete:
      tags:
      - Networks
      summary: Delete per-client PSK entry
      description: Removes a per-client PSK entry. The client can no longer use this
        passphrase on the SSID. Active sessions using this PSK are disconnected.
      parameters:
      - name: networkId
        in: path
        required: true
        schema:
          type: string
      - name: pskId
        in: path
        required: true
        schema:
          type: string
      responses:
        '204':
          description: PSK entry deleted
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: PSK entry or network not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /nodes/{nodeId}/config/export:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    get:
      tags:
      - Nodes
      summary: Export node configuration
      description: Downloads the effective configuration for a node as a tar.gz archive.
        Includes all applied templates, OVSDB config rows, and generated files. The
        `format` query parameter selects the output format (netjson for machine-readable
        JSON, openwrt for UCI-compatible output).
      parameters:
      - name: format
        in: query
        schema:
          type: string
          enum:
          - netjson
          - openwrt
          default: netjson
        description: Output format for the configuration archive
      responses:
        '200':
          description: Configuration archive (tar.gz)
          content:
            application/gzip:
              schema:
                type: string
                format: binary
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Node not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /nodes/{nodeId}/commands:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    post:
      tags:
      - Nodes
      summary: Execute command on node
      description: Sends a command to be executed on the node. Supported command types
        include shell commands, reboot, factory reset, connectivity tests (ping, traceroute),
        and WiFi diagnostics (iwinfo, survey). Commands are queued and executed asynchronously
        — poll the returned command ID for status.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NodeCommandRequest'
      responses:
        '202':
          description: Command queued for execution
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/NodeCommand'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Node not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '409':
          description: Node is offline — cannot execute command
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /nodes/{nodeId}/commands/{commandId}:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    get:
      tags:
      - Nodes
      summary: Get command status
      description: Returns the current status and result of a previously submitted
        command. Status values — queued, running, completed, failed, timed_out. The
        output field contains the command's stdout when completed.
      parameters:
      - name: commandId
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Command status and result
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/NodeCommand'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Node or command not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /nodes/{nodeId}/coordinates:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    put:
      tags:
      - Nodes
      summary: Update node coordinates
      description: Updates the geographic coordinates of a node. Intended for both
        admin use and device self-reporting. When called by a device using its key
        authentication, multi-tenancy checks are bypassed (device-facing endpoint).
        Coordinates are in GeoJSON Point format.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Coordinates'
      responses:
        '200':
          description: Coordinates updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/Coordinates'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Node not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /nodes/{nodeId}/alert-settings:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    get:
      tags:
      - Nodes
      summary: Get per-device alert thresholds
      description: Returns the alert threshold overrides for this specific node. Different
        device types (gateway vs extender) have different baseline expectations —
        these per-device settings override the fleet-wide alert rules defined in `/admin/alert-rules`.
      responses:
        '200':
          description: Alert threshold overrides
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/DeviceAlertSettings'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Node not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    patch:
      tags:
      - Nodes
      summary: Update per-device alert thresholds
      description: Configures alert threshold overrides for this node. Each threshold
        can be set to a custom value or `null` to revert to the fleet-wide default.
        Supported metrics — ping_loss_percent, rtt_ms, cpu_percent, memory_percent,
        uptime_seconds, traffic_mbps.
      parameters:
      - name: nodeId
        in: path
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DeviceAlertSettingsUpdate'
      responses:
        '200':
          description: Alert thresholds updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/DeviceAlertSettings'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Node not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /nodes/{nodeId}/agent-bridge/config:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    get:
      tags:
      - Agents
      summary: Get agent MQTT bridge configuration
      description: Returns the MQTT agent bridge configuration for this node. The agent
        bridge runs a local Mosquitto broker on the device and maintains a persistent
        MQTT bridge connection to the cloud broker for third-party agent communication.
      responses:
        '200':
          description: Agent bridge configuration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentBridgeConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Node not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    put:
      tags:
      - Agents
      summary: Update agent MQTT bridge configuration
      description: Configures the MQTT agent bridge on this node, including the cloud
        broker address and port, TLS certificates, connection keepalive interval, maximum
        offline queue size, fallback broker settings, and enable/disable state.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AgentBridgeConfig'
      responses:
        '200':
          description: Agent bridge configuration updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentBridgeConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Node not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /nodes/{nodeId}/agent-bridge/status:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    get:
      tags:
      - Agents
      summary: Get agent MQTT bridge runtime status
      description: Returns the live operational status of the MQTT agent bridge on
        this node, including connection state, queue depth, connected agent count,
        TLS version, certificate expiry, and emergency mode indicators.
      responses:
        '200':
          description: Agent bridge status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentBridgeStatus'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Node not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /nodes/{nodeId}/agents:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    get:
      tags:
      - Agents
      summary: List third-party agent config records
      description: Returns all desired agent control and config records known to the
        cloud for this node. This does not reflect actual runtime state — use `/agents/{agentId}/status`
        for live health and metrics. Agent availability is discovered from the node
        firmware, not created by this endpoint.
      parameters:
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: per_page
        in: query
        schema:
          type: integer
          default: 50
          maximum: 200
      responses:
        '200':
          description: Paginated list of agent config records
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/PaginatedResponse'
                - type: object
                  properties:
                    items:
                      type: array
                      items:
                        $ref: '#/components/schemas/Agent'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      tags:
      - Agents
      summary: Create an agent config record
      description: Creates a desired control and config record for a supported or
        custom agent key. This does **not** install software on the CPE — binary selection,
        packaging, placement, and supervision are the firmware designer's responsibility.
        The cloud manages desired configuration state only. Available agent keys and
        their capabilities are discovered from `/agents/capabilities`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AgentConfigUpdate'
      responses:
        '201':
          description: Agent config record created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Agent'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /nodes/{nodeId}/agents/capabilities:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    get:
      tags:
      - Agents
      summary: List agent capabilities discovered from the node
      description: Returns firmware- or node-discovered agent keys, supported providers,
        config schema hints, available actions, and whether status and metrics are
        supported. This inventory is reported by the node; the cloud does not invent
        agent capabilities.
      responses:
        '200':
          description: List of discovered agent capabilities
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentCapabilityList'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /nodes/{nodeId}/agents/{agentId}:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    - name: agentId
      in: path
      required: true
      schema:
        type: string
    get:
      tags:
      - Agents
      summary: Get an agent config record
      description: Returns the desired config and control state for a specific agent.
        Secret values are never returned — use the `secretRefs` field to see which
        secrets are configured.
      responses:
        '200':
          description: Agent config record
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Agent'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Agent config record not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    patch:
      tags:
      - Agents
      summary: Update an agent config record
      description: Updates desired config, secrets, and enablement state for an agent.
        Fields omitted from the request body are left unchanged. Secret values are
        write-only and never returned by read endpoints.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AgentConfigUpdate'
      responses:
        '200':
          description: Agent config record updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Agent'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Agent config record not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    delete:
      tags:
      - Agents
      summary: Delete an agent config record
      description: Removes the cloud-managed desired config and control record for
        this agent. This does **not** uninstall software from the CPE. The agent may
        continue to run with its last-known configuration until the node firmware
        intervenes.
      responses:
        '204':
          description: Agent config record deleted
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Agent config record not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /nodes/{nodeId}/agents/{agentId}/enable:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    - name: agentId
      in: path
      required: true
      schema:
        type: string
    post:
      tags:
      - Agents
      summary: Enable an agent
      description: Enables the agent at the desired-state level. The node firmware
        is responsible for acting on the enable instruction. This is equivalent to
        setting `enabled = true` via `PATCH` but provides an explicit audit trail
        for enable actions.
      responses:
        '200':
          description: Agent enabled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Agent'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Agent config record not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /nodes/{nodeId}/agents/{agentId}/disable:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    - name: agentId
      in: path
      required: true
      schema:
        type: string
    post:
      tags:
      - Agents
      summary: Disable an agent
      description: Disables the agent at the desired-state level. The node firmware
        is responsible for acting on the disable instruction. This is equivalent to
        setting `enabled = false` via `PATCH` but provides an explicit audit trail
        for disable actions.
      responses:
        '200':
          description: Agent disabled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Agent'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Agent config record not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /nodes/{nodeId}/agents/{agentId}/status:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    - name: agentId
      in: path
      required: true
      schema:
        type: string
    get:
      tags:
      - Agents
      summary: Get agent runtime status
      description: Returns live health, last heartbeat, error state, and optional
        runtime metrics for the agent. This reflects the actual runtime state reported
        by the node, not the desired config state.
      responses:
        '200':
          description: Agent runtime status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentStatus'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Agent config record not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /nodes/{nodeId}/agents/{agentId}/typed-config:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    - name: agentId
      in: path
      required: true
      schema:
        type: string
    get:
      tags:
      - Agents
      summary: Get typed provider config for an agent
      description: Returns the typed provider configuration overlay for a known agent
        category. The response schema depends on the agent's category and provider
        (e.g., `SpeedtestProviderConfig` for speedtest agents, `IDSConfig` for IDS/IPS
        engines). If the agent uses only generic config, returns a 404 Not Found.
      responses:
        '200':
          description: Typed provider configuration
          content:
            application/json:
              schema:
                type: object
                description: Provider-specific typed configuration
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: No typed config available for this agent
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    put:
      tags:
      - Agents
      summary: Set typed provider config for an agent
      description: Creates or replaces the typed provider configuration overlay for
        a known agent category. The request body schema depends on the agent's category
        and provider. Secret values must go through the agent's generic config or
        the `secrets` field on the agent record — this endpoint manages typed non-secret
        configuration only.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: Provider-specific typed configuration
      responses:
        '200':
          description: Typed provider configuration updated
          content:
            application/json:
              schema:
                type: object
                description: Updated provider-specific typed configuration
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Agent config record not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /nodes/{nodeId}/vpn/service:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    get:
      tags:
      - Agents
      summary: Get third-party VPN service configuration
      description: Returns the VPN overlay service configuration for this node. Supports
        third-party VPN providers such as Tailscale, ZeroTier, and Cloudflare WARP
        running alongside built-in WireGuard/IPSec.
      responses:
        '200':
          description: VPN service configuration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VPNServiceConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Node not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    put:
      tags:
      - Agents
      summary: Update VPN service configuration
      description: Configures a third-party VPN overlay service on this node, including
        provider type, authentication token, subnet routing, exit node role, DNS settings,
        and Magic-DNS hostname.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VPNServiceConfig'
      responses:
        '200':
          description: VPN service configuration updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VPNServiceConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Node not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /nodes/{nodeId}/analytics/agent:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    get:
      tags:
      - Agents
      summary: Get analytics agent configuration
      description: Returns the analytics and monitoring agent configuration for this
        node. Supports agents such as Telegraf (InfluxDB stack) and Prometheus exporters
        for external monitoring systems.
      responses:
        '200':
          description: Analytics agent configuration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AnalyticsAgentConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Node not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    put:
      tags:
      - Agents
      summary: Update analytics agent configuration
      description: Configures the analytics and monitoring agent on this node, including
        provider type, collector endpoint, scrape interval, metrics filters, and authentication
        credentials.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AnalyticsAgentConfig'
      responses:
        '200':
          description: Analytics agent configuration updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AnalyticsAgentConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Node not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /nodes/{nodeId}/remote-access/config:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    get:
      tags:
      - Agents
      summary: Get remote access configuration
      description: Returns the remote access and tunneling configuration for this
        node. Supports providers such as Cloudflare Tunnel, ngrok, and custom tunnel
        services for secure remote access to the node.
      responses:
        '200':
          description: Remote access configuration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RemoteAccessConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Node not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    put:
      tags:
      - Agents
      summary: Update remote access configuration
      description: Configures remote access and tunneling on this node, including
        provider type, authentication credentials, local service binding, public-facing
        URL, TLS enforcement, and ingress access rules.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RemoteAccessConfig'
      responses:
        '200':
          description: Remote access configuration updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RemoteAccessConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Node not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /nodes/{nodeId}/iot-bridge/config:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    get:
      tags:
      - Agents
      summary: Get IoT bridge configuration
      description: Returns the IoT bridge and coordinator configuration for this node.
        Supports bridge providers such as Zigbee2MQTT and Home Assistant for local
        IoT device management.
      responses:
        '200':
          description: IoT bridge configuration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IoTBridgeConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Node not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    put:
      tags:
      - Agents
      summary: Update IoT bridge configuration
      description: Configures the IoT bridge and coordinator on this node, including
        provider type, coordinator serial port, MQTT broker, authentication credentials,
        PAN ID, Zigbee channel, and network encryption key.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/IoTBridgeConfig'
      responses:
        '200':
          description: IoT bridge configuration updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IoTBridgeConfig'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Node not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /devices/{deviceId}/sessions:
    parameters:
    - $ref: '#/components/parameters/DeviceId'
    get:
      tags:
      - Devices
      summary: Get WiFi session history
      description: Returns historical WiFi connection sessions for a client device.
        Each session records connect/disconnect times, SSID, band, radio, and traffic
        bytes. Supports time-range filtering via `from` and `to` parameters.
      parameters:
      - name: from
        in: query
        schema:
          type: string
          format: date-time
      - name: to
        in: query
        schema:
          type: string
          format: date-time
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: per_page
        in: query
        schema:
          type: integer
          default: 50
          maximum: 200
      responses:
        '200':
          description: WiFi session history
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/WifiSession'
                  meta:
                    $ref: '#/components/schemas/Pagination'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Device not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/floorplans:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Locations
      summary: List floor plans
      description: Returns all floor plans for a location. Floor plans are uploaded
        images (PNG/SVG) with optional device placement coordinates for indoor positioning.
      parameters:
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: per_page
        in: query
        schema:
          type: integer
          default: 50
          maximum: 200
      responses:
        '200':
          description: Floor plan list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/FloorPlan'
                  meta:
                    $ref: '#/components/schemas/Pagination'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Location not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      tags:
      - Locations
      summary: Upload floor plan
      description: Uploads a floor plan image and assigns it to a floor number within
        the location. The image is used for indoor device placement visualization.
      parameters:
      - name: locationId
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/FloorPlanCreate'
      responses:
        '201':
          description: Floor plan uploaded
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/FloorPlan'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Location not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/floorplans/{floorplanId}:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Locations
      summary: Get floor plan
      description: Returns floor plan details including the image URL and any device
        placement coordinates.
      parameters:
      - name: floorplanId
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Floor plan details
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: '#/components/schemas/FloorPlan'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Floor plan or location not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    delete:
      tags:
      - Locations
      summary: Delete floor plan
      description: Removes a floor plan and its image. Device placements on this floor
        plan are also removed.
      parameters:
      - name: locationId
        in: path
        required: true
        schema:
          type: string
      - name: floorplanId
        in: path
        required: true
        schema:
          type: string
      responses:
        '204':
          description: Floor plan deleted
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Floor plan or location not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /devices/{deviceId}/identities:
    parameters:
    - $ref: '#/components/parameters/DeviceId'
    get:
      tags:
      - Devices
      summary: List merged MAC identities for a device
      description: Returns all client identity records for this device, showing MAC
        addresses that have been associated with the same physical device through
        deduplication signals (DHCP fingerprint, hostname, traffic patterns, OS fingerprint,
        or manual merging). Each identity lists merged MACs, the confidence score,
        and the evidence signals used.
      parameters:
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: per_page
        in: query
        schema:
          type: integer
          default: 50
          maximum: 200
      responses:
        '200':
          description: Paginated list of client identities
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/PaginatedResponse'
                - type: object
                  properties:
                    items:
                      type: array
                      items:
                        $ref: '#/components/schemas/ClientIdentity'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Device not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /devices/{deviceId}/identities/{identityId}:
    parameters:
    - $ref: '#/components/parameters/DeviceId'
    - name: identityId
      in: path
      required: true
      schema:
        type: string
    get:
      tags:
      - Devices
      summary: Get a client identity record
      description: Returns a single client identity record including all merged MACs,
        confidence score, and deduplication signals.
      responses:
        '200':
          description: Client identity record
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClientIdentity'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Identity record not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    delete:
      tags:
      - Devices
      summary: Delete a client identity record
      description: Removes an identity merge. All previously merged MACs are treated
        as independent devices again. Use with caution — this may fragment device
        history and analytics.
      responses:
        '204':
          description: Identity record deleted
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Identity record not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /devices/{deviceId}/identities/merge:
    parameters:
    - $ref: '#/components/parameters/DeviceId'
    post:
      tags:
      - Devices
      summary: Manually merge two MACs into one identity
      description: Manually associates two MAC addresses as belonging to the same
        physical device. This is useful when automated deduplication fails or when
        an operator knows two MACs represent the same client (e.g. a phone using both
        private and hardware MAC addresses). Creates a new identity record with `manualOverride
        = true`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - primaryMac
              - secondaryMac
              properties:
                primaryMac:
                  type: string
                  description: Canonical MAC to use for the merged identity
                secondaryMac:
                  type: string
                  description: Additional MAC to merge into the identity
      responses:
        '201':
          description: Identity merge created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClientIdentity'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Device not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /devices/{deviceId}/quality:
    parameters:
    - $ref: '#/components/parameters/DeviceId'
    get:
      tags:
      - Devices
      summary: Get client connection quality
      description: Returns the current connection quality assessment for this device.
        Scores are composite 0-100 values across signal quality (RSSI/SNR/MCS/PHY
        rate), link quality (retransmission/packet errors), air quality (channel utilization/interference),
        and application readiness (TCP/DNS/HTTP/TLS latency).
      responses:
        '200':
          description: Client connection quality assessment
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClientConnectionQuality'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Device not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /devices/{deviceId}/quality/history:
    parameters:
    - $ref: '#/components/parameters/DeviceId'
    get:
      tags:
      - Devices
      summary: Get client connection quality history
      description: Returns a time series of connection quality assessments for this
        device, useful for tracking quality trends and identifying degradation patterns
        over time.
      parameters:
      - name: from
        in: query
        schema:
          type: string
          format: date-time
      - name: to
        in: query
        schema:
          type: string
          format: date-time
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: per_page
        in: query
        schema:
          type: integer
          default: 50
          maximum: 200
      responses:
        '200':
          description: Client connection quality history
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/PaginatedResponse'
                - type: object
                  properties:
                    items:
                      type: array
                      items:
                        $ref: '#/components/schemas/ClientConnectionQuality'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Device not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/analytics/client-quality:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Telemetry
      summary: Get aggregate client quality across location
      description: Returns aggregated connection quality scores across all connected
        clients at the location, including quality distribution, worst-performing
        clients, and overall quality trend.
      responses:
        '200':
          description: Aggregated client quality summary
          content:
            application/json:
              schema:
                type: object
                properties:
                  averageScore:
                    type: integer
                    minimum: 0
                    maximum: 100
                  worstClients:
                    type: array
                    items:
                      type: object
                      properties:
                        deviceId:
                          type: string
                        mac:
                          type: string
                        score:
                          type: integer
                        issue:
                          type: string
                  scoreDistribution:
                    type: object
                    properties:
                      excellent:
                        type: integer
                        description: Count of clients scoring 80-100
                      good:
                        type: integer
                        description: Count of clients scoring 60-79
                      fair:
                        type: integer
                        description: Count of clients scoring 40-59
                      poor:
                        type: integer
                        description: Count of clients scoring 0-39
                  trend:
                    type: string
                    enum:
                    - improving
                    - stable
                    - degrading
                  timestamp:
                    type: string
                    format: date-time
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/analytics/qoe:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Telemetry
      summary: Get Quality of Outcome (QoO) score
      description: Returns the current QoO score for the location. The QoO score (0-100)
        measures how closely network conditions meet application- specific quality
        requirements defined in `/admin/qoe/application-classes`. Based on BBF TR-452
        (Quality Attenuation) and IETF draft-ietf-ippm-qoo. Scores are broken down
        by application class (streaming, gaming, voice, etc.) and by network segment
        (access, WiFi, WAN, ISP backbone, CDN edge).
      responses:
        '200':
          description: QoO score and breakdown
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QoOScore'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/analytics/qoe/history:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Telemetry
      summary: Get QoO score history
      description: Returns a time series of QoO scores for trend analysis and SLA
        verification. Supports optional `from`/`to` time range filtering.
      parameters:
      - name: from
        in: query
        schema:
          type: string
          format: date-time
      - name: to
        in: query
        schema:
          type: string
          format: date-time
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: per_page
        in: query
        schema:
          type: integer
          default: 50
          maximum: 200
      responses:
        '200':
          description: QoO score history
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/PaginatedResponse'
                - type: object
                  properties:
                    items:
                      type: array
                      items:
                        $ref: '#/components/schemas/QoOScore'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /admin/qoe/application-classes:
    get:
      tags:
      - Telemetry
      summary: List QoO application class definitions
      description: Returns all defined application classes with their quality requirements
        (throughput, latency, packet loss, jitter thresholds per service tier profile).
      parameters:
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: per_page
        in: query
        schema:
          type: integer
          default: 50
          maximum: 200
      responses:
        '200':
          description: List of application class definitions
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/PaginatedResponse'
                - type: object
                  properties:
                    items:
                      type: array
                      items:
                        $ref: '#/components/schemas/QoOApplicationClass'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      tags:
      - Telemetry
      summary: Define a new QoO application class
      description: Creates a new application class definition with quality requirements.
        These definitions are used by the QoO scoring engine to evaluate whether network
        conditions meet application needs.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/QoOApplicationClass'
      responses:
        '201':
          description: Application class created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QoOApplicationClass'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /admin/qoe/application-classes/{classId}:
    parameters:
    - name: classId
      in: path
      required: true
      schema:
        type: string
    get:
      tags:
      - Telemetry
      summary: Get a QoO application class definition
      description: Returns details for a single QoE application class.
      responses:
        '200':
          description: Application class definition
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QoOApplicationClass'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Application class not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    put:
      tags:
      - Telemetry
      summary: Update a QoO application class definition
      description: Updates a QoE application class, modifying classification rules or bandwidth limits.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/QoOApplicationClass'
      responses:
        '200':
          description: Application class updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QoOApplicationClass'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Application class not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    delete:
      tags:
      - Telemetry
      summary: Delete a QoO application class definition
      description: Deletes a QoE application class and removes its QoS rules from all locations.
      responses:
        '204':
          description: Application class deleted
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Application class not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /nodes/{nodeId}/quality-attenuation:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    get:
      tags:
      - Telemetry
      summary: Get quality attenuation measurement
      description: Returns the latest Quality Attenuation (Delta-Q) measurement for
        this node, based on BBF TR-452. Quality Attenuation captures network quality
        as a latency distribution plus packet loss at a given load, enabling composable
        per-segment analysis and fault isolation.
      responses:
        '200':
          description: Quality attenuation measurement
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QualityAttenuationReport'
        '400':
          description: Bad request — validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized — invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Node not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited — too many requests
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/zwave:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Networks
      summary: Get Z-Wave controller config
      description: Returns Z-Wave 800 controller configuration and status.
      responses:
        '200':
          description: Z-Wave config
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ZWaveConfig'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    put:
      tags:
      - Networks
      summary: Configure Z-Wave controller
      description: Configures the Z-Wave 800 controller including network key, regional
        frequency, and security classes.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ZWaveConfig'
      responses:
        '200':
          description: Z-Wave config updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ZWaveConfig'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/zwave/devices:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Networks
      summary: List Z-Wave devices
      description: Returns the list of paired Z-Wave devices on the mesh network.
      responses:
        '200':
          description: Z-Wave device list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/ZWaveDevice'
                  meta:
                    $ref: '#/components/schemas/Pagination'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/tor:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Security
      summary: Get Tor client config
      description: Returns Tor client configuration including SOCKS proxy, transparent
        proxy, and kill-switch settings.
      responses:
        '200':
          description: Tor config
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TorConfig'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    put:
      tags:
      - Security
      summary: Configure Tor client
      description: Configures the Tor client on the router for anonymous network access.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TorConfig'
      responses:
        '200':
          description: Tor config updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TorConfig'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /nodes/{nodeId}/attestation:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    get:
      tags:
      - Security
      summary: Get hardware attestation report
      description: Returns the TPM 2.0 remote attestation report with measured boot
        PCR values. Used for supply chain integrity verification and compliance auditing.
      responses:
        '200':
          description: Attestation report
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AttestationReport'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /reference/advisories:
    get:
      tags:
      - Partners
      summary: List security advisories
      description: Returns firmware security advisories (CVEs and vendor disclosures).
        Can be filtered by firmware version to show only relevant advisories.
      parameters:
      - name: firmwareVersion
        in: query
        schema:
          type: string
        description: Filter advisories affecting this firmware version.
      - name: severity
        in: query
        schema:
          type: string
          enum:
          - CRITICAL
          - HIGH
          - MEDIUM
          - LOW
          - INFORMATIONAL
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: per_page
        in: query
        schema:
          type: integer
          default: 50
          maximum: 200
      responses:
        '200':
          description: Security advisories
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/SecurityAdvisory'
                  meta:
                    $ref: '#/components/schemas/Pagination'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /reference/sbom:
    get:
      tags:
      - Partners
      summary: Get firmware SBOM
      description: Returns the Software Bill of Materials (SBOM) for a firmware version
        in SPDX or CycloneDX format. Required for EU Cyber Resilience Act (CRA) compliance.
      parameters:
      - name: version
        in: query
        required: true
        schema:
          type: string
        description: Firmware version to retrieve SBOM for.
      - name: format
        in: query
        schema:
          type: string
          enum:
          - spdx
          - cyclonedx
          default: spdx
        description: SBOM format.
      responses:
        '200':
          description: SBOM document
          content:
            application/json:
              schema:
                type: object
                description: SBOM document in the requested format (SPDX JSON or CycloneDX
                  JSON).
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /nodes/{nodeId}/lifecycle:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    put:
      tags:
      - Nodes
      summary: Update node lifecycle state
      description: Transitions a node's lifecycle state (active, end-of-life, decommissioned,
        RMA). EOL nodes stop receiving firmware updates. Decommissioned nodes are
        archived.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - lifecycleState
              properties:
                lifecycleState:
                  type: string
                  enum:
                  - eol
                  - decommissioned
                  - rma
                reason:
                  type: string
                  description: Reason for the lifecycle transition.
      responses:
        '200':
          description: Lifecycle state updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Node'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /inventory/devices/{serialNumber}/lifecycle:
    parameters:
    - name: serialNumber
      in: path
      required: true
      schema:
        type: string
    put:
      tags:
      - Partners
      summary: Update inventory device lifecycle state
      description: Transitions an inventory device's lifecycle state. Used for managing
        hardware end-of-life and RMA workflows.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - lifecycleState
              properties:
                lifecycleState:
                  type: string
                  enum:
                  - eol
                  - decommissioned
                  - rma
                reason:
                  type: string
      responses:
        '200':
          description: Lifecycle state updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InventoryDevice'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /nodes/{nodeId}/tamper-events:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    get:
      tags:
      - Security
      summary: List tamper events
      description: Returns chassis intrusion and physical tamper detection events
        for this node.
      parameters:
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: per_page
        in: query
        schema:
          type: integer
          default: 50
          maximum: 200
      responses:
        '200':
          description: Tamper events
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/TamperEvent'
                  meta:
                    $ref: '#/components/schemas/Pagination'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      tags:
      - Security
      summary: Acknowledge tamper events
      description: Acknowledge one or more tamper events by ID.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - eventIds
              properties:
                eventIds:
                  type: array
                  items:
                    type: string
      responses:
        '200':
          description: Events acknowledged
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/usp:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Connectivity
      summary: Get USP agent config and status
      description: Returns the TR-369 USP agent configuration and operational status.
        The USP agent is an agent like any other — the REST API manages its config
        and enable/disable state. USP protocol operations (Get, Set, Operate, Notify)
        stay inside the agent.
      responses:
        '200':
          description: USP agent config and status
          content:
            application/json:
              schema:
                type: object
                properties:
                  config:
                    $ref: '#/components/schemas/USPAgentConfig'
                  status:
                    $ref: '#/components/schemas/USPAgentStatus'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    put:
      tags:
      - Connectivity
      summary: Configure USP agent
      description: Configures the TR-369 USP agent (controller URL, credentials, enable/disable).
        Follows the same agent config pattern as all other agents. USP protocol operations
        and TR-181 data model exposure stay inside the agent.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/USPAgentConfig'
      responses:
        '200':
          description: USP agent config updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/USPAgentConfig'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /nodes/{nodeId}/containers:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    get:
      tags:
      - Nodes
      summary: List running containers
      description: Returns operational status of containers running on this node.
        Container lifecycle (install/start/stop/remove) is managed by the firmware,
        not the REST API. This endpoint provides read-only visibility.
      responses:
        '200':
          description: Container list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/ContainerStatus'
                  meta:
                    $ref: '#/components/schemas/Pagination'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/home-assistant:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Networks
      summary: Get Home Assistant config
      description: Returns Home Assistant integration configuration including pairing
        state and entity discovery settings.
      responses:
        '200':
          description: Home Assistant config
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HomeAssistantConfig'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    put:
      tags:
      - Networks
      summary: Configure Home Assistant integration
      description: Configures Home Assistant integration on the router. Enables MQTT
        auto-discovery and entity exposure to a Home Assistant instance.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HomeAssistantConfig'
      responses:
        '200':
          description: HA config updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HomeAssistantConfig'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /nodes/{nodeId}/poe/ports:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    get:
      tags:
      - Nodes
      summary: List PoE port status
      description: Returns per-port PoE status including power draw, PD class, and
        fault state.
      responses:
        '200':
          description: PoE port list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/PoEPortConfig'
                  meta:
                    $ref: '#/components/schemas/Pagination'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /nodes/{nodeId}/poe/ports/{portId}:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    - name: portId
      in: path
      required: true
      schema:
        type: integer
    put:
      tags:
      - Nodes
      summary: Configure PoE port
      description: 'Configures per-port PoE settings: enable/disable, priority, and
        power limit.'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PoEPortConfig'
      responses:
        '200':
          description: PoE port updated
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      tags:
      - Nodes
      summary: Power-cycle PoE port
      description: Power-cycles a PoE port (off, wait, on). Used to reset a hung powered
        device.
      responses:
        '200':
          description: Port power-cycled
        '202':
          description: Power cycle initiated
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /nodes/{nodeId}/fan:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    get:
      tags:
      - Nodes
      summary: Get fan status
      description: Returns fan speed, mode, and thermal threshold configuration.
      responses:
        '200':
          description: Fan config
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FanConfig'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    put:
      tags:
      - Nodes
      summary: Configure fan
      description: Configures fan mode (auto/manual/off), manual duty cycle, and temperature
        thresholds.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FanConfig'
      responses:
        '200':
          description: Fan config updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FanConfig'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/carbon:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Nodes
      summary: Get carbon-aware config
      description: Returns carbon-aware workload scheduling configuration and current
        carbon intensity data.
      responses:
        '200':
          description: Carbon config
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CarbonConfig'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    put:
      tags:
      - Nodes
      summary: Configure carbon-aware scheduling
      description: 'Configures carbon-aware computing: region, carbon threshold, and
        deferrable workload selection.'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CarbonConfig'
      responses:
        '200':
          description: Carbon config updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CarbonConfig'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /nodes/{nodeId}/energy:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    get:
      tags:
      - Telemetry
      summary: Get energy consumption report
      description: Returns energy consumption and efficiency metrics for EU Ecodesign
        compliance reporting.
      parameters:
      - name: period
        in: query
        schema:
          type: string
          enum:
          - 15min
          - hour
          - day
          - week
          - month
          default: day
      responses:
        '200':
          description: Energy report
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EnergyReport'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /inventory/devices/{serialNumber}/dpp:
    parameters:
    - name: serialNumber
      in: path
      required: true
      schema:
        type: string
    get:
      tags:
      - Partners
      summary: Get Digital Product Passport
      description: Returns the EU ESPR Digital Product Passport for this device, including
        materials composition, carbon footprint, repairability score, and spare part
        availability.
      responses:
        '200':
          description: Digital Product Passport
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DigitalProductPassport'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /nodes/{nodeId}/watchdog:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    get:
      tags:
      - Nodes
      summary: Get watchdog config
      description: Returns system watchdog configuration including per-service escalation
        levels and restart counters.
      responses:
        '200':
          description: Watchdog config
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WatchdogConfig'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    put:
      tags:
      - Nodes
      summary: Configure watchdog
      description: 'Configures system watchdog: hardware/software watchdog toggle,
        timeout, and per-service restart/escalation policy.'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WatchdogConfig'
      responses:
        '200':
          description: Watchdog config updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WatchdogConfig'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /nodes/{nodeId}/boot-integrity:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    get:
      tags:
      - Security
      summary: Get boot integrity report
      description: Returns the 10-stage boot-time integrity self-check report from
        the last boot. Includes per-stage pass/fail status, TPM PCR values, and firmware
        signing key fingerprint.
      responses:
        '200':
          description: Boot integrity report
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BootIntegrityReport'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /nodes/{nodeId}/firmware/slots:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    get:
      tags:
      - Nodes
      summary: Get firmware slot status
      description: Returns A/B firmware slot health, active/next boot slot, U-Boot
        environment integrity, and recovery partition availability.
      responses:
        '200':
          description: Firmware slot status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FirmwareSlotStatus'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/telemetry/report-profiles:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Telemetry
      summary: List telemetry report profiles
      description: Returns configured telemetry report profiles with collection intervals,
        conditions, encoding, and destination configuration.
      responses:
        '200':
          description: Report profiles
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/TelemetryReportProfile'
                  meta:
                    $ref: '#/components/schemas/Pagination'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      tags:
      - Telemetry
      summary: Create telemetry report profile
      description: Creates a new telemetry report profile defining what metrics to
        collect, how often, under what conditions, and where to send them.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TelemetryReportProfileCreate'
      responses:
        '201':
          description: Profile created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TelemetryReportProfile'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/telemetry/report-profiles/{profileId}:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    - name: profileId
      in: path
      required: true
      schema:
        type: string
    get:
      tags:
      - Telemetry
      summary: Get telemetry report profile
      description: Returns a single telemetry report profile.
      responses:
        '200':
          description: Report profile
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TelemetryReportProfile'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    patch:
      tags:
      - Telemetry
      summary: Update telemetry report profile
      description: Updates a telemetry report profile configuration.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TelemetryReportProfileCreate'
      responses:
        '200':
          description: Profile updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TelemetryReportProfile'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    delete:
      tags:
      - Telemetry
      summary: Delete telemetry report profile
      description: Deletes a telemetry report profile.
      responses:
        '204':
          description: Profile deleted
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/config/snapshots:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Locations
      summary: List config snapshots
      description: Returns the list of configuration snapshots for this location.
      parameters:
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: per_page
        in: query
        schema:
          type: integer
          default: 50
          maximum: 200
      responses:
        '200':
          description: Config snapshots
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/ConfigSnapshot'
                  meta:
                    $ref: '#/components/schemas/Pagination'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      tags:
      - Locations
      summary: Create config snapshot
      description: Creates a point-in-time snapshot of the current configuration.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - name
              properties:
                name:
                  type: string
                tags:
                  type: array
                  items:
                    type: string
                description:
                  type: string
      responses:
        '201':
          description: Snapshot created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConfigSnapshot'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/config/snapshots/{snapshotId}/rollback:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    - name: snapshotId
      in: path
      required: true
      schema:
        type: string
    post:
      tags:
      - Locations
      summary: Rollback to config snapshot
      description: Restores the location configuration to the state captured in this
        snapshot.
      responses:
        '200':
          description: Rollback initiated
        '202':
          description: Rollback queued
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/scheduled-changes:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Locations
      summary: List scheduled config changes
      description: Returns pending and completed scheduled configuration changes for
        this location.
      parameters:
      - name: status
        in: query
        schema:
          type: string
          enum:
          - pending
          - executing
          - completed
          - failed
          - cancelled
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: per_page
        in: query
        schema:
          type: integer
          default: 50
          maximum: 200
      responses:
        '200':
          description: Scheduled changes
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/ScheduledChange'
                  meta:
                    $ref: '#/components/schemas/Pagination'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      tags:
      - Locations
      summary: Schedule a config change
      description: Schedules a configuration change to execute at a future time. Supports
        one-shot and recurring schedules.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - name
              - operation
              - scheduledAt
              properties:
                name:
                  type: string
                description:
                  type: string
                operation:
                  type: string
                  enum:
                  - firmware_upgrade
                  - ssid_update
                  - radio_config
                  - wifi_schedule_toggle
                  - wan_config
                  - security_policy
                  - reboot
                targetResource:
                  type: string
                desiredState:
                  type: object
                scheduledAt:
                  type: string
                  format: date-time
                recurring: &id003
                  type: object
                  properties:
                    cronExpression:
                      type: string
                      example: 0 3 * * *
                    timezone:
                      type: string
                      default: UTC
      responses:
        '201':
          description: Change scheduled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ScheduledChange'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/scheduled-changes/{scheduleId}:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    - name: scheduleId
      in: path
      required: true
      schema:
        type: string
    get:
      tags:
      - Locations
      summary: Get scheduled change detail
      description: Returns details of a specific scheduled configuration change.
      responses:
        '200':
          description: Scheduled change
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ScheduledChange'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    delete:
      tags:
      - Locations
      summary: Cancel scheduled change
      description: Cancels a pending scheduled configuration change.
      responses:
        '204':
          description: Scheduled change cancelled
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/voice-assistant:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Networks
      summary: Get voice assistant config
      description: Returns voice assistant integration status for Alexa, Google Home,
        and Apple HomeKit.
      responses:
        '200':
          description: Voice assistant config
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VoiceAssistantConfig'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    put:
      tags:
      - Networks
      summary: Configure voice assistant integration
      description: Enables/disables voice assistant platforms and configures OAuth-based
        account linking parameters.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VoiceAssistantConfig'
      responses:
        '200':
          description: Config updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VoiceAssistantConfig'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/automations:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Networks
      summary: List automation rules
      description: Returns local automation rules with trigger-condition-action semantics.
      parameters:
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: per_page
        in: query
        schema:
          type: integer
          default: 50
          maximum: 200
      responses:
        '200':
          description: Automation rules
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/AutomationRule'
                  meta:
                    $ref: '#/components/schemas/Pagination'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      tags:
      - Networks
      summary: Create automation rule
      description: Creates a local automation rule. Rules execute on the router without
        cloud dependency.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AutomationRule'
      responses:
        '201':
          description: Rule created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AutomationRule'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/automations/{ruleId}:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    - name: ruleId
      in: path
      required: true
      schema:
        type: string
    get:
      tags:
      - Networks
      summary: Get automation rule
      description: Returns a single automation rule.
      responses:
        '200':
          description: Automation rule
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AutomationRule'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    patch:
      tags:
      - Networks
      summary: Update automation rule
      description: Updates an automation rule.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AutomationRule'
      responses:
        '200':
          description: Rule updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AutomationRule'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    delete:
      tags:
      - Networks
      summary: Delete automation rule
      description: Deletes a local automation rule and stops its execution.
      responses:
        '204':
          description: Rule deleted
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/cameras:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Networks
      summary: List IP cameras
      description: Returns configured IP cameras with RTSP/ONVIF/HomeKit Secure Video
        integration status.
      parameters:
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: per_page
        in: query
        schema:
          type: integer
          default: 50
          maximum: 200
      responses:
        '200':
          description: Camera list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/CameraConfig'
                  meta:
                    $ref: '#/components/schemas/Pagination'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      tags:
      - Networks
      summary: Add IP camera
      description: Adds an IP camera for RTSP streaming, ONVIF PTZ control, and optional
        HomeKit Secure Video.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CameraConfig'
      responses:
        '201':
          description: Camera added
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CameraConfig'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/cameras/{cameraId}:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    - name: cameraId
      in: path
      required: true
      schema:
        type: string
    patch:
      tags:
      - Networks
      summary: Update camera config
      description: Updates IP camera configuration.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CameraConfig'
      responses:
        '200':
          description: Camera updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CameraConfig'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    delete:
      tags:
      - Networks
      summary: Remove camera
      description: Deletes a camera configuration from the router camera integration.
      responses:
        '204':
          description: Camera removed
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/paid-access:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Security
      summary: Get paid WiFi config
      description: Returns paid WiFi access configuration including pricing tiers
        and revenue metrics.
      responses:
        '200':
          description: Paid access config
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaidAccessConfig'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    put:
      tags:
      - Security
      summary: Configure paid WiFi access
      description: Configures paid WiFi monetization with pricing tiers and payment
        gateway integration.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PaidAccessConfig'
      responses:
        '200':
          description: Config updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaidAccessConfig'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/paid-access/sessions:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Security
      summary: List paid WiFi sessions
      description: Returns active and historical paid WiFi access sessions.
      parameters:
      - name: status
        in: query
        schema:
          type: string
          enum:
          - active
          - expired
          - cancelled
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: per_page
        in: query
        schema:
          type: integer
          default: 50
          maximum: 200
      responses:
        '200':
          description: Paid sessions
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/PaidSession'
                  meta:
                    $ref: '#/components/schemas/Pagination'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/motion:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Presence
      summary: Get WiFi motion config
      description: Returns WiFi CSI-based motion detection configuration including
        per-zone sensitivity and occupancy.
      responses:
        '200':
          description: Motion config
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MotionZoneConfig'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    put:
      tags:
      - Presence
      summary: Configure WiFi motion detection
      description: Configures WiFi CSI-based motion detection with per-zone sensitivity
        settings.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MotionZoneConfig'
      responses:
        '200':
          description: Config updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MotionZoneConfig'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/motion/events:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Presence
      summary: List motion events
      description: Returns recent WiFi motion detection events.
      parameters:
      - name: zoneId
        in: query
        schema:
          type: string
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: per_page
        in: query
        schema:
          type: integer
          default: 50
          maximum: 200
      responses:
        '200':
          description: Motion events
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/MotionEvent'
                  meta:
                    $ref: '#/components/schemas/Pagination'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /customers/{customerId}/locations/{locationId}/network/airtime-fairness:
    parameters:
    - $ref: '#/components/parameters/CustomerId'
    - $ref: '#/components/parameters/LocationId'
    get:
      tags:
      - Networks
      summary: Get airtime fairness config
      description: Returns airtime fairness configuration controlling per-client airtime
        allocation.
      responses:
        '200':
          description: Airtime fairness config
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AirtimeFairnessConfig'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    put:
      tags:
      - Networks
      summary: Configure airtime fairness
      description: Configures airtime fairness to prevent slow clients from dominating
        channel time.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AirtimeFairnessConfig'
      responses:
        '200':
          description: Config updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AirtimeFairnessConfig'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /nodes/{nodeId}/radios/{radioId}/phy-diag:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    - $ref: '#/components/parameters/RadioId'
    get:
      tags:
      - Radios
      summary: Get PHY diagnostics
      description: 'Returns chipset-level PHY diagnostics: per-chain RSSI, MCS distribution,
        retry counts, noise floor, and channel utilization.'
      parameters:
      - name: samplePeriodSec
        in: query
        schema:
          type: integer
          default: 60
      responses:
        '200':
          description: PHY diagnostics
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PHYDiagnostics'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /nodes/{nodeId}/flow-export:
    parameters:
    - $ref: '#/components/parameters/NodeId'
    get:
      tags:
      - Telemetry
      summary: Get flow export config
      description: Returns NetFlow/IPFIX flow export configuration for traffic analysis
        integration with external collectors.
      responses:
        '200':
          description: Flow export config
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FlowExportConfig'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    put:
      tags:
      - Telemetry
      summary: Configure flow export
      description: Configures NetFlow v5/v9 or IPFIX flow data export to an external
        collector for security monitoring and traffic analysis.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FlowExportConfig'
      responses:
        '200':
          description: Config updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FlowExportConfig'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /admin/ipam/pools:
    get:
      tags:
      - Partners
      summary: List IPAM subnet pools
      description: Returns IP address management pools for subnet planning and allocation
        across locations.
      parameters:
      - name: page
        in: query
        schema:
          type: integer
          default: 1
      - name: per_page
        in: query
        schema:
          type: integer
          default: 50
          maximum: 200
      responses:
        '200':
          description: Subnet pools
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/SubnetPool'
                  meta:
                    $ref: '#/components/schemas/Pagination'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      tags:
      - Partners
      summary: Create IPAM subnet pool
      description: Creates an IP address management pool for subnet planning.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - name
              - networkCidr
              properties:
                name:
                  type: string
                networkCidr:
                  type: string
                subnetPrefixLen:
                  type: integer
                  default: 24
                gateway:
                  type: string
                dnsServers:
                  type: array
                  items:
                    type: string
      responses:
        '201':
          description: Pool created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubnetPool'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /admin/ipam/pools/{poolId}:
    parameters:
    - name: poolId
      in: path
      required: true
      schema:
        type: string
    get:
      tags:
      - Partners
      summary: Get IPAM subnet pool
      description: Returns details of an IPAM subnet pool including allocations.
      responses:
        '200':
          description: Subnet pool
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubnetPool'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    delete:
      tags:
      - Partners
      summary: Delete IPAM subnet pool
      description: Deletes an IPAM subnet pool. Cannot delete if subnets are allocated.
      responses:
        '204':
          description: Pool deleted
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /admin/sso:
    get:
      tags:
      - Partners
      summary: Get SSO config
      description: Returns SAML/OIDC single sign-on configuration for operator authentication.
      responses:
        '200':
          description: SSO config
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SSOConfig'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    put:
      tags:
      - Partners
      summary: Configure SSO
      description: Configures SAML or OIDC single sign-on for operator authentication
        with Just-in-Time provisioning and role mapping from IdP groups.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SSOConfig'
      responses:
        '200':
          description: SSO config updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SSOConfig'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '429':
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
components:
  securitySchemes:
    oauth2ClientCredentials:
      type: oauth2
      flows:
        clientCredentials:
          tokenUrl: /oauth/token
          scopes:
            customers:read: Read customer data
            customers:write: Manage customers
            locations:read: Read location data
            locations:write: Manage locations
            nodes:read: Read node/device state
            nodes:write: Manage node configuration and actions
            security:read: Read security/parental-control policies
            security:write: Manage security/parental-control policies
            webhooks:write: Manage webhook subscriptions
  parameters:
    PartnerId:
      name: partnerId
      in: path
      required: true
      description: Partner identifier (maps to `PLUME_PARTNER_ID` in the real Plume
        Cloud API)
      schema:
        type: string
    GroupId:
      name: groupId
      in: path
      required: true
      description: Group identifier (maps to `PLUME_GROUP_ID` in the real Plume Cloud
        API)
      schema:
        type: string
    CustomerId:
      name: customerId
      in: path
      required: true
      description: Customer identifier
      schema:
        type: string
    LocationId:
      name: locationId
      in: path
      required: true
      description: Location identifier
      schema:
        type: string
    NodeId:
      name: nodeId
      in: path
      required: true
      description: Node identifier (maps to `AWLAN_Node._uuid` / serial number)
      schema:
        type: string
    RadioId:
      name: radioId
      in: path
      required: true
      description: Radio identifier (maps to `Wifi_Radio_Config._uuid`)
      schema:
        type: string
    NetworkId:
      name: networkId
      in: path
      required: true
      description: Logical network (SSID) identifier
      schema:
        type: string
    VifId:
      name: vifId
      in: path
      required: true
      description: VIF identifier (maps to `Wifi_VIF_Config._uuid`)
      schema:
        type: string
    DeviceId:
      name: deviceId
      in: path
      required: true
      description: Connected client device identifier (typically derived from MAC
        address)
      schema:
        type: string
    PersonId:
      name: personId
      in: path
      required: true
      schema:
        type: string
    OptimizationId:
      name: optimizationId
      in: path
      required: true
      description: Mesh topology optimization job identifier
      schema:
        type: string
    PageParam:
      name: page
      in: query
      description: 1-indexed page number
      schema:
        type: integer
        minimum: 1
        default: 1
    PageSizeParam:
      name: pageSize
      in: query
      schema:
        type: integer
        minimum: 1
        maximum: 200
        default: 50
    DeploymentId:
      name: deploymentId
      in: path
      required: true
      description: Deployment identifier
      schema:
        type: string
    SerialNumber:
      name: serialNumber
      in: path
      required: true
      description: Inventory device serial number
      schema:
        type: string
    MemberId:
      name: memberId
      in: path
      required: true
      description: Deployment member (node) identifier
      schema:
        type: string
    PskId:
      name: pskId
      in: path
      required: true
      description: Per-client PSK entry identifier
      schema:
        type: string
    CommandId:
      name: commandId
      in: path
      required: true
      description: Node command identifier (asynchronous command tracking)
      schema:
        type: string
    FloorPlanId:
      name: floorplanId
      in: path
      required: true
      description: Floor plan identifier
      schema:
        type: string
  responses:
    Accepted:
      description: Request accepted for asynchronous processing
      content:
        application/json:
          schema:
            type: object
            properties:
              jobId:
                type: string
              status:
                type: string
                enum:
                - pending
    BadRequest:
      description: Malformed request
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    Unauthorized:
      description: Missing or invalid credentials
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    TooManyRequests:
      description: Rate limited — too many requests
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    NotFound:
      description: Resource not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    Conflict:
      description: Resource already exists or conflicting state
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    UnprocessableEntity:
      description: Semantically invalid request (e.g. unsupported channel for regulatory
        domain)
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
  schemas:
    Error:
      type: object
      required:
      - code
      - message
      properties:
        code:
          type: string
          example: NODE_NOT_FOUND
        message:
          type: string
        details:
          type: object
          additionalProperties: true
    PaginatedResponse:
      type: object
      properties:
        page:
          type: integer
        pageSize:
          type: integer
        totalItems:
          type: integer
        totalPages:
          type: integer
    TokenResponse:
      type: object
      properties:
        access_token:
          type: string
        token_type:
          type: string
          example: Bearer
        expires_in:
          type: integer
          example: 3600
        scope:
          type: string
    GeoAddress:
      type: object
      properties:
        line1:
          type: string
        line2:
          type: string
        city:
          type: string
        state:
          type: string
        postalCode:
          type: string
        country:
          type: string
        latitude:
          type: number
          format: double
        longitude:
          type: number
          format: double
    Coordinates:
      type: object
      description: GeoJSON Point coordinates. Flat representation with `[longitude,
        latitude]` per GeoJSON spec. Used for device self-reported location and outdoor
        positioning.
      properties:
        type:
          type: string
          enum:
          - Point
          default: Point
        coordinates:
          type: array
          minItems: 2
          maxItems: 2
          items:
            type: number
            format: double
          description: '[longitude, latitude] array per GeoJSON spec'
      required:
      - coordinates
    Band:
      type: string
      enum:
      - 2.4G
      - 5G
      - 5GL
      - 5GU
      - 6G
    Partner:
      type: object
      description: 'Top-level tenant. In the real Plume Cloud API this corresponds
        to the assigned

        `PLUME_PARTNER_ID` scope of a partner/ISP integration.

        '
      properties:
        id:
          type: string
          readOnly: true
        name:
          type: string
        status:
          type: string
          enum:
          - active
          - suspended
        createdAt:
          type: string
          format: date-time
          readOnly: true
    Group:
      type: object
      description: 'Sub-organization within a Partner (e.g. a reseller or regional
        business unit).

        Corresponds to `PLUME_GROUP_ID` in the real Plume Cloud API.

        '
      properties:
        id:
          type: string
          readOnly: true
        partnerId:
          type: string
          readOnly: true
        name:
          type: string
        createdAt:
          type: string
          format: date-time
          readOnly: true
    GroupCreate:
      type: object
      required:
      - name
      properties:
        name:
          type: string
    GroupUpdate:
      type: object
      properties:
        name:
          type: string
    Customer:
      type: object
      properties:
        id:
          type: string
          readOnly: true
        groupId:
          type: string
          readOnly: true
        externalId:
          type: string
          description: Partner/CSP-supplied subscriber ID
        name:
          type: string
        email:
          type: string
          format: email
        status:
          type: string
          enum:
          - active
          - suspended
          - closed
        createdAt:
          type: string
          format: date-time
          readOnly: true
        updatedAt:
          type: string
          format: date-time
          readOnly: true
    CustomerCreate:
      type: object
      required:
      - externalId
      - name
      - email
      properties:
        externalId:
          type: string
        name:
          type: string
        email:
          type: string
          format: email
    CustomerUpdate:
      type: object
      properties:
        name:
          type: string
        email:
          type: string
          format: email
        status:
          type: string
          enum:
          - active
          - suspended
          - closed
    Location:
      type: object
      properties:
        id:
          type: string
          readOnly: true
        customerId:
          type: string
          readOnly: true
        name:
          type: string
        timezone:
          type: string
          example: America/Los_Angeles
        address:
          $ref: '#/components/schemas/GeoAddress'
        regulatoryDomain:
          type: string
          description: ISO 3166-1 alpha-2 country code used for channel/power regulatory
            limits
          example: US
        createdAt:
          type: string
          format: date-time
          readOnly: true
    LocationCreate:
      type: object
      required:
      - name
      - regulatoryDomain
      properties:
        name:
          type: string
        timezone:
          type: string
        address:
          $ref: '#/components/schemas/GeoAddress'
        regulatoryDomain:
          type: string
    LocationUpdate:
      type: object
      properties:
        name:
          type: string
        timezone:
          type: string
        address:
          $ref: '#/components/schemas/GeoAddress'
    FloorPlan:
      type: object
      description: Uploaded floor plan image for indoor device placement within a
        location.
      properties:
        id:
          type: string
          readOnly: true
        locationId:
          type: string
          readOnly: true
        floor:
          type: integer
          description: Floor number (0=ground, -1=basement, etc.)
        name:
          type: string
          description: Human-readable floor name (e.g. "Ground Floor")
        imageUrl:
          type: string
          format: uri
          readOnly: true
        imageWidth:
          type: integer
          readOnly: true
        imageHeight:
          type: integer
          readOnly: true
        createdAt:
          type: string
          format: date-time
          readOnly: true
    FloorPlanCreate:
      type: object
      description: Request to upload a floor plan image.
      required:
      - floor
      - image
      properties:
        floor:
          type: integer
          description: Floor number (0=ground, -1=basement, etc.)
        name:
          type: string
        image:
          type: string
          format: binary
          description: Floor plan image file (PNG or SVG)
    NodeRole:
      type: string
      enum:
      - gateway
      - extender
      - iot_bridge
      description: Corresponds to OpenSync `AWLAN_Node.mode` (router/bridge) plus
        deployment role
    Node:
      type: object
      description: Backed by `AWLAN_Node` OVSDB row plus fleet-management metadata.
      properties:
        id:
          type: string
          readOnly: true
        locationId:
          type: string
          readOnly: true
        serialNumber:
          type: string
        macAddress:
          type: string
        model:
          type: string
        role:
          $ref: '#/components/schemas/NodeRole'
        name:
          type: string
        firmwareVersion:
          type: string
        connectionState:
          type: string
          enum:
          - online
          - offline
          - provisioning
        redirectorAddress:
          type: string
          description: OpenSync `AWLAN_Node.redirector_addr`
        cloudControllerAddress:
          type: string
          description: OpenSync `AWLAN_Node.manager_addr`
        uptimeSeconds:
          type: integer
        isParent:
          type: boolean
          description: True if this node is the mesh root/gateway
        parentNodeId:
          type: string
          nullable: true
        lastSeenAt:
          type: string
          format: date-time
        createdAt:
          type: string
          format: date-time
          readOnly: true
        lifecycleState:
          type: string
          enum:
          - active
          - eol
          - decommissioned
          - rma
          default: active
          description: Device lifecycle state. 'eol' marks end-of-life (no more updates).
            'decommissioned' marks retired devices. 'rma' is for return/repair process.
        lifecycleChangedAt:
          type: string
          format: date-time
          readOnly: true
          description: When the lifecycle state was last changed.
    NodeCreate:
      type: object
      required:
      - serialNumber
      properties:
        serialNumber:
          type: string
        name:
          type: string
        role:
          $ref: '#/components/schemas/NodeRole'
        parentNodeId:
          type: string
    NodeUpdate:
      type: object
      properties:
        name:
          type: string
        role:
          $ref: '#/components/schemas/NodeRole'
        parentNodeId:
          type: string
    NodeCommand:
      type: object
      description: An asynchronous command submitted to a node for execution.
      properties:
        id:
          type: string
          readOnly: true
        nodeId:
          type: string
          readOnly: true
        type:
          type: string
          enum:
          - custom
          - reboot
          - factory_reset
          - ping
          - traceroute
          - iwinfo
          description: Command type
        input:
          type: object
          description: Command-specific input (e.g. `{"command":"uptime"}` for custom,
            null for reboot)
        status:
          type: string
          enum:
          - queued
          - running
          - completed
          - failed
          - timed_out
          readOnly: true
        output:
          type: string
          description: Command stdout when completed
          readOnly: true
        exitCode:
          type: integer
          readOnly: true
        createdAt:
          type: string
          format: date-time
          readOnly: true
        completedAt:
          type: string
          format: date-time
          readOnly: true
    NodeCommandRequest:
      type: object
      description: Request to execute a command on a node.
      required:
      - type
      properties:
        type:
          type: string
          enum:
          - custom
          - reboot
          - factory_reset
          - ping
          - traceroute
          - iwinfo
          description: Command type
        input:
          type: object
          description: Command-specific input. For custom commands use `{"command":"shell_cmd"}`.
            For reboot and factory_reset, omit this field or send null.
        timeout:
          type: integer
          default: 30
          description: Command timeout in seconds
    FirmwareInfo:
      type: object
      properties:
        currentVersion:
          type: string
        availableVersion:
          type: string
        autoUpgradeEnabled:
          type: boolean
        lastUpgradeAt:
          type: string
          format: date-time
    FirmwareUpgradeJob:
      type: object
      properties:
        jobId:
          type: string
        targetVersion:
          type: string
        status:
          type: string
          enum:
          - scheduled
          - downloading
          - installing
          - rebooting
          - completed
          - failed
        scheduledAt:
          type: string
          format: date-time
    HwMode:
      type: string
      description: Maps to `Wifi_Radio_Config.hw_mode`.
      enum:
      - 11a
      - 11b
      - 11g
      - 11n
      - 11ab
      - 11ac
      - 11ax
      - 11be
    HtMode:
      type: string
      description: Channel width from `Wifi_Radio_Config.ht_mode` (upstream also accepts
        direction annotations like HT40+/HT40- which map to HT40).
      enum:
      - HT20
      - HT2040
      - HT40
      - HT40+
      - HT40-
      - HT80
      - HT160
      - HT80+80
      - HT320
    ChannelMode:
      type: string
      description: Channel selection mode from `Wifi_Radio_Config.channel_mode`.
      enum:
      - auto
      - manual
      - cloud
      - acs
    Radio:
      type: object
      description: Backed by `Wifi_Radio_Config` (desired) merged with `Wifi_Radio_State`
        (actual).
      properties:
        id:
          type: string
          readOnly: true
        nodeId:
          type: string
          readOnly: true
        ifName:
          type: string
          example: wl1
        band:
          $ref: '#/components/schemas/Band'
        enabled:
          type: boolean
        channel:
          type: integer
        channelMode:
          $ref: '#/components/schemas/ChannelMode'
        htMode:
          $ref: '#/components/schemas/HtMode'
        hwMode:
          $ref: '#/components/schemas/HwMode'
        centerFreq0Chan:
          type: integer
          description: Center channel for 320/160/80 MHz bandwidth (WiFi 6E/7)
        txPowerDbm:
          type: integer
        country:
          type: string
          example: US
        dfsState:
          type: string
          enum:
          - not_dfs
          - cac_in_progress
          - cac_completed
          - nop
          - available
        zeroWaitDfs:
          type: string
          description: Zero-wait DFS mode from `Wifi_Radio_Config.zero_wait_dfs`.
          enum:
          - disable
          - enable
          - precac
          nullable: true
        channelUtilizationPercent:
          type: number
        thermalTxChainMask:
          type: integer
          description: Bitmask of TX chains active, reduced under thermal throttling
    RadioUpdate:
      type: object
      properties:
        enabled:
          type: boolean
        channel:
          type: integer
        channelMode:
          $ref: '#/components/schemas/ChannelMode'
        htMode:
          $ref: '#/components/schemas/HtMode'
        txPowerDbm:
          type: integer
        zeroWaitDfs:
          type: string
          enum:
          - disable
          - enable
          - precac
          nullable: true
    ChannelPlan:
      type: object
      properties:
        radioId:
          type: string
          readOnly: true
        mode:
          $ref: '#/components/schemas/ChannelMode'
        currentChannel:
          type: integer
        candidateChannels:
          type: array
          items:
            type: integer
        lastChangeReason:
          type: string
          enum:
          - manual
          - dfs_radar
          - acs_periodic
          - thermal
          - boot
        lastChangedAt:
          type: string
          format: date-time
    ChannelPlanUpdate:
      type: object
      properties:
        mode:
          $ref: '#/components/schemas/ChannelMode'
        candidateChannels:
          type: array
          items:
            type: integer
    AfcStatus:
      type: object
      description: AFC (Automated Frequency Coordination) status for 6 GHz operation.
        Backed by `Wifi_AFC` + `Wifi_Transmit_Power_Constraints`. Returned at both
        the per-radio and per-location/network scope.
      properties:
        enabled:
          type: boolean
          description: Whether AFC operation is enabled.
        radioId:
          type: string
          readOnly: true
          description: Radio identifier. Populated when returned from a radio-scoped
            endpoint; absent at location/network scope.
        afcServerUrl:
          type: string
          description: URL of the AFC service provider.
        requestState:
          type: string
          enum:
          - idle
          - not_requested
          - pending
          - requesting
          - granted
          - approved
          - rejected
          - denied
          - expired
          description: Current state of the AFC spectrum inquiry.
        lastRequest:
          type: string
          format: date-time
          description: Timestamp of the last AFC request.
        expiresAt:
          type: string
          format: date-time
          description: Timestamp when the current spectrum grant expires.
        maxPowerDbm:
          type: integer
          description: Maximum allowed EIRP in dBm.
        allowedChannels:
          type: array
          items:
            type: object
            properties:
              channel:
                type: integer
              maxEirpDbm:
                type: number
    AfcConfigUpdate:
      type: object
      properties:
        afcServerUrl:
          type: string
        enabled:
          type: boolean
    NeighborReport:
      type: object
      description: Backed by `Wifi_VIF_Neighbors`.
      properties:
        bssid:
          type: string
        channel:
          type: integer
        band:
          $ref: '#/components/schemas/Band'
        signalDbm:
          type: integer
        neighborNodeId:
          type: string
          nullable: true
          description: Populated if the neighbor is itself a node in this fleet (mesh
            topology)
        mldAddress:
          type: string
          nullable: true
          description: MLD address if part of a WiFi 7 Multi-Link Operation set
    SecurityMode:
      type: string
      enum:
      - open
      - wpa2_personal
      - wpa2_enterprise
      - wpa3_personal
      - wpa3_enterprise
      - wpa2_wpa3_mixed
      - owe
    Network:
      type: object
      description: Logical SSID definition, fanned out to `Wifi_VIF_Config` rows per
        node/radio.
      properties:
        id:
          type: string
          readOnly: true
        locationId:
          type: string
          readOnly: true
        ssid:
          type: string
        ssidBroadcast:
          type: boolean
          default: true
        securityMode:
          $ref: '#/components/schemas/SecurityMode'
        passphrase:
          type: string
          writeOnly: true
        vlanId:
          type: integer
          nullable: true
        bands:
          type: array
          items:
            $ref: '#/components/schemas/Band'
        bridge:
          type: string
          example: br-home
        mloEnabled:
          type: boolean
          description: Enables WiFi 7 Multi-Link Operation across bands sharing this
            SSID
        isGuest:
          type: boolean
          default: false
        createdAt:
          type: string
          format: date-time
          readOnly: true
    NetworkCreate:
      type: object
      required:
      - ssid
      - securityMode
      - bands
      properties:
        ssid:
          type: string
        ssidBroadcast:
          type: boolean
        securityMode:
          $ref: '#/components/schemas/SecurityMode'
        passphrase:
          type: string
        vlanId:
          type: integer
        bands:
          type: array
          items:
            $ref: '#/components/schemas/Band'
        mloEnabled:
          type: boolean
        isGuest:
          type: boolean
    NetworkUpdate:
      type: object
      properties:
        ssid:
          type: string
        ssidBroadcast:
          type: boolean
        securityMode:
          $ref: '#/components/schemas/SecurityMode'
        passphrase:
          type: string
        vlanId:
          type: integer
        bands:
          type: array
          items:
            $ref: '#/components/schemas/Band'
        mloEnabled:
          type: boolean
    ClientPsk:
      type: object
      description: Per-client pre-shared key entry for PPSK/MPSK. Maps a client MAC
        address to a unique passphrase and optional VLAN assignment. Enables a single
        SSID to serve multiple VLANs based on the password used.
      properties:
        id:
          type: string
          readOnly: true
        networkId:
          type: string
          readOnly: true
        macAddress:
          type: string
          description: Client MAC address
        vlanId:
          type: integer
          description: VLAN assigned to this client when using this PSK
        label:
          type: string
          description: Human-readable label for this entry (e.g. "IoT sensor", "Guest
            pass")
        createdAt:
          type: string
          format: date-time
          readOnly: true
    ClientPskCreate:
      type: object
      description: Request to create one or more per-client PSK entries.
      required:
      - macAddress
      - passphrase
      properties:
        macAddress:
          type: string
          description: Client MAC address
        passphrase:
          type: string
          format: password
          description: Unique passphrase for this client (min 8 characters)
        vlanId:
          type: integer
          description: Optional VLAN assignment for this client
        label:
          type: string
    Vif:
      type: object
      description: Backed by `Wifi_VIF_Config`/`Wifi_VIF_State` for a single node/radio.
      properties:
        id:
          type: string
          readOnly: true
        networkId:
          type: string
        nodeId:
          type: string
          readOnly: true
        radioId:
          type: string
          readOnly: true
        ifName:
          type: string
          example: home-ap-24
        mode:
          type: string
          enum:
          - ap
          - sta
        enabled:
          type: boolean
        mldAddress:
          type: string
          nullable: true
        mldIfName:
          type: string
          nullable: true
        associatedClientCount:
          type: integer
          readOnly: true
    GuestNetwork:
      type: object
      properties:
        locationId:
          type: string
          readOnly: true
        enabled:
          type: boolean
        networkId:
          type: string
          nullable: true
          description: Reference to the Network resource backing the guest SSID
        isolateFromLan:
          type: boolean
          default: true
        bandwidthLimitMbps:
          type: integer
          nullable: true
        expiresAt:
          type: string
          format: date-time
          nullable: true
        smsVerification:
          $ref: '#/components/schemas/SMSVerificationConfig'
          description: SMS verification for guest WiFi access.
        tiers:
          type: array
          items:
            $ref: '#/components/schemas/GuestTier'
          description: Multiple guest access tiers with different bandwidth caps,
            durations, and device limits.
        multiTierEnabled:
          type: boolean
          default: false
          description: Enable multi-tier guest access.
    GuestNetworkUpdate:
      type: object
      properties:
        enabled:
          type: boolean
        ssid:
          type: string
        passphrase:
          type: string
        isolateFromLan:
          type: boolean
        bandwidthLimitMbps:
          type: integer
        expiresAt:
          type: string
          format: date-time
    Device:
      type: object
      description: Backed by `Wifi_Associated_Clients` (live) plus historical device
        inventory.
      properties:
        id:
          type: string
          readOnly: true
        locationId:
          type: string
          readOnly: true
        macAddress:
          type: string
        mldAddress:
          type: string
          nullable: true
          description: Present if the client is a WiFi 7 MLO device
        hostname:
          type: string
          nullable: true
        nickname:
          type: string
          nullable: true
        ipAddress:
          type: string
          nullable: true
        deviceType:
          type: string
          description: Fingerprinted device classification (e.g. "smart_tv", "phone",
            "laptop")
        vendor:
          type: string
          nullable: true
        connected:
          type: boolean
        connectedNodeId:
          type: string
          nullable: true
        connectedVifId:
          type: string
          nullable: true
        band:
          $ref: '#/components/schemas/Band'
        rssiDbm:
          type: integer
          nullable: true
        txRateMbps:
          type: number
          nullable: true
        rxRateMbps:
          type: number
          nullable: true
        blocked:
          type: boolean
          default: false
        firstSeenAt:
          type: string
          format: date-time
        lastSeenAt:
          type: string
          format: date-time
        fingerprint:
          $ref: '#/components/schemas/DeviceFingerprint'
          description: Device fingerprint data from traffic analysis. Populated when
            DPI/FSM is active.
        lifecycleState:
          type: string
          enum:
          - active
          - eol
          - decommissioned
          - rma
          default: active
          description: Device lifecycle state. 'eol' marks end-of-life (no more updates).
            'decommissioned' marks retired devices. 'rma' is for return/repair process.
        lifecycleChangedAt:
          type: string
          format: date-time
          readOnly: true
          description: When the lifecycle state was last changed.
    DeviceUpdate:
      type: object
      properties:
        nickname:
          type: string
        deviceGroup:
          type: string
    DeviceAlertSettings:
      type: object
      description: Per-device alert threshold overrides. Each field accepts a numeric
        threshold or null to revert to the fleet-wide default.
      properties:
        nodeId:
          type: string
          readOnly: true
        pingLossPercent:
          type: number
          nullable: true
          description: Maximum acceptable ping packet loss percentage
        rttMs:
          type: number
          nullable: true
          description: Maximum acceptable round-trip time in milliseconds
        cpuPercent:
          type: number
          nullable: true
          description: Maximum acceptable CPU usage percentage
        memoryPercent:
          type: number
          nullable: true
          description: Maximum acceptable memory usage percentage
        uptimeSeconds:
          type: integer
          nullable: true
          description: Minimum required uptime before alerting
        trafficMbps:
          type: number
          nullable: true
          description: Maximum acceptable traffic rate in Mbps
        updatedAt:
          type: string
          format: date-time
          readOnly: true
    DeviceAlertSettingsUpdate:
      type: object
      description: Partial update for per-device alert thresholds. Set any field to
        null to revert to the fleet-wide default.
      properties:
        pingLossPercent:
          type: number
          nullable: true
        rttMs:
          type: number
          nullable: true
        cpuPercent:
          type: number
          nullable: true
        memoryPercent:
          type: number
          nullable: true
        uptimeSeconds:
          type: integer
          nullable: true
        trafficMbps:
          type: number
          nullable: true
    WifiSession:
      type: object
      description: Historical WiFi connection session for a client device. Tracks
        connect/disconnect events with associated SSID, band, and traffic data.
      properties:
        id:
          type: string
          readOnly: true
        deviceId:
          type: string
          description: Client device identifier
        macAddress:
          type: string
        ssid:
          type: string
        band:
          $ref: '#/components/schemas/Band'
        radioId:
          type: string
        nodeId:
          type: string
        connectedAt:
          type: string
          format: date-time
        disconnectedAt:
          type: string
          format: date-time
          nullable: true
        txBytes:
          type: integer
          readOnly: true
        rxBytes:
          type: integer
          readOnly: true
        durationSeconds:
          type: integer
          readOnly: true
    SteeringPolicy:
      type: object
      properties:
        locationId:
          type: string
          readOnly: true
        bandSteeringEnabled:
          type: boolean
        clientSteeringEnabled:
          type: boolean
        rssiThresholdDbm:
          type: integer
          description: Minimum RSSI before a client becomes a steering candidate
        preferBand:
          $ref: '#/components/schemas/Band'
        apSteeringEnabled:
          type: boolean
          description: Steer between nodes (mesh) in addition to bands on the same
            node
    SteeringPolicyUpdate:
      type: object
      properties:
        bandSteeringEnabled:
          type: boolean
        clientSteeringEnabled:
          type: boolean
        rssiThresholdDbm:
          type: integer
        preferBand:
          $ref: '#/components/schemas/Band'
        apSteeringEnabled:
          type: boolean
    DeviceSteeringState:
      type: object
      description: Per-device band/client steering state mapped from `Band_Steering_Clients`.
      properties:
        deviceId:
          type: string
          readOnly: true
        csMode:
          type: string
          description: Client steering mode (`cs_mode`).
          enum:
          - false
          - home
          - away
        csState:
          type: string
          description: Client steering execution state (`cs_state`).
          enum:
          - none
          - steering
          - expired
          - failed
          - xing_low
          - xing_high
          - xing_disabled
        kickType:
          type: string
          description: Kick method (`kick_type`).
          enum:
          - none
          - deauth
          - disassoc
          - bss_tm_req
          - rrm_br_req
          - btm_deauth
          - btm_disassoc
        rejectDetection:
          type: string
          description: Probe rejection detection mode (`reject_detection`).
          enum:
          - none
          - probe_all
          - probe_null
          - probe_direct
          - auth_block
        forceKick:
          type: string
          description: Force-kick mode (`force_kick`).
          enum:
          - none
          - speculative
          - directed
          - ghost_device
        eligibleForSteering:
          type: boolean
        lastSteerAttemptAt:
          type: string
          format: date-time
          nullable: true
        lastSteerResult:
          type: string
          enum:
          - success
          - rejected
          - timeout
          - not_attempted
        history:
          type: array
          items:
            type: object
            properties:
              at:
                type: string
                format: date-time
              fromNodeId:
                type: string
              toNodeId:
                type: string
              fromBand:
                $ref: '#/components/schemas/Band'
              toBand:
                $ref: '#/components/schemas/Band'
              result:
                type: string
                enum:
                - success
                - rejected
                - timeout
    SteeringJob:
      type: object
      properties:
        jobId:
          type: string
        deviceId:
          type: string
        status:
          type: string
          enum:
          - pending
          - sent
          - acknowledged
          - failed
    SpeedTestResult:
      type: object
      properties:
        testId:
          type: string
          readOnly: true
        nodeId:
          type: string
          readOnly: true
        status:
          type: string
          enum:
          - pending
          - running
          - completed
          - failed
        downloadMbps:
          type: number
          nullable: true
        uploadMbps:
          type: number
          nullable: true
        latencyMs:
          type: number
          nullable: true
        jitterMs:
          type: number
          nullable: true
        server:
          type: string
          nullable: true
        startedAt:
          type: string
          format: date-time
        completedAt:
          type: string
          format: date-time
          nullable: true
        providerId:
          type: string
          readOnly: true
          description: ID of the speedtest provider that generated this result
    SpeedTestJob:
      type: object
      properties:
        testId:
          type: string
        status:
          type: string
          enum:
          - pending
        providerId:
          type: string
          description: ID of the speedtest provider that will run this test
    SpeedtestProviderConfig:
      type: object
      properties:
        id:
          type: string
          readOnly: true
        displayName:
          type: string
          description: Human-readable label for this provider (e.g. "Ookla — Frankfurt
            server" or "iPerf3 — internal DC")
        provider:
          type: string
          enum:
          - builtin
          - ookla
          - iperf3
          - custom
          default: builtin
        serverUrl:
          type: string
          format: uri
        scheduleIntervalMinutes:
          type: integer
          minimum: 15
        interfaceBinding:
          type: string
        downloadThresholdMbps:
          type: number
        uploadThresholdMbps:
          type: number
        resultCallbackUrl:
          type: string
          format: uri
        enabled:
          type: boolean
          default: true
    StatsSample:
      type: object
      properties:
        timestamp:
          type: string
          format: date-time
        metric:
          type: string
        value:
          type: number
        radioId:
          type: string
          nullable: true
    DiagnosticsSummary:
      type: object
      properties:
        nodeId:
          type: string
          readOnly: true
        healthScore:
          type: integer
          minimum: 0
          maximum: 100
        cpuUsagePercent:
          type: number
        memoryUsagePercent:
          type: number
        temperatureCelsius:
          type: number
          nullable: true
        rebootCount24h:
          type: integer
        lastCrashAt:
          type: string
          format: date-time
          nullable: true
        activeAlerts:
          type: array
          items:
            type: string
    LogPullJob:
      type: object
      properties:
        jobId:
          type: string
        status:
          type: string
          enum:
          - pending
          - uploading
          - completed
          - failed
        downloadUrl:
          type: string
          nullable: true
    UplinkStatus:
      type: object
      description: Backed by `Connection_Manager_Uplink`.
      properties:
        ifName:
          type: string
          example: eth0
        ifType:
          type: string
          enum:
          - eth
          - gre
          - vlan
          - bond
          - lte
        priority:
          type: integer
        isUsed:
          type: boolean
        hasL2:
          type: boolean
        hasL3:
          type: boolean
        wanIp:
          type: string
          nullable: true
        latencyMs:
          type: number
          nullable: true
        mediaType:
          type: string
          enum:
          - ethernet
          - docsis
          - dsl
          - pon
          - satellite
          - lte
          - nr
          - wifi
          - other
          description: Physical media type of the WAN uplink, detected from the CPE
            WAN interface.
          readOnly: true
        mediaDetail:
          type: object
          description: Media-specific diagnostics. Fields vary by mediaType.
          properties:
            docsis:
              type: object
              properties:
                downstreamChannels:
                  type: integer
                upstreamChannels:
                  type: integer
                modulation:
                  type: string
                cmtsMac:
                  type: string
            dsl:
              type: object
              properties:
                lineMode:
                  type: string
                  enum:
                  - adsl
                  - adsl2
                  - adsl2p
                  - vdsl
                  - vdsl2
                  - gfast
                downstreamRateBps:
                  type: integer
                upstreamRateBps:
                  type: integer
                snrMarginDb:
                  type: number
                attenuationDb:
                  type: number
            pon:
              type: object
              properties:
                ontId:
                  type: string
                oltVendor:
                  type: string
                rxPowerDbm:
                  type: number
                txPowerDbm:
                  type: number
    BackhaulLink:
      type: object
      properties:
        nodeId:
          type: string
          readOnly: true
        linkType:
          type: string
          enum:
          - wired
          - wireless
        parentNodeId:
          type: string
        band:
          $ref: '#/components/schemas/Band'
          nullable: true
        rssiDbm:
          type: integer
          nullable: true
        txRateMbps:
          type: number
          nullable: true
        rxRateMbps:
          type: number
          nullable: true
    SecurityPolicy:
      type: object
      properties:
        locationId:
          type: string
          readOnly: true
        threatProtectionEnabled:
          type: boolean
        dpiTaggingEnabled:
          type: boolean
        iotIsolationEnabled:
          type: boolean
        blockedCategories:
          type: array
          items:
            type: string
            example: malware
    SecurityPolicyUpdate:
      type: object
      properties:
        threatProtectionEnabled:
          type: boolean
        dpiTaggingEnabled:
          type: boolean
        iotIsolationEnabled:
          type: boolean
        blockedCategories:
          type: array
          items:
            type: string
    ThreatEvent:
      type: object
      properties:
        id:
          type: string
          readOnly: true
        locationId:
          type: string
          readOnly: true
        deviceId:
          type: string
          nullable: true
        category:
          type: string
          example: botnet_c2
        severity:
          type: string
          enum:
          - low
          - medium
          - high
          - critical
        action:
          type: string
          enum:
          - blocked
          - flagged
          - allowed
        destination:
          type: string
          nullable: true
        detectedAt:
          type: string
          format: date-time
    ContentFilteringConfig:
      type: object
      properties:
        locationId:
          type: string
          readOnly: true
        enabled:
          type: boolean
        blockedCategories:
          type: array
          items:
            type: string
            example: adult_content
        allowList:
          type: array
          items:
            type: string
        denyList:
          type: array
          items:
            type: string
    ContentFilteringConfigUpdate:
      type: object
      properties:
        enabled:
          type: boolean
        blockedCategories:
          type: array
          items:
            type: string
        allowList:
          type: array
          items:
            type: string
        denyList:
          type: array
          items:
            type: string
    ContentFilteringProviderConfig:
      type: object
      properties:
        provider:
          type: string
          enum:
          - builtin
          - opendns
          - nextdns
          - cleanbrowsing
          - custom
          default: builtin
        serverUrl:
          type: string
          format: uri
        apiKey:
          type: string
          writeOnly: true
        profileId:
          type: string
        categories:
          type: array
          items:
            type: string
        enabled:
          type: boolean
    ParentalControlProfile:
      type: object
      properties:
        id:
          type: string
          readOnly: true
        locationId:
          type: string
          readOnly: true
        name:
          type: string
        deviceIds:
          type: array
          items:
            type: string
        contentFilterLevel:
          type: string
          enum:
          - none
          - mild
          - moderate
          - strict
        schedules:
          type: array
          items:
            $ref: '#/components/schemas/AccessSchedule'
        pausedUntil:
          type: string
          format: date-time
          nullable: true
          description: If set, internet access for this profile's devices is paused
            until this time
    ParentalControlProfileCreate:
      type: object
      required:
      - name
      properties:
        name:
          type: string
        deviceIds:
          type: array
          items:
            type: string
        contentFilterLevel:
          type: string
          enum:
          - none
          - mild
          - moderate
          - strict
        schedules:
          type: array
          items:
            $ref: '#/components/schemas/AccessSchedule'
    ParentalControlProfileUpdate:
      type: object
      properties:
        name:
          type: string
        deviceIds:
          type: array
          items:
            type: string
        contentFilterLevel:
          type: string
          enum:
          - none
          - mild
          - moderate
          - strict
        schedules:
          type: array
          items:
            $ref: '#/components/schemas/AccessSchedule'
        pausedUntil:
          type: string
          format: date-time
          nullable: true
    AccessSchedule:
      type: object
      properties:
        daysOfWeek:
          type: array
          items:
            type: string
            enum:
            - mon
            - tue
            - wed
            - thu
            - fri
            - sat
            - sun
        startTime:
          type: string
          example: 08:00
        endTime:
          type: string
          example: '20:00'
        allow:
          type: boolean
          default: true
    CaptivePortalConfig:
      type: object
      properties:
        locationId:
          type: string
          readOnly: true
        networkId:
          type: string
        enabled:
          type: boolean
        portalUrl:
          type: string
          nullable: true
        authType:
          type: string
          enum:
          - click_through
          - terms_of_service
          - credentials
          - external_radius
        sessionTimeoutMinutes:
          type: integer
    CaptivePortalConfigUpdate:
      type: object
      properties:
        networkId:
          type: string
        enabled:
          type: boolean
        portalUrl:
          type: string
        authType:
          type: string
          enum:
          - click_through
          - terms_of_service
          - credentials
          - external_radius
        sessionTimeoutMinutes:
          type: integer
    Person:
      type: object
      properties:
        id:
          type: string
          readOnly: true
        locationId:
          type: string
          readOnly: true
        name:
          type: string
        deviceIds:
          type: array
          items:
            type: string
        bleTokenId:
          type: string
          nullable: true
          description: Associated BLE beacon/token used for room-level presence
    PersonCreate:
      type: object
      required:
      - name
      properties:
        name:
          type: string
        deviceIds:
          type: array
          items:
            type: string
        bleTokenId:
          type: string
    PersonUpdate:
      type: object
      properties:
        name:
          type: string
        deviceIds:
          type: array
          items:
            type: string
        bleTokenId:
          type: string
    Room:
      type: object
      properties:
        id:
          type: string
          readOnly: true
        locationId:
          type: string
          readOnly: true
        name:
          type: string
        anchorNodeId:
          type: string
          description: The node whose BLE/RSSI signal defines this room's zone
    PresenceEvent:
      type: object
      properties:
        id:
          type: string
          readOnly: true
        personId:
          type: string
          nullable: true
        roomId:
          type: string
        eventType:
          type: string
          enum:
          - entered
          - left
        occurredAt:
          type: string
          format: date-time
    VpnType:
      type: string
      enum:
      - wireguard
      - ipsec
      - openvpn
    VpnConfig:
      type: object
      properties:
        locationId:
          type: string
          readOnly: true
        enabled:
          type: boolean
        vpnType:
          $ref: '#/components/schemas/VpnType'
        mode:
          type: string
          enum:
          - client
          - site_to_site
        remoteEndpoint:
          type: string
          nullable: true
        routeAllTraffic:
          type: boolean
        status:
          type: string
          enum:
          - connected
          - connecting
          - disconnected
          - error
          readOnly: true
    VpnConfigUpdate:
      type: object
      properties:
        enabled:
          type: boolean
        vpnType:
          $ref: '#/components/schemas/VpnType'
        mode:
          type: string
          enum:
          - client
          - site_to_site
        remoteEndpoint:
          type: string
        presharedKey:
          type: string
          writeOnly: true
        routeAllTraffic:
          type: boolean
    VPNServiceConfig:
      type: object
      properties:
        provider:
          type: string
          enum:
          - tailscale
          - zerotier
          - cloudflare_warp
          - custom
          default: tailscale
        authToken:
          type: string
          writeOnly: true
        subnetRoutes:
          type: array
          items:
            type: string
          description: Subnet CIDRs to advertise via this node
        exitNode:
          type: boolean
          description: Advertise this node as an exit node for internet-bound traffic
        dnsConfig:
          type: object
          properties:
            nameservers:
              type: array
              items:
                type: string
            searchDomains:
              type: array
              items:
                type: string
        enableMssh:
          type: boolean
          description: Enable Magic DNS or equivalent overlay DNS
        hostname:
          type: string
          description: Overlay network hostname for this node
        enabled:
          type: boolean
          default: false
    EventType:
      type: string
      description: Cloud event type. Where possible these map to upstream OpenSync
        telemetry sources (OVSDB `Reboot_Status`, `Alarms`, `Uplink_Events`, `opensync_stats.proto`
        steering events, and cloud-side optimization jobs).
      enum:
      - node.online
      - node.offline
      - node.rebooted
      - node.factory_reset
      - node.firmware_upgraded
      - node.firmware_downgraded
      - node.firmware_upgrade_failed
      - node.crashed
      - device.connected
      - device.disconnected
      - device.blocked
      - device.steering_started
      - device.steering_completed
      - device.steering_failed
      - device.kicked
      - uplink.link_up
      - uplink.link_down
      - uplink.router_unreachable
      - uplink.internet_unreachable
      - wan.failover
      - alarm.raised
      - alarm.cleared
      - optimization.started
      - optimization.succeeded
      - optimization.failed
      - speedtest.completed
      - threat.detected
      - presence.entered
      - presence.left
      - dfs.radar_detected
      - agent.config_created
      - agent.config_deleted
      - agent.enabled
      - agent.disabled
      - agent.config_updated
      - agent.status_changed
      - agent.error
      - speedtest.provider_changed
      - security.ids_engine_changed
      - content_filtering.provider_changed
      - agent_policy.created
      - agent_policy.updated
      - agent_policy.deleted
      - agent_policy.enforced
      - vpn.service_changed
      - analytics.agent_changed
      - remote_access.config_changed
      - iot_bridge.config_changed
      - client.identity_merged
      - client.identity_split
      - client_quality.degraded
      - client_quality.improved
      - qoe.score_changed
      - quality_attenuation.threshold_breach
    Webhook:
      type: object
      properties:
        id:
          type: string
          readOnly: true
        customerId:
          type: string
          readOnly: true
        url:
          type: string
          format: uri
        eventTypes:
          type: array
          items:
            $ref: '#/components/schemas/EventType'
        secret:
          type: string
          writeOnly: true
        enabled:
          type: boolean
        createdAt:
          type: string
          format: date-time
          readOnly: true
    WebhookCreate:
      type: object
      required:
      - url
      - eventTypes
      properties:
        url:
          type: string
          format: uri
        eventTypes:
          type: array
          items:
            $ref: '#/components/schemas/EventType'
        secret:
          type: string
    CloudEvent:
      type: object
      properties:
        id:
          type: string
          readOnly: true
        type:
          $ref: '#/components/schemas/EventType'
        locationId:
          type: string
        nodeId:
          type: string
          nullable: true
        deviceId:
          type: string
          nullable: true
        occurredAt:
          type: string
          format: date-time
        data:
          type: object
          additionalProperties: true
    BulkOperationPreview:
      type: object
      description: Dry-run preview result for bulk operations. Returns the nodes that
        would be affected without executing the operation.
      properties:
        mode:
          type: string
          enum:
          - claim
          - update
          - move
        affectedCount:
          type: integer
          description: Number of nodes that would be affected
        nodes:
          type: array
          items:
            $ref: '#/components/schemas/BulkOperationPreviewNode'
    BulkOperationPreviewNode:
      type: object
      description: One row of a bulk-operation dry-run preview. Field names use snake_case
        to match the request body of `/bulk/nodes`; this is the only response schema
        in the spec that does so intentionally.
      properties:
        serial_number:
          type: string
        current_location_name:
          type: string
        current_customer_id:
          type: string
        new_location_name:
          type: string
        action:
          type: string
          enum:
          - create
          - update
          - move
          - noop
        warnings:
          type: array
          items:
            type: string
    BulkNodesRequest:
      type: object
      description: Shared request body for `POST /bulk/nodes` and the dry-run variant
        `POST /bulk/nodes/preview`. Uses snake_case field names — this is the only
        request schema in the spec that does so, mirroring the upstream Plume bulk-claim
        payload format.
      required:
      - nodes
      properties:
        nodes:
          type: array
          maxItems: 5000
          items:
            type: object
            required:
            - serial_number
            properties:
              serial_number:
                type: string
              location_name:
                type: string
              role:
                type: string
                enum:
                - gateway
                - satellite
              nickname:
                type: string
        mode:
          type: string
          enum:
          - claim
          - update
          - move
          default: claim
    Pagination:
      type: object
      properties:
        page:
          type: integer
        per_page:
          type: integer
        total:
          type: integer
        total_pages:
          type: integer
    LEDPattern:
      type: object
      description: Per-state LED pattern configuration matching `LED_Config` rows.
      properties:
        name:
          type: string
          enum:
          - idle
          - error
          - connected
          - connecting
          - connectfail
          - wps
          - optimize
          - locate
          - hwerror
          - thermal
          - btconnectable
          - btconnecting
          - btconnected
          - btconnectfail
          - upgrading
          - upgraded
          - upgradefail
          - hwtest
          - iotalarm
          - cloud
        priority:
          type: integer
        position:
          type: integer
          minimum: 0
          maximum: 255
        pattern:
          type: string
          description: On/off/time pattern string (platform-specific format).
    LEDPatternUpdate:
      type: object
      properties:
        priority:
          type: integer
        position:
          type: integer
        pattern:
          type: string
    SSIDCreate:
      type: object
      required:
      - ssid
      - security
      properties:
        ssid:
          type: string
        broadcast:
          type: string
          enum:
          - enabled
          - disabled
          - disabled_null
          default: enabled
        band:
          type: string
          enum:
          - 2.4G
          - 5G
          - 6G
          default: 5G
        security:
          $ref: '#/components/schemas/SecurityMode'
        vlan_id:
          type: integer
        client_limit:
          type: integer
          default: 64
        pmf:
          type: string
          description: Protected Management Frames.
          enum:
          - disabled
          - optional
          - required
          default: required
        airtime_precedence:
          type: string
          enum:
          - low
          - medium
          - high
          default: medium
      example:
        ssid: SmithWiFi
        broadcast: enabled
        band: 5G
        security:
          mode: wpa3-compat
          psk: '****'
          pmf: required
          group_rekey_sec: 3600
        vlan_id: null
        client_limit: 64
        airtime_precedence: medium
    BandSteeringConfig:
      type: object
      properties:
        enabled:
          type: boolean
          default: true
        prefer_5g:
          type: boolean
          default: true
        prefer_6g:
          type: boolean
          default: false
        algorithm:
          type: string
          enum:
          - rssi_based
          - channel_utilization
          - hybrid
          default: hybrid
        parameters:
          type: object
          properties:
            chan_util_check_sec:
              type: integer
            chan_util_hwm_pct:
              type: integer
            chan_util_lwm_pct:
              type: integer
            inactivity_check_sec:
              type: integer
            inactivity_timeout_normal_sec:
              type: integer
            inactivity_timeout_overload_sec:
              type: integer
            rssi_xing_threshold_dbm:
              type: integer
            kick_debounce_threshold:
              type: integer
            kick_debounce_period_sec:
              type: integer
            success_threshold_secs:
              type: integer
    ConnectedClient:
      type: object
      properties:
        mac:
          type: string
        nickname:
          type: string
        ip:
          type: string
        hostname:
          type: string
        state:
          type: string
          enum:
          - active
          - idle
          - power save
        capabilities:
          type: array
          items:
            type: string
        signal:
          type: object
          properties:
            rssi_dbm:
              type: integer
            snr_db:
              type: integer
            phy_rate_mbps:
              type: integer
        band:
          type: string
        ssid:
          type: string
        radio:
          type: string
        vlan_id:
          type: integer
          nullable: true
        security:
          type: object
          properties:
            key_mgmt:
              type: string
            pairwise_cipher:
              type: string
        blocked:
          type: boolean
        uptime_sec:
          type: integer
        bytes_rx:
          type: integer
        bytes_tx:
          type: integer
    SecurityConfig:
      type: object
      properties:
        shield:
          type: object
          properties:
            enabled:
              type: boolean
            level:
              type: string
              enum:
              - low
              - medium
              - high
            block_malware:
              type: boolean
            block_phishing:
              type: boolean
            block_botnet:
              type: boolean
            block_intrusion:
              type: boolean
        iot_protection:
          type: object
          properties:
            enabled:
              type: boolean
            auto_isolate:
              type: boolean
            vlan_id:
              type: integer
    IoTBridgeConfig:
      type: object
      properties:
        provider:
          type: string
          enum:
          - home_assistant
          - zigbee2mqtt
          - custom
          default: zigbee2mqtt
        coordinatorPort:
          type: string
          description: Serial port path for the coordinator dongle (e.g. /dev/ttyUSB0)
        mqttBroker:
          type: string
          format: uri
          description: MQTT broker for device event and state reporting
        authToken:
          type: string
          writeOnly: true
          description: API token for the bridge provider (e.g. Home Assistant long-lived
            access token)
        panId:
          type: string
          description: Zigbee PAN ID (hex string)
        channel:
          type: integer
          description: Zigbee channel (11-26)
          minimum: 11
          maximum: 26
        networkKey:
          type: string
          writeOnly: true
          description: Zigbee network encryption key (hex string)
        enabled:
          type: boolean
          default: false
    IDSConfig:
      type: object
      properties:
        engine:
          type: string
          enum:
          - wips
          - snort
          - suricata
          - zeek
          - none
          default: wips
        enabled:
          type: boolean
        mode:
          type: string
          enum:
          - ids
          - ips
        rulesetUrl:
          type: string
          format: uri
        rulesetUpdateSchedule:
          type: string
          description: Cron expression for automatic ruleset fetching
        interfaces:
          type: array
          items:
            type: string
        alertWebhook:
          type: string
          format: uri
        performanceProfile:
          type: string
          enum:
          - low
          - medium
          - high
          - max
          default: medium
    DHCPConfig:
      type: object
      properties:
        enabled:
          type: boolean
        status:
          type: string
          description: Server status from `DHCPv4_Server.status`.
          enum:
          - disabled
          - enabled
          - error
          readOnly: true
        subnet:
          type: string
        pool_start:
          type: string
        pool_end:
          type: string
        lease_time_sec:
          type: integer
          default: 86400
        dns:
          type: array
          items:
            type: string
        gateway:
          type: string
        options:
          type: array
          items:
            type: object
            properties:
              tag:
                type: integer
              value:
                type: string
        vendorClassId:
          type: string
          description: DHCP option 60 — Vendor Class Identifier sent by the CPE client.
        clientId:
          type: string
          description: DHCP option 61 — Client Identifier.
        sendHostname:
          type: boolean
          description: Send DHCP hostname (option 12) in client requests.
          default: true
        customDnsServers:
          type: array
          items:
            type: string
          description: Custom DNS servers to include in DHCP server responses (option
            6).
        customNtpServers:
          type: array
          items:
            type: string
          description: Custom NTP servers to include in DHCP server responses (option
            42).
        domainName:
          type: string
          description: Domain name to include in DHCP server responses (option 15).
      example:
        enabled: true
        subnet: 192.168.1.0/24
        pool_start: 192.168.1.100
        pool_end: 192.168.1.250
        lease_time_sec: 86400
        dns:
        - 192.168.1.1
        gateway: 192.168.1.1
    DHCPLease:
      type: object
      description: DHCP lease from `DHCPv4_Lease` / `DHCPv6_Lease`.
      properties:
        hostname:
          type: string
        mac:
          type: string
        ip:
          type: string
        expiry:
          type: string
          format: date-time
        status:
          type: string
          description: Lease status from `DHCPv4_Lease.status`.
          enum:
          - leased
          - static
          - error
        leaseTimeSec:
          type: number
        fingerprint:
          type: string
          description: DHCP fingerprint / vendor class identifier
          nullable: true
    DHCPReservation:
      type: object
      description: Static DHCP reservation from `DHCP_reserved_IP`.
      properties:
        id:
          type: string
          readOnly: true
        mac:
          type: string
        hostname:
          type: string
        ip:
          type: string
        enabled:
          type: boolean
    DHCPReservationCreate:
      type: object
      required:
      - mac
      properties:
        mac:
          type: string
        hostname:
          type: string
        ip:
          type: string
    PortForwardCreate:
      type: object
      required:
      - protocol
      - external_port
      - internal_ip
      - internal_port
      properties:
        name:
          type: string
        protocol:
          type: string
          enum:
          - tcp
          - udp
        external_port:
          type: integer
        internal_ip:
          type: string
        internal_port:
          type: integer
        nat_loopback:
          type: boolean
          default: false
        enabled:
          type: boolean
          default: true
    PortForwardUpdate:
      type: object
      properties:
        external_port:
          type: integer
        internal_ip:
          type: string
        internal_port:
          type: integer
        nat_loopback:
          type: boolean
        enabled:
          type: boolean
    StaticRoute:
      type: object
      required:
      - destination
      - gateway
      properties:
        destination:
          type: string
        netmask:
          type: string
        gateway:
          type: string
        interface:
          type: string
        metric:
          type: integer
          default: 1
        family:
          type: string
          enum:
          - ipv4
          - ipv6
          default: ipv4
    PolicyRouteRule:
      type: object
      properties:
        name:
          type: string
        addr_family:
          type: string
          enum:
          - ipv4
          - ipv6
        priority:
          type: integer
        source_prefix:
          type: string
        destination_prefix:
          type: string
        input_interface:
          type: string
        output_interface:
          type: string
        fwmark:
          type: integer
        lookup_table:
          type: integer
    VPNCreate:
      type: object
      required:
      - name
      - remote_endpoint
      - remote_subnets
      properties:
        name:
          type: string
        type:
          type: string
          enum:
          - ipsec
          - wireguard
          - openvpn
          default: ipsec
        enabled:
          type: boolean
          default: true
        local_subnets:
          type: array
          items:
            type: string
        remote_endpoint:
          type: string
        remote_subnets:
          type: array
          items:
            type: string
        psk:
          type: string
          writeOnly: true
        ike:
          type: object
          properties:
            version:
              type: string
              description: IKE version from `IPSec_Config.key_exchange`.
              enum:
              - ike
              - ikev1
              - ikev2
              default: ikev2
            encryption:
              type: string
              enum:
              - aes128
              - aes192
              - aes256
              - 3des
              default: aes256
            integrity:
              type: string
              enum:
              - sha1
              - sha256
              - sha384
              - sha512
              - md5
              default: sha256
            dh_group:
              type: string
              enum:
              - modp768
              - modp1024
              - modp1536
              - modp2048
              - modp3072
              - modp4096
              - modp6144
              - modp8192
              default: modp2048
            lifetime_sec:
              type: integer
              default: 28800
        esp:
          type: object
          properties:
            encryption:
              type: string
              enum:
              - aes128
              - aes192
              - aes256
              - 3des
              - null
              default: aes256
            integrity:
              type: string
              enum:
              - sha1
              - sha256
              - sha384
              - sha512
              - md5
              - null
              default: sha256
            lifetime_sec:
              type: integer
              default: 3600
        healthcheck_ip:
          type: string
        openvpn:
          $ref: '#/components/schemas/OpenVPNConfig'
          description: OpenVPN-specific configuration. Required when type is openvpn.
      example:
        name: office-vpn
        type: ipsec
        enabled: true
        local_subnets:
        - 192.168.1.0/24
        remote_endpoint: 203.0.113.50
        remote_subnets:
        - 10.0.0.0/8
        psk: '****'
        ike:
          encryption: aes256
          integrity: sha256
          dh_group: modp2048
          lifetime_sec: 28800
        esp:
          encryption: aes256
          integrity: sha256
          lifetime_sec: 3600
        healthcheck_ip: 10.0.0.1
    VPNUpdate:
      type: object
      properties:
        enabled:
          type: boolean
        remote_endpoint:
          type: string
        remote_subnets:
          type: array
          items:
            type: string
        healthcheck_ip:
          type: string
        openvpn:
          $ref: '#/components/schemas/OpenVPNConfig'
          description: OpenVPN-specific configuration update.
    QoSConfig:
      type: object
      properties:
        mode:
          type: string
          enum:
          - adaptive
          - static
          - disabled
        adaptive_qos:
          type: object
          properties:
            enabled:
              type: boolean
            active_thresh_kbps:
              type: integer
            num_pingers:
              type: integer
            ping_interval_ms:
              type: integer
            latency_measure_type:
              type: string
              enum:
              - active
              - passive
            reflectors:
              type: array
              items:
                type: string
        queues:
          type: array
          items:
            type: object
            properties:
              name:
                type: string
              priority:
                type: integer
              bandwidth_min_kbps:
                type: integer
              bandwidth_max_kbps:
                type: integer
              dscp:
                type: integer
    LoggingConfig:
      type: object
      properties:
        modules:
          type: array
          items:
            type: object
            properties:
              name:
                type: string
              severity:
                type: string
                enum:
                - emerg
                - alert
                - crit
                - err
                - warning
                - notice
                - info
                - debug
        remote_logging:
          type: object
          properties:
            enabled:
              type: boolean
            server:
              type: string
            port:
              type: integer
    RevSSHRequest:
      type: object
      properties:
        server_host:
          type: string
        server_port:
          type: integer
          default: 22
        server_user:
          type: string
        tunnel_remote_port:
          type: integer
        idle_timeout_sec:
          type: integer
          default: 3600
    TelemetryConfig:
      type: object
      properties:
        stats_collection:
          type: object
          properties:
            enabled:
              type: boolean
            reporting_interval_sec:
              type: integer
              default: 3600
            sampling_interval_sec:
              type: integer
              default: 60
        stats_types:
          type: array
          items:
            type: string
            enum:
            - latency
            - neighbor
            - survey
            - client
            - capacity
            - radio
            - quality
            - device
            - rssi
            - steering
        mqtt_reporting:
          type: object
          properties:
            enabled:
              type: boolean
            topic_prefix:
              type: string
            qos:
              type: integer
              enum:
              - 0
              - 1
              - 2
              default: 1
    TelemetryReportConfig:
      type: object
      required:
      - name
      - interval_sec
      properties:
        name:
          type: string
        interval_sec:
          type: integer
          minimum: 60
        format:
          type: string
          enum:
          - json
          - csv
          - protobuf
          default: json
        mqtt_topic:
          type: string
        filter:
          type: object
          properties:
            smac:
              type: array
              items:
                type: string
            dmac:
              type: array
              items:
                type: string
            src_ip:
              type: array
              items:
                type: string
            dst_ip:
              type: array
              items:
                type: string
            app_names:
              type: array
              items:
                type: string
            app_categories:
              type: array
              items:
                type: string
    ProvisionProfile:
      type: object
      properties:
        radios:
          type: array
          items:
            $ref: '#/components/schemas/RadioUpdate'
        ssids:
          type: array
          items:
            $ref: '#/components/schemas/SSIDCreate'
        dhcp:
          $ref: '#/components/schemas/DHCPConfig'
        services:
          type: object
          additionalProperties:
            type: boolean
        country:
          type: string
          pattern: ^[A-Z]{2}$
        band_steering:
          $ref: '#/components/schemas/BandSteeringConfig'
        security:
          $ref: '#/components/schemas/SecurityConfig'
      example:
        country: US
        radios:
        - enabled: true
          channel_mode: cloud
        - enabled: true
          channel_mode: cloud
          width: HT80
        ssids:
        - ssid: MyWiFi
          security:
            mode: wpa3-compat
            psk: '****'
          band: 5G
        dhcp:
          enabled: true
          subnet: 192.168.1.0/24
          pool_start: 192.168.1.100
          pool_end: 192.168.1.250
        band_steering:
          enabled: true
        security:
          shield:
            enabled: true
            level: medium
    ProvisionTemplate:
      type: object
      required:
      - name
      - profile
      properties:
        name:
          type: string
        description:
          type: string
        profile:
          $ref: '#/components/schemas/ProvisionProfile'
        tags:
          type: array
          items:
            type: string
    ThreadConfig:
      type: object
      properties:
        enabled:
          type: boolean
        network_name:
          type: string
        pan_id:
          type: integer
        ext_pan_id:
          type: string
        network_key:
          type: string
          writeOnly: true
        channel:
          type: integer
        mesh_local_prefix:
          type: string
        commissioning_psk:
          type: string
          writeOnly: true
        reporting_interval_sec:
          type: integer
    PasspointConfig:
      type: object
      properties:
        enabled:
          type: boolean
        hessid:
          type: string
        domain_name:
          type: array
          items:
            type: string
        roaming_consortium_oi:
          type: array
          items:
            type: string
        venue_group:
          type: integer
        venue_type:
          type: integer
        venue_name:
          type: string
        operator_friendly_name:
          type: string
        osu_ssid:
          type: string
        osu_providers:
          type: array
          items:
            type: object
            properties:
              osu_server_uri:
                type: string
              osu_friendly_name:
                type: string
              osu_nai:
                type: string
    TableReference:
      type: object
      properties:
        name:
          type: string
        domain:
          type: string
        is_root:
          type: boolean
        max_rows:
          type: string
        columns:
          type: integer
        description:
          type: string
    ProfileCreate:
      type: object
      required:
      - name
      properties:
        name:
          type: string
        icon:
          type: string
        color:
          type: string
        members:
          type: array
          items:
            type: string
        content_filtering:
          type: object
          properties:
            categories:
              type: array
              items:
                type: string
            safe_search:
              type: boolean
        time_limits:
          type: object
          properties:
            daily_minutes:
              type: integer
            bedtime_start:
              type: string
            bedtime_end:
              type: string
    ProfileUpdate:
      type: object
      properties:
        name:
          type: string
        icon:
          type: string
        color:
          type: string
        content_filtering:
          type: object
          properties:
            categories:
              type: array
              items:
                type: string
            safe_search:
              type: boolean
        time_limits:
          type: object
          properties:
            daily_minutes:
              type: integer
            bedtime_start:
              type: string
            bedtime_end:
              type: string
    FocusSchedule:
      type: object
      required:
      - name
      - schedule
      properties:
        name:
          type: string
        profile_id:
          type: string
        device_macs:
          type: array
          items:
            type: string
        schedule:
          type: object
          properties:
            timezone:
              type: string
            rules:
              type: array
              items:
                type: object
                properties:
                  days:
                    type: string
                  start:
                    type: string
                  end:
                    type: string
        block_app_categories:
          type: array
          items:
            type: string
        enabled:
          type: boolean
          default: true
    TrafficBoost:
      type: object
      properties:
        name:
          type: string
        profile_id:
          type: string
        device_macs:
          type: array
          items:
            type: string
        priority:
          type: string
          enum:
          - high
          - realtime
        schedule:
          type: object
          properties:
            timezone:
              type: string
            rules:
              type: array
              items:
                type: object
                properties:
                  days:
                    type: string
                  start:
                    type: string
                  end:
                    type: string
        enabled:
          type: boolean
          default: true
    IoTNetworkConfig:
      type: object
      properties:
        enabled:
          type: boolean
          default: true
        ssid:
          type: string
        security:
          $ref: '#/components/schemas/SecurityMode'
        isolation:
          type: object
          properties:
            isolate_from_primary:
              type: boolean
              default: true
            vlan_id:
              type: integer
        zigbee:
          type: object
          properties:
            coordinator_enabled:
              type: boolean
            channel:
              type: integer
            permit_join:
              type: boolean
        client_limit:
          type: integer
    LTEConfig:
      type: object
      properties:
        failover_enabled:
          type: boolean
        failover_mode:
          type: string
          enum:
          - automatic
          - manual
          - disabled
        apn:
          type: string
        apn_username:
          type: string
        apn_password:
          type: string
          writeOnly: true
        apn_auth_proto:
          type: string
          enum:
          - ''
          - CHAP
          - PAP
        apn_proto_type:
          type: string
          enum:
          - ''
          - ipv4
          - ipv6
          - ipv4v6
        active_sim_slot:
          type: integer
        force_lte:
          type: boolean
        data_limit_gb:
          type: integer
        notify_at_pct:
          type: integer
        nrBands:
          type: array
          items:
            type: integer
          description: Preferred NR (5G) bands for FWA operation (e.g. n77, n78, n41).
        nrMode:
          type: string
          enum:
          - NSA
          - SA
          - auto
          description: 5G network mode. NSA uses LTE anchor, SA is standalone 5G.
          default: auto
        mmWaveBeamId:
          type: integer
          description: Preferred mmWave beam ID for directional FWA.
        cbrsEnabled:
          type: boolean
          description: Enable CBRS (Citizens Broadband Radio Service) spectrum access.
        cbrsSasUrl:
          type: string
          description: CBRS SAS (Spectrum Access System) server URL.
        cbrsGrantExpiry:
          type: string
          format: date-time
          description: CBRS spectrum grant expiration timestamp.
        nrRsrp:
          type: integer
          description: 5G NR RSRP in dBm.
          readOnly: true
        nrRsrq:
          type: number
          description: 5G NR RSRQ in dB.
          readOnly: true
        nrSinr:
          type: number
          description: 5G NR SINR in dB.
          readOnly: true
    WANConfig:
      type: object
      properties:
        mode:
          type: string
          enum:
          - dhcp
          - static
          - pppoe
          - bridge
          - lte
          default: dhcp
        static_ip:
          type: string
        netmask:
          type: string
        gateway:
          type: string
        mtu:
          type: integer
          default: 1500
        dns_servers:
          type: array
          items:
            type: string
        vlan_id:
          type: integer
          minimum: 0
          maximum: 4094
        vlan_priority:
          type: integer
          minimum: 0
          maximum: 7
        pppoe:
          type: object
          properties:
            username:
              type: string
            password:
              type: string
              writeOnly: true
            service_name:
              type: string
            ac_name:
              type: string
        upnp_mode:
          type: string
          enum:
          - disabled
          - internal
          - external
          - internal_iptv
          - external_iptv
          default: disabled
        mac_clone:
          type: string
          pattern: ^([0-9A-Fa-f]{2}:){5}[0-9A-Fa-f]{2}$
      example:
        mode: pppoe
        mtu: 1492
        dns_servers:
        - 8.8.8.8
        - 1.1.1.1
        vlan_id: 10
        vlan_priority: 0
        pppoe:
          username: user@isp.example.com
          password: '****'
          service_name: ''
        upnp_mode: disabled
    UplinkHealth:
      type: object
      properties:
        interface:
          type: string
        type:
          type: string
          description: Uplink interface type from `Connection_Manager_Uplink.if_type`.
          enum:
          - eth
          - vif
          - vlan
          - pppoe
          - lte
          - gre
          - gre6
          - softwds
          - tap
          - bridge
          - unmanaged
        priority:
          type: integer
        is_active:
          type: boolean
        has_l2:
          type: boolean
        has_l3:
          type: boolean
        ipv4_status:
          type: string
          enum:
          - ready
          - active
          - inactive
          - blocked
        ipv6_status:
          type: string
          enum:
          - ready
          - active
          - inactive
          - blocked
        unreachable_link_count:
          type: integer
        unreachable_internet_count:
          type: integer
        unreachable_cloud_count:
          type: integer
        link_speed_mbps:
          type: integer
        ntp_synced:
          type: boolean
        jitter_ms:
          type: integer
          description: Jitter in ms
        packet_loss_pct:
          type: number
          description: Packet loss %
        mos_score:
          type: number
          description: MOS 1.0-5.0
        latency_ms:
          type: integer
          description: RTT latency in ms
        uptime_pct:
          type: number
          description: Uptime % last 24h
    MultiWANConfig:
      type: object
      properties:
        enabled:
          type: boolean
          default: false
        uplinks:
          type: array
          items:
            type: object
            properties:
              name:
                type: string
              type:
                type: string
                enum:
                - eth
                - pppoe
                - vlan
                - lte
                - gre
              priority:
                type: integer
              config:
                $ref: '#/components/schemas/WANConfig'
        failover:
          type: object
          properties:
            mode:
              type: string
              enum:
              - active_standby
              - load_balance
              - round_robin
              default: active_standby
            healthcheck_ip:
              type: string
            healthcheck_interval_sec:
              type: integer
              default: 30
            failover_threshold:
              type: integer
              default: 3
            failback_delay_sec:
              type: integer
              default: 300
      example:
        enabled: true
        uplinks:
        - name: primary-fiber
          type: eth
          priority: 100
        - name: backup-lte
          type: lte
          priority: 200
        failover:
          mode: active_standby
          healthcheck_ip: 8.8.8.8
          healthcheck_interval_sec: 30
          failover_threshold: 3
          failback_delay_sec: 300
    IGMPProxyConfig:
      type: object
      properties:
        igmp_proxy:
          type: string
          enum:
          - disabled
          - IGMPv1
          - IGMPv2
          - IGMPv3
          default: disabled
        mld_proxy:
          type: string
          enum:
          - disabled
          - MLDv1
          - MLDv2
          default: disabled
        upstream_interface:
          type: string
        downstream_interfaces:
          type: array
          items:
            type: string
        fast_leave:
          type: boolean
          default: false
        query_interval_sec:
          type: integer
          default: 125
    DNSConfig:
      type: object
      properties:
        mode:
          type: string
          enum:
          - auto
          - manual
          - isp
          default: auto
        primary:
          type: string
        secondary:
          type: string
        dns_over_tls:
          type: boolean
          default: false
        dot_server:
          type: string
        local_domain:
          type: string
        rebind_protection:
          type: boolean
          default: true
        cache_size:
          type: integer
          default: 10000
        static_hosts:
          type: array
          items:
            type: object
            properties:
              hostname:
                type: string
              ip:
                type: string
    DDNSConfig:
      type: object
      properties:
        enabled:
          type: boolean
          default: false
        provider:
          type: string
          enum:
          - dyndns
          - noip
          - cloudflare
          - custom
          - duckdns
          - afraid
        hostname:
          type: string
        username:
          type: string
        password:
          type: string
          writeOnly: true
        token:
          type: string
          writeOnly: true
        update_interval_sec:
          type: integer
          default: 600
        custom_url:
          type: string
    NTPConfig:
      type: object
      properties:
        servers:
          type: array
          items:
            type: string
          default:
          - pool.ntp.org
        sync_interval_sec:
          type: integer
          default: 3600
        timezone:
          type: string
          example: America/New_York
    NetworkTopology:
      type: object
      properties:
        nodes:
          type: array
          items:
            type: object
            properties:
              id:
                type: string
              serial:
                type: string
              role:
                type: string
                enum:
                - gateway
                - satellite
              model:
                type: string
              status:
                type: string
        links:
          type: array
          items:
            type: object
            properties:
              source:
                type: string
              target:
                type: string
              type:
                type: string
                enum:
                - ethernet
                - wifi_backhaul
                - ethernet_backhaul
              rssi_dbm:
                type: integer
              phy_rate_mbps:
                type: integer
              channel:
                type: integer
              band:
                type: string
    MeshTopology:
      type: object
      properties:
        gateway_id:
          type: string
        satellites:
          type: array
          items:
            type: object
            properties:
              node_id:
                type: string
              serial:
                type: string
              parent_id:
                type: string
              hop_count:
                type: integer
              backhaul_type:
                type: string
                enum:
                - wired
                - wireless_2g
                - wireless_5g
                - wireless_6g
                - wireless_5gl
                - wireless_5gu
              backhaul_rssi_dbm:
                type: integer
              backhaul_phy_rate_mbps:
                type: integer
              backhaul_channel:
                type: integer
              connected_clients:
                type: integer
              firmware_version:
                type: string
              uptime_sec:
                type: integer
              last_seen:
                type: string
                format: date-time
    LinkLayerNeighbor:
      type: object
      properties:
        local_interface:
          type: string
        remote_mac:
          type: string
        remote_name:
          type: string
        remote_port:
          type: string
        remote_model:
          type: string
        protocol:
          type: string
          enum:
          - lldp
          - cdp
          - unknown
    ISPDashboard:
      type: object
      properties:
        total_customers:
          type: integer
        active_customers:
          type: integer
        total_locations:
          type: integer
        online_locations:
          type: integer
        total_nodes:
          type: integer
        online_nodes:
          type: integer
        total_clients:
          type: integer
        avg_health_score:
          type: number
        threats_blocked:
          type: integer
        avg_speedtest_dl_mbps:
          type: number
        avg_speedtest_ul_mbps:
          type: number
        bandwidth_utilization_pct:
          type: number
        top_devices:
          type: array
          items:
            type: object
            properties:
              model:
                type: string
              count:
                type: integer
        top_applications:
          type: array
          items:
            type: object
            properties:
              name:
                type: string
              category:
                type: string
              bandwidth_pct:
                type: number
        customer_growth:
          type: array
          items:
            type: object
            properties:
              date:
                type: string
                format: date
              new:
                type: integer
              churned:
                type: integer
              total:
                type: integer
    NetworkHealthScore:
      type: object
      properties:
        overall:
          type: integer
          minimum: 0
          maximum: 100
        categories:
          type: object
          properties:
            connectivity:
              type: integer
            wifi_performance:
              type: integer
            security:
              type: integer
            latency:
              type: integer
            throughput:
              type: integer
        issues:
          type: array
          items:
            type: object
            properties:
              category:
                type: string
              severity:
                type: string
                enum:
                - info
                - warning
                - critical
              description:
                type: string
              recommendation:
                type: string
    BandwidthReport:
      type: object
      properties:
        period:
          type: string
        resolution:
          type: string
        total_download_gb:
          type: number
        total_upload_gb:
          type: number
        peak_download_mbps:
          type: number
        peak_upload_mbps:
          type: number
        series:
          type: array
          items:
            type: object
            properties:
              timestamp:
                type: string
                format: date-time
              download_mbps:
                type: number
              upload_mbps:
                type: number
        by_ssid:
          type: array
          items:
            type: object
            properties:
              ssid:
                type: string
              download_gb:
                type: number
              upload_gb:
                type: number
        by_band:
          type: array
          items:
            type: object
            properties:
              band:
                type: string
              download_gb:
                type: number
              upload_gb:
                type: number
    ClientAnalytics:
      type: object
      properties:
        period:
          type: string
        unique_clients:
          type: integer
        avg_daily_clients:
          type: number
        new_clients:
          type: integer
        by_os:
          type: array
          items:
            type: object
            properties:
              os:
                type: string
              count:
                type: integer
              pct:
                type: number
        by_device_type:
          type: array
          items:
            type: object
            properties:
              type:
                type: string
              count:
                type: integer
              pct:
                type: number
        by_vendor:
          type: array
          items:
            type: object
            properties:
              vendor:
                type: string
              count:
                type: integer
              pct:
                type: number
        signal_distribution:
          type: array
          items:
            type: object
            properties:
              range:
                type: string
              count:
                type: integer
              pct:
                type: number
    WiFiAnalytics:
      type: object
      properties:
        period:
          type: string
        avg_channel_utilization_pct:
          type: number
        avg_noise_dbm:
          type: integer
        channel_changes:
          type: integer
        dfs_events:
          type: integer
        by_radio:
          type: array
          items:
            type: object
            properties:
              band:
                type: string
              channel:
                type: integer
              utilization_pct:
                type: number
              noise_dbm:
                type: integer
              clients_avg:
                type: number
              airtime_efficiency_pct:
                type: number
        interference_score:
          type: integer
          minimum: 0
          maximum: 100
    AppAnalytics:
      type: object
      properties:
        period:
          type: string
        applications:
          type: array
          items:
            type: object
            properties:
              name:
                type: string
              category:
                type: string
              download_bytes:
                type: integer
              upload_bytes:
                type: integer
              active_clients:
                type: integer
              bandwidth_pct:
                type: number
        categories:
          type: array
          items:
            type: object
            properties:
              name:
                type: string
              download_bytes:
                type: integer
              upload_bytes:
                type: integer
              bandwidth_pct:
                type: number
    AuditLogEntry:
      type: object
      properties:
        id:
          type: string
        timestamp:
          type: string
          format: date-time
        actor:
          type: string
        actor_type:
          type: string
          enum:
          - user
          - api_key
          - system
          - end_user
        action:
          type: string
          enum:
          - create
          - update
          - delete
          - read
          - execute
          - login
          - logout
        resource:
          type: string
        resource_id:
          type: string
        changes:
          type: object
          properties:
            before:
              type: object
            after:
              type: object
        ip_address:
          type: string
        user_agent:
          type: string
      example:
        id: audit_20260629_001
        timestamp: '2026-06-29T14:30:00Z'
        actor: api_key:ak_pref_abcd
        actor_type: api_key
        action: update
        resource: SSID
        resource_id: SmithWiFi
        changes:
          before:
            security:
              mode: wpa2-psk
          after:
            security:
              mode: wpa3-compat
        ip_address: 203.0.113.100
        user_agent: ISP-OSS/2.1
    ComplianceStatus:
      type: object
      properties:
        country:
          type: string
        regulatory_domain:
          type: string
        status:
          type: string
          enum:
          - compliant
          - non_compliant
          - unknown
        radios:
          type: array
          items:
            type: object
            properties:
              band:
                type: string
              allowed_channels:
                type: array
                items:
                  type: integer
              current_channel:
                type: integer
              max_tx_power_dbm:
                type: integer
              current_tx_power_dbm:
                type: integer
              dfs_required:
                type: boolean
              dfs_enabled:
                type: boolean
              compliant:
                type: boolean
              violations:
                type: array
                items:
                  type: string
    DataResidencyConfig:
      type: object
      properties:
        region:
          type: string
          enum:
          - us
          - eu
          - apac
          - latam
          - mea
        telemetry_residency:
          type: string
          enum:
          - same_as_data
          - custom
        telemetry_region:
          type: string
        pii_excluded:
          type: boolean
          default: false
        retention_days:
          type: integer
          default: 90
        gdpr_enabled:
          type: boolean
          default: false
    Tenant:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        domain:
          type: string
        status:
          type: string
          enum:
          - active
          - suspended
          - cancelled
          - trial
        tier:
          type: string
          enum:
          - sandbox
          - starter
          - growth
          - enterprise
          - unlimited
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        limits:
          type: object
          properties:
            max_customers:
              type: integer
            max_locations_per_customer:
              type: integer
            max_nodes_per_location:
              type: integer
            max_api_keys:
              type: integer
            rate_limit_rps:
              type: integer
    TenantCreate:
      type: object
      required:
      - name
      - domain
      properties:
        name:
          type: string
        domain:
          type: string
        tier:
          type: string
          enum:
          - sandbox
          - starter
          - growth
          - enterprise
          - unlimited
          default: sandbox
        contact_email:
          type: string
        contact_name:
          type: string
      example:
        name: Example ISP Inc.
        domain: example-isp.com
        tier: growth
        contact_email: ops@example-isp.com
        contact_name: John Doe
    TenantUpdate:
      type: object
      properties:
        name:
          type: string
        status:
          type: string
          enum:
          - active
          - suspended
          - cancelled
        tier:
          type: string
    ApiKey:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        prefix:
          type: string
          description: First 4 chars of key for identification
        tenant_id:
          type: string
        roles:
          type: array
          items:
            type: string
            enum:
            - admin
            - read_only
            - noc
            - support_tier1
            - support_tier2
            - billing
            - provisioning
        scopes:
          type: array
          items:
            type: string
        last_used:
          type: string
          format: date-time
        expires_at:
          type: string
          format: date-time
        created_at:
          type: string
          format: date-time
    ApiKeyCreate:
      type: object
      required:
      - name
      - tenant_id
      properties:
        name:
          type: string
        tenant_id:
          type: string
        roles:
          type: array
          items:
            type: string
            enum:
            - admin
            - read_only
            - noc
            - support_tier1
            - support_tier2
            - billing
            - provisioning
          default:
          - admin
        expires_in_days:
          type: integer
          default: 365
      example:
        name: production-oss-integration
        tenant_id: tnt_abc123
        roles:
        - admin
        expires_in_days: 365
    RateLimitConfig:
      type: object
      properties:
        per_second:
          type: integer
          default: 100
        per_minute:
          type: integer
          default: 5000
        burst_multiplier:
          type: number
          default: 2
        endpoints:
          type: object
          additionalProperties:
            type: object
            properties:
              per_second:
                type: integer
              per_minute:
                type: integer
        notification_threshold_pct:
          type: integer
          default: 85
    RemoteAccessConfig:
      type: object
      properties:
        provider:
          type: string
          enum:
          - cloudflare_tunnel
          - ngrok
          - custom
          default: cloudflare_tunnel
        authToken:
          type: string
          writeOnly: true
        localService:
          type: string
          description: Local service address to expose (e.g. 127.0.0.1:8080)
        remoteUrl:
          type: string
          format: uri
          description: Public-facing URL for the tunnel
        tlsEnabled:
          type: boolean
          default: true
        ingressRules:
          type: array
          description: Access control rules for the tunnel
          items:
            type: object
            properties:
              path:
                type: string
              allow:
                type: array
                items:
                  type: string
        enabled:
          type: boolean
          default: false
    SubAccountCreate:
      type: object
      required:
      - email
      - roles
      properties:
        email:
          type: string
        name:
          type: string
        roles:
          type: array
          items:
            type: string
            enum:
            - admin
            - read_only
            - noc
            - support_tier1
            - support_tier2
            - billing
            - provisioning
        customer_ids:
          type: array
          items:
            type: string
          description: Scope to specific customers
    AppUser:
      type: object
      properties:
        id:
          type: string
        email:
          type: string
        name:
          type: string
        phone:
          type: string
        mfa_enabled:
          type: boolean
        locations:
          type: array
          items:
            type: object
            properties:
              id:
                type: string
              name:
                type: string
              role:
                type: string
                enum:
                - owner
                - admin
                - member
      example:
        id: usr_n1o2p3
        email: jane.smith@example.com
        name: Jane Smith
        phone: +1-555-0100
        mfa_enabled: true
        locations:
        - id: loc_x9y8z7
          name: Smith Residence
          role: owner
        - id: loc_a1b2c3
          name: Beach House
          role: admin
    AppLocation:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        type:
          type: string
          enum:
          - home
          - business
        status:
          type: string
          enum:
          - online
          - offline
          - degraded
        online_nodes:
          type: integer
        total_nodes:
          type: integer
        connected_clients:
          type: integer
        thumbnail_url:
          type: string
    AppLocationStatus:
      type: object
      properties:
        location_id:
          type: string
        online:
          type: boolean
        gateway_status:
          type: object
          properties:
            status:
              type: string
              enum:
              - online
              - offline
              - provisioning
              - error
            uptime_sec:
              type: integer
            cpu_load_pct:
              type: integer
            memory_free_mb:
              type: integer
        internet:
          type: object
          properties:
            connected:
              type: boolean
            wan_ip:
              type: string
            wan_type:
              type: string
        speedtest_last:
          type: object
          properties:
            download_mbps:
              type: number
            upload_mbps:
              type: number
            latency_ms:
              type: number
            timestamp:
              type: string
              format: date-time
        security_summary:
          type: object
          properties:
            threats_today:
              type: integer
            blocked_total:
              type: integer
        active_clients:
          type: integer
        guest_clients:
          type: integer
    AppNotificationPrefs:
      type: object
      properties:
        push_token:
          type: string
          writeOnly: true
        platform:
          type: string
          enum:
          - ios
          - android
        new_device_connect:
          type: boolean
          default: true
        security_threat:
          type: boolean
          default: true
        node_offline:
          type: boolean
          default: true
        firmware_update:
          type: boolean
          default: true
        speedtest_complete:
          type: boolean
          default: false
        guest_login:
          type: boolean
          default: false
        weekly_report:
          type: boolean
          default: true
    TR069BridgeStatus:
      type: object
      properties:
        enabled:
          type: boolean
        acs_url:
          type: string
        connection_status:
          type: string
          enum:
          - connected
          - disconnected
          - error
          - unknown
        last_inform:
          type: string
          format: date-time
        inform_interval_sec:
          type: integer
        cpe_id:
          type: string
        provisioning_code:
          type: string
    TR069BridgeConfig:
      type: object
      properties:
        enabled:
          type: boolean
          default: false
        acs_url:
          type: string
        acs_username:
          type: string
        acs_password:
          type: string
          writeOnly: true
        periodic_inform_interval_sec:
          type: integer
          default: 3600
        connection_request_username:
          type: string
        connection_request_password:
          type: string
          writeOnly: true
        connection_request_port:
          type: integer
          default: 7547
        stun_enable:
          type: boolean
        stun_server:
          type: string
        nat_detected:
          type: boolean
    FastRoamingConfig:
      type: object
      properties:
        ft_80211r:
          type: boolean
          default: false
        ft_over_ds:
          type: boolean
          default: false
        ft_mobility_domain_id:
          type: integer
        neighbor_report_80211k:
          type: boolean
          default: true
        bss_transition_80211v:
          type: boolean
          default: true
        reassociation_deadline_ms:
          type: integer
          default: 1000
        pmk_caching:
          type: boolean
          default: true
        okc:
          type: boolean
          default: true
      example:
        ft_80211r: true
        ft_over_ds: true
        ft_mobility_domain_id: 1
        neighbor_report_80211k: true
        bss_transition_80211v: true
        pmk_caching: true
        okc: true
    WiFiSchedule:
      type: object
      required:
      - ssid
      - rules
      properties:
        id:
          type: string
        ssid:
          type: string
        enabled:
          type: boolean
          default: true
        rules:
          type: array
          items:
            type: object
            required:
            - days
            - start
            - end
            properties:
              days:
                type: array
                items:
                  type: string
                  enum:
                  - mon
                  - tue
                  - wed
                  - thu
                  - fri
                  - sat
                  - sun
              start:
                type: string
                example: 06:00
              end:
                type: string
                example: '23:00'
        timezone:
          type: string
    MLOConfig:
      type: object
      properties:
        enabled:
          type: boolean
          default: false
        mode:
          type: string
          enum:
          - str
          - emlsr
          - nstr
          default: str
        bands:
          type: array
          items:
            type: string
            enum:
            - 2.4G
            - 5G
            - 6G
          default:
          - 5G
          - 6G
        max_simultaneous_links:
          type: integer
          minimum: 2
          maximum: 3
          default: 2
      example:
        enabled: true
        mode: str
        bands:
        - 5G
        - 6G
        max_simultaneous_links: 2
    MinRSSIConfig:
      type: object
      properties:
        enabled:
          type: boolean
          default: false
        threshold_2g_dbm:
          type: integer
          default: -82
        threshold_5g_dbm:
          type: integer
          default: -78
        threshold_6g_dbm:
          type: integer
          default: -75
        probe_response_threshold_dbm:
          type: integer
          default: -85
        grace_period_sec:
          type: integer
          default: 15
    ClientConnectionQuality:
      type: object
      properties:
        id:
          type: string
          readOnly: true
        deviceId:
          type: string
        overallScore:
          type: integer
          minimum: 0
          maximum: 100
          description: Composite connection quality score, weighted across all sub-scores
        signalQuality:
          type: object
          properties:
            rssiDbm:
              type: integer
            snrDb:
              type: number
            mcsIndex:
              type: integer
            spatialStreams:
              type: integer
            phyRateMbps:
              type: number
            band:
              type: string
              enum:
              - 2.4GHz
              - 5GHz
              - 6GHz
            score:
              type: integer
              minimum: 0
              maximum: 100
        linkQuality:
          type: object
          properties:
            retransmissionRatePct:
              type: number
            packetErrorRatePct:
              type: number
            frameErrorRatePct:
              type: number
            negotiatedRateMbps:
              type: number
            score:
              type: integer
              minimum: 0
              maximum: 100
        airQuality:
          type: object
          properties:
            channelUtilizationPct:
              type: number
            interferenceLevel:
              type: string
              enum:
              - low
              - medium
              - high
            contentionRatio:
              type: number
            freeAirPercent:
              type: number
            coChannelApCount:
              type: integer
            noiseFloorDbm:
              type: integer
            score:
              type: integer
              minimum: 0
              maximum: 100
        applicationReadiness:
          type: object
          properties:
            tcpLatencyMs:
              type: number
            dnsLatencyMs:
              type: number
            httpLatencyMs:
              type: number
            tlsHandshakeMs:
              type: number
            throughputAvailabilityMbps:
              type: number
            score:
              type: integer
              minimum: 0
              maximum: 100
        trend:
          type: string
          enum:
          - improving
          - stable
          - degrading
        timestamp:
          type: string
          format: date-time
          readOnly: true
    ClientIdentity:
      type: object
      properties:
        id:
          type: string
          readOnly: true
        deviceId:
          type: string
          description: Primary device record ID in the system
        primaryMac:
          type: string
          description: Canonical MAC address used for this identity
        mergedMacs:
          type: array
          description: All MAC addresses observed for this device, including randomized/private
            MACs
          items:
            type: string
        confidence:
          type: integer
          minimum: 0
          maximum: 100
          description: Confidence level of the identity merge (0-100)
        signals:
          type: array
          description: Evidence signals used to establish this identity
          items:
            $ref: '#/components/schemas/ClientIdentitySignal'
        firstSeenAt:
          type: string
          format: date-time
          readOnly: true
        lastSeenAt:
          type: string
          format: date-time
          readOnly: true
        lastUpdatedAt:
          type: string
          format: date-time
          readOnly: true
        manualOverride:
          type: boolean
          description: True if a user manually merged or split this identity
    ClientIdentitySignal:
      type: object
      properties:
        type:
          type: string
          enum:
          - dhcp_fingerprint
          - dhcp_hostname
          - oui_match
          - traffic_pattern
          - os_fingerprint
          - ml_prediction
          - persistent_identifier
          - manual
          description: Method used to associate this MAC with the identity
        value:
          type: string
          description: The signal value that matched (e.g. DHCP fingerprint string,
            hostname, OS name)
        confidence:
          type: integer
          minimum: 0
          maximum: 100
        firstSeenAt:
          type: string
          format: date-time
    ClientIsolationConfig:
      type: object
      properties:
        per_ssid:
          type: array
          items:
            type: object
            properties:
              ssid:
                type: string
              client_isolation:
                type: boolean
                default: false
              isolate_from_primary:
                type: boolean
                default: false
              isolate_from_other_ssids:
                type: boolean
                default: false
              vlan_id:
                type: integer
    ProvisioningPolicy:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        match_type:
          type: string
          enum:
          - oui
          - serial_pattern
          - model
          - all
        match_value:
          type: string
        action:
          type: string
          enum:
          - assign_customer
          - assign_location
          - apply_template
          - auto_claim
        customer_id:
          type: string
        location_id:
          type: string
        template_id:
          type: string
        enabled:
          type: boolean
        priority:
          type: integer
        created_at:
          type: string
          format: date-time
    ProvisioningPolicyCreate:
      type: object
      required:
      - name
      - match_type
      - match_value
      - action
      properties:
        name:
          type: string
        match_type:
          type: string
          enum:
          - oui
          - serial_pattern
          - model
          - all
        match_value:
          type: string
          description: OUI prefix (e.g. "A0:B1:C2"), regex for serial, or model string
        action:
          type: string
          enum:
          - assign_customer
          - assign_location
          - apply_template
          - auto_claim
        customer_id:
          type: string
        location_id:
          type: string
        template_id:
          type: string
        enabled:
          type: boolean
          default: true
        priority:
          type: integer
          default: 100
    ProvisioningPolicyUpdate:
      type: object
      properties:
        name:
          type: string
        match_value:
          type: string
        enabled:
          type: boolean
        priority:
          type: integer
        customer_id:
          type: string
        location_id:
          type: string
        template_id:
          type: string
    TrafficStats:
      type: object
      properties:
        interfaces:
          type: array
          items:
            type: object
            properties:
              name:
                type: string
              rx_bytes_sec:
                type: integer
              tx_bytes_sec:
                type: integer
              rx_packets_sec:
                type: integer
              tx_packets_sec:
                type: integer
              rx_errors:
                type: integer
              tx_errors:
                type: integer
              rx_dropped:
                type: integer
              tx_dropped:
                type: integer
        timestamp:
          type: string
          format: date-time
    AnalyticsAgentConfig:
      type: object
      properties:
        provider:
          type: string
          enum:
          - telegraf
          - prometheus_exporter
          - custom
          default: telegraf
        endpoint:
          type: string
          format: uri
          description: Remote collector endpoint (e.g. InfluxDB URL, Prometheus remote-write
            endpoint)
        scrapeIntervalSeconds:
          type: integer
          minimum: 10
          default: 60
        metricsFilter:
          type: object
          description: Include/exclude patterns for metric selection
          properties:
            include:
              type: array
              items:
                type: string
            exclude:
              type: array
              items:
                type: string
        authToken:
          type: string
          writeOnly: true
        enabled:
          type: boolean
          default: false
    AnomalyEvent:
      type: object
      properties:
        id:
          type: string
        type:
          type: string
          enum:
          - throughput_drop
          - latency_spike
          - client_disconnect
          - channel_congestion
          - reboot_loop
          - excessive_dfs
          - high_cpu
          - low_memory
        severity:
          type: string
          enum:
          - info
          - warning
          - critical
        detected_at:
          type: string
          format: date-time
        resolved_at:
          type: string
          format: date-time
        description:
          type: string
        metrics:
          type: object
        recommendation:
          type: string
        auto_resolved:
          type: boolean
    BillingStatus:
      type: object
      properties:
        customer_id:
          type: string
        subscription_status:
          type: string
          enum:
          - active
          - past_due
          - cancelled
          - suspended
          - trial
        plan:
          type: string
        service_level:
          type: string
        billing_cycle:
          type: string
          enum:
          - monthly
          - annual
        current_period_start:
          type: string
          format: date-time
        current_period_end:
          type: string
          format: date-time
        data_usage_gb:
          type: number
        data_cap_gb:
          type: number
        overage_gb:
          type: number
        locations:
          type: integer
          description: Number of active locations
        nodes:
          type: integer
          description: Number of claimed nodes
      example:
        customer_id: cust_a1b2c3d4
        subscription_status: active
        plan: homepass_premium
        service_level: fullService
        billing_cycle: monthly
        current_period_start: '2026-06-01T00:00:00Z'
        current_period_end: '2026-07-01T00:00:00Z'
        data_usage_gb: 245.5
        data_cap_gb: 1024
        overage_gb: 0
        locations: 2
        nodes: 4
    DeviceGroup:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        description:
          type: string
        color:
          type: string
        device_macs:
          type: array
          items:
            type: string
        node_ids:
          type: array
          items:
            type: string
        tags:
          type: array
          items:
            type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    DeviceGroupCreate:
      type: object
      required:
      - name
      properties:
        name:
          type: string
        description:
          type: string
        color:
          type: string
        device_macs:
          type: array
          items:
            type: string
        node_ids:
          type: array
          items:
            type: string
        tags:
          type: array
          items:
            type: string
    ServiceTier:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        description:
          type: string
        tier_level:
          type: integer
        features:
          type: object
          properties:
            adaptive_wifi:
              type: boolean
            guest_network:
              type: boolean
            iot_network:
              type: boolean
            advanced_security:
              type: boolean
            content_filtering:
              type: boolean
            parental_controls:
              type: boolean
            lte_backup:
              type: boolean
            motion_sense:
              type: boolean
            thread_matter:
              type: boolean
            workpass:
              type: boolean
            captive_portal:
              type: boolean
            employee_access:
              type: boolean
            analytics:
              type: boolean
            prioritized_support:
              type: boolean
            third_party_agents:
              type: boolean
            custom_agent_config:
              type: boolean
        max_nodes:
          type: integer
        max_ssids:
          type: integer
        max_profiles:
          type: integer
    ServiceTierCreate:
      type: object
      required:
      - name
      properties:
        name:
          type: string
        description:
          type: string
        tier_level:
          type: integer
          default: 1
        max_nodes:
          type: integer
          default: 10
        max_ssids:
          type: integer
          default: 5
        max_profiles:
          type: integer
          default: 10
    RADIUSConfig:
      type: object
      properties:
        enabled:
          type: boolean
          default: false
        servers:
          type: array
          items:
            type: object
            properties:
              name:
                type: string
              ip:
                type: string
              auth_port:
                type: integer
                default: 1812
              acct_port:
                type: integer
                default: 1813
              secret:
                type: string
                writeOnly: true
              priority:
                type: integer
                default: 1
              timeout_sec:
                type: integer
                default: 5
              retries:
                type: integer
                default: 3
        accounting_enabled:
          type: boolean
          default: true
        accounting_interval_sec:
          type: integer
          default: 300
        dynamic_authorization:
          type: boolean
          default: false
          description: Enable Change-of-Authorization (CoA) for dynamic policy updates
        coa_port:
          type: integer
          default: 3799
        nas_identifier:
          type: string
        called_station_id:
          type: string
        socialLogin:
          $ref: '#/components/schemas/SocialLoginConfig'
          description: RADIUS-based social login for guest WiFi.
    FlowConfig:
      type: object
      properties:
        enabled:
          type: boolean
          default: false
        capture_method:
          type: string
          enum:
          - email
          - phone
          - social_login
          - none
          default: email
        integrations:
          type: object
          properties:
            mailchimp_api_key:
              type: string
              writeOnly: true
            mailchimp_list_id:
              type: string
            hubspot_enabled:
              type: boolean
              default: false
            hubspot_api_key:
              type: string
              writeOnly: true
            salesforce_enabled:
              type: boolean
              default: false
            webhook_url:
              type: string
        data_collection_consent:
          type: boolean
          default: true
        privacy_policy_url:
          type: string
    FlowCampaign:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        active:
          type: boolean
          default: true
        capture_method:
          type: string
          enum:
          - email
          - phone
          - social_login
          - none
          default: email
        offer:
          type: string
        terms_url:
          type: string
        captured_contacts:
          type: integer
        conversion_rate_pct:
          type: number
        start_date:
          type: string
          format: date
        end_date:
          type: string
          format: date
    BusinessAnalytics:
      type: object
      properties:
        period:
          type: object
          properties:
            start:
              type: string
            end:
              type: string
        footfall:
          type: object
          properties:
            total_unique:
              type: integer
            daily_avg:
              type: number
            peak_day:
              type: string
            peak_hour:
              type: string
            return_rate_pct:
              type: number
        sessions:
          type: object
          properties:
            total:
              type: integer
            avg_duration_min:
              type: integer
            peak_concurrent:
              type: integer
        devices:
          type: object
          properties:
            apple_pct:
              type: number
            android_pct:
              type: number
            laptop_pct:
              type: number
            mobile_pct:
              type: number
            tablet_pct:
              type: number
        engagement:
          type: object
          properties:
            social_logins:
              type: object
              additionalProperties:
                type: number
            opt_in_marketing:
              type: integer
    MACPool:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        description:
          type: string
        mac_ranges:
          type: array
          items:
            type: object
            properties:
              start_mac:
                type: string
              end_mac:
                type: string
              count:
                type: integer
        assigned_count:
          type: integer
        available_count:
          type: integer
        customer_id:
          type: string
    MACPoolCreate:
      type: object
      required:
      - name
      - mac_ranges
      properties:
        name:
          type: string
        description:
          type: string
        mac_ranges:
          type: array
          items:
            type: object
            required:
            - start_mac
            - end_mac
            properties:
              start_mac:
                type: string
              end_mac:
                type: string
        customer_id:
          type: string
    CommissionRule:
      type: object
      properties:
        partner_id:
          type: string
          format: uuid
        share_pct:
          type: number
        flat_rate_per_plan:
          type: number
        settlement_frequency:
          type: string
          enum:
          - weekly
          - biweekly
          - monthly
          - quarterly
        min_payout:
          type: number
        active:
          type: boolean
        updated_at:
          type: string
          format: date-time
    CommissionRuleUpdate:
      type: object
      properties:
        share_pct:
          type: number
          minimum: 0
          maximum: 100
        flat_rate_per_plan:
          type: number
          minimum: 0
        settlement_frequency:
          type: string
          enum:
          - weekly
          - biweekly
          - monthly
          - quarterly
        min_payout:
          type: number
          minimum: 0
        active:
          type: boolean
    PartnerWallet:
      type: object
      properties:
        partner_id:
          type: string
          format: uuid
        current_balance:
          type: number
        total_earned:
          type: number
        total_paid_out:
          type: number
        pending_settlement:
          type: number
        next_settlement_date:
          type: string
          format: date
        transactions:
          type: array
          items:
            type: object
            properties:
              id:
                type: string
                format: uuid
              type:
                type: string
                enum:
                - credit
                - debit
              amount:
                type: number
              description:
                type: string
              reference:
                type: string
              timestamp:
                type: string
                format: date-time
    CustomerGroup:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        description:
          type: string
        parent_group_id:
          type: string
        member_type:
          type: string
        tags:
          type: string
        query_filter:
          type: string
        policy_set_id:
          type: string
        member_count:
          type: string
        created_at:
          type: string
        updated_at:
          type: string
    CustomerGroupCreate:
      type: object
      properties:
        name:
          type: string
        description:
          type: string
        parent_group_id:
          type: string
        member_type:
          type: string
        tags:
          type: string
        query_filter:
          type: string
    GroupMember:
      type: object
      properties:
        id:
          type: string
        type:
          type: string
        name:
          type: string
        tags:
          type: string
        added_at:
          type: string
    GroupMemberAdd:
      type: object
      properties:
        ids:
          type: string
        tag_selectors:
          type: string
    GroupStats:
      type: object
      properties:
        total_members:
          type: string
        nodes_online:
          type: string
        total_traffic_bytes_up:
          type: string
        total_traffic_bytes_down:
          type: string
        avg_health_score:
          type: string
        device_types:
          type: string
        period:
          type: string
        generated_at:
          type: string
    Deployment:
      type: object
      description: A named deployment environment that doubles as a device group.
        Each deployment has a tier that determines protection rules — tier 1 (production)
        devices cannot be reassigned to tier 2 (development) deployments.
      properties:
        id:
          type: string
          readOnly: true
        name:
          type: string
          description: Unique deployment name (e.g. "production", "development-east")
        tier:
          type: integer
          description: Protection tier (1=production, 2=development)
          enum:
          - 1
          - 2
        description:
          type: string
        isProtected:
          type: boolean
          description: True when tier is 1 (production). Protected deployments cannot
            be deleted without confirmation and devices within them are subject to
            downgrade restrictions.
          readOnly: true
        nodeCount:
          type: integer
          readOnly: true
        createdAt:
          type: string
          format: date-time
          readOnly: true
        updatedAt:
          type: string
          format: date-time
          readOnly: true
    DeploymentCreate:
      type: object
      required:
      - name
      - tier
      properties:
        name:
          type: string
          description: Unique deployment name
        tier:
          type: integer
          description: Protection tier (1=production, 2=development)
          enum:
          - 1
          - 2
        description:
          type: string
    DeploymentUpdate:
      type: object
      properties:
        name:
          type: string
        description:
          type: string
        tier:
          type: integer
          description: Protection tier (1=production, 2=development). Only changeable
            when the deployment has no members; assigning a tier to a deployment with
            members violates the protection rule and returns 422.
          enum:
          - 1
          - 2
    DeploymentMember:
      type: object
      description: A node assigned to a deployment. Each node belongs to exactly one
        deployment at a time.
      properties:
        id:
          type: string
          readOnly: true
        deploymentId:
          type: string
          readOnly: true
        nodeId:
          type: string
        serialNumber:
          type: string
          description: Inventory serial number of the underlying device
        locationId:
          type: string
          readOnly: true
        customerId:
          type: string
          readOnly: true
        addedAt:
          type: string
          format: date-time
          readOnly: true
    DeploymentMemberAdd:
      type: object
      description: Bulk addition request for deployment members. Supports optional
        force flag to override protection rules.
      required:
      - nodeIds
      properties:
        nodeIds:
          type: array
          description: Node identifiers to add to the deployment
          items:
            type: string
        forceAssignment:
          type: boolean
          default: false
          description: When true, bypasses the protection rule that prevents assigning
            production-tier devices to a development deployment. Requires elevated
            privileges.
    NodeOnboardingStatus:
      type: object
      description: Current onboarding status for a node, including its deployment
        assignment when complete.
      properties:
        nodeId:
          type: string
          readOnly: true
        status:
          type: string
          enum:
          - pending
          - in_progress
          - completed
          - failed
        deploymentId:
          type: string
          nullable: true
          description: Deployment the node was onboarded into (null if not assigned)
        deploymentName:
          type: string
          nullable: true
          readOnly: true
        deploymentTier:
          type: integer
          nullable: true
          readOnly: true
          description: Tier of the assigned deployment (1=production, 2=development)
        startedAt:
          type: string
          format: date-time
        completedAt:
          type: string
          format: date-time
          nullable: true
        errors:
          type: array
          items:
            type: string
          description: Error messages if status is failed
    NodeOnboardingRequest:
      type: object
      description: Request to onboard a node into a deployment.
      required:
      - deploymentId
      properties:
        deploymentId:
          type: string
          description: Target deployment identifier
        forceAssignment:
          type: boolean
          default: false
          description: When true, bypasses the protection rule that prevents onboarding
            production-tier devices into a development deployment. Requires elevated
            privileges.
    DeviceDeploymentAssignment:
      type: object
      description: Current deployment assignment for an inventory device.
      properties:
        serial:
          type: string
          readOnly: true
        deploymentId:
          type: string
          nullable: true
          description: Current deployment identifier (null if unassigned)
        deploymentName:
          type: string
          nullable: true
          readOnly: true
        deploymentTier:
          type: integer
          nullable: true
          readOnly: true
          description: Tier of the assigned deployment (1=production, 2=development)
        previousDeploymentId:
          type: string
          nullable: true
          readOnly: true
          description: Previous deployment before the last reassignment, for audit
        assignedAt:
          type: string
          format: date-time
          readOnly: true
        updatedAt:
          type: string
          format: date-time
          readOnly: true
    DeviceDeploymentAssignmentCreate:
      type: object
      description: Request to assign an inventory device to a deployment.
      required:
      - deploymentId
      properties:
        deploymentId:
          type: string
          description: Target deployment identifier
        forceAssignment:
          type: boolean
          default: false
          description: When true, bypasses the protection rule that prevents assigning
            production-tier devices to a development deployment. Requires elevated
            privileges.
    InheritableConfig:
      type: object
      properties:
        scope:
          type: string
        scope_id:
          type: string
        effective:
          type: string
        inherited_from:
          type: string
        overrides:
          type: string
        merge_strategy:
          type: string
        applied_at:
          type: string
    InheritableConfigUpdate:
      type: object
      properties:
        overrides:
          type: string
        merge_strategy:
          type: string
    InventoryDevice:
      type: object
      properties:
        serial:
          type: string
        mac:
          type: string
        model:
          type: string
        manufacturer:
          type: string
        firmware_version:
          type: string
        hardware_version:
          type: string
        status:
          type: string
        current_location_id:
          type: string
        warehouse_id:
          type: string
        customer_id:
          type: string
        node_id:
          type: string
        deployment_id:
          type: string
          nullable: true
          description: Current deployment assignment (null if unassigned)
        deployment_tier:
          type: integer
          nullable: true
          description: Tier of the assigned deployment (1=production, 2=development,
            null if unassigned)
        cost:
          type: string
        batch_id:
          type: string
        supplier_id:
          type: string
        purchase_order_id:
          type: string
        warranty_expiry:
          type: string
        created_at:
          type: string
        updated_at:
          type: string
        lifecycleState:
          type: string
          enum:
          - active
          - eol
          - decommissioned
          - rma
          default: active
          description: Device lifecycle state. 'eol' marks end-of-life (no more updates).
            'decommissioned' marks retired devices. 'rma' is for return/repair process.
        lifecycleChangedAt:
          type: string
          format: date-time
          readOnly: true
          description: When the lifecycle state was last changed.
    InventoryDeviceCreate:
      type: object
      properties:
        serial:
          type: string
        mac:
          type: string
        model:
          type: string
        manufacturer:
          type: string
        firmware_version:
          type: string
        hardware_version:
          type: string
        cost:
          type: string
        warehouse_id:
          type: string
        purchase_order_id:
          type: string
        warranty_expiry:
          type: string
    InventoryDeviceUpdate:
      type: object
      properties:
        status:
          type: string
        current_location_id:
          type: string
        warehouse_id:
          type: string
        customer_id:
          type: string
        node_id:
          type: string
        warranty_expiry:
          type: string
        note:
          type: string
    PurchaseOrder:
      type: object
      properties:
        id:
          type: string
        order_number:
          type: string
        supplier:
          type: string
        status:
          type: string
        line_items:
          type: string
        notes:
          type: string
        ordered_at:
          type: string
        received_at:
          type: string
        created_at:
          type: string
    PurchaseOrderCreate:
      type: object
      properties:
        order_number:
          type: string
        supplier:
          type: string
        line_items:
          type: string
        notes:
          type: string
    PurchaseOrderUpdate:
      type: object
      properties:
        status:
          type: string
        goods_receipt:
          type: string
        notes:
          type: string
    RMACase:
      type: object
      properties:
        id:
          type: string
        rma_number:
          type: string
        original_serial:
          type: string
        replacement_serial:
          type: string
        status:
          type: string
        issue_description:
          type: string
        vendor_response:
          type: string
        disposition:
          type: string
        created_at:
          type: string
        resolved_at:
          type: string
    RMACaseCreate:
      type: object
      properties:
        original_serial:
          type: string
        issue_description:
          type: string
        replacement_serial:
          type: string
    TenantNetwork:
      type: object
      properties:
        id:
          type: string
        label:
          type: string
        type:
          type: string
        ssid_name:
          type: string
        broadcast_ssid:
          type: string
        security:
          type: string
        vlan_id:
          type: string
        isolation_level:
          type: string
        bandwidth_limit_kbps_up:
          type: string
        bandwidth_limit_kbps_down:
          type: string
        max_devices:
          type: string
        egress_acl:
          type: string
        status:
          type: string
        expires_at:
          type: string
        connected_devices:
          type: string
        created_at:
          type: string
    TenantNetworkCreate:
      type: object
      properties:
        label:
          type: string
        type:
          type: string
        ssid_name:
          type: string
        broadcast_ssid:
          type: string
        security:
          type: string
        passphrase:
          type: string
        vlan_id:
          type: string
        isolation_level:
          type: string
        bandwidth_limit_kbps_up:
          type: string
        bandwidth_limit_kbps_down:
          type: string
        max_devices:
          type: string
        expires_at:
          type: string
    TenantNetworkUpdate:
      type: object
      properties:
        label:
          type: string
        broadcast_ssid:
          type: string
        bandwidth_limit_kbps_up:
          type: string
        bandwidth_limit_kbps_down:
          type: string
        max_devices:
          type: string
        isolation_level:
          type: string
        expires_at:
          type: string
        status:
          type: string
    WIPSConfig:
      type: object
      properties:
        enabled:
          type: string
        scan_mode:
          type: string
        sensor_ap_ids:
          type: string
        detection_classes:
          type: string
        thresholds:
          type: string
        containment_mode:
          type: string
        allowlist_macs:
          type: string
        allowlist_ssids:
          type: string
        updated_at:
          type: string
    WIPSConfigUpdate:
      type: object
      properties:
        enabled:
          type: string
        scan_mode:
          type: string
        sensor_ap_ids:
          type: string
        detection_classes:
          type: string
        thresholds:
          type: string
        containment_mode:
          type: string
        allowlist_macs:
          type: string
        allowlist_ssids:
          type: string
    WIPSAlarm:
      type: object
      properties:
        id:
          type: string
        class:
          type: string
        severity:
          type: string
        state:
          type: string
        source_mac:
          type: string
        dest_mac:
          type: string
        bssid:
          type: string
        ssid:
          type: string
        channel:
          type: string
        rssi:
          type: string
        sensor_node_id:
          type: string
        classification:
          type: string
        containment_state:
          type: string
        detected_at:
          type: string
        resolved_at:
          type: string
    DPPBootstrapRequest:
      type: object
      properties:
        uri:
          type: string
        label:
          type: string
    DPPEnrollee:
      type: object
      properties:
        id:
          type: string
        mac:
          type: string
        bootstrap_uri:
          type: string
        status:
          type: string
          enum:
          - requested
          - in_progress
          - succeeded
          - timed_out
          - failed
        assigned_ssid:
          type: string
        certificate_expiry:
          type: string
        created_at:
          type: string
    DPPSession:
      type: object
      properties:
        id:
          type: string
        enrollee_mac:
          type: string
        state:
          type: string
          enum:
          - requested
          - in_progress
          - succeeded
          - timed_out
          - failed
        target_ssid:
          type: string
        akm:
          type: string
          description: AKM from `DPP_Config.akm`.
          enum:
          - psk
          - sae
          - psk-sae
          - dpp
          - dpp-sae
          - dpp-psk-sae
        auth:
          type: string
          description: Auth mode from `DPP_Config.auth`.
          enum:
          - initiate_on_announce
          - initiate_now
          - respond_only
          - chirp_and_respond
        key_curve:
          type: string
          description: Key curve from `DPP_Config.own_bi_key_curve`.
          enum:
          - prime256v1
          - secp384r1
          - secp521r1
          - brainpoolP256r1
          - brainpoolP384r1
          - brainpoolP512r1
        conf_role:
          type: string
          description: Configurator role from `DPP_Config.configurator_conf_role`.
          enum:
          - sta-psk
          - sta-sae
          - sta-psk-sae
          - sta-dpp
          - sta-dpp-sae
          - sta-dpp-psk-sae
          - ap-psk
          - ap-sae
          - ap-psk-sae
          - ap-dpp
          - ap-dpp-sae
          - ap-dpp-psk-sae
        error_code:
          type: string
        error_message:
          type: string
        started_at:
          type: string
        completed_at:
          type: string
    DPPSessionCreate:
      type: object
      properties:
        enrollee_id:
          type: string
        ssid:
          type: string
        passphrase:
          type: string
        security:
          type: string
          enum:
          - psk
          - sae
          - psk-sae
          - dpp
          - dpp-sae
          - dpp-psk-sae
    MatterFabric:
      type: object
      properties:
        fabric_id:
          type: string
        label:
          type: string
        root_ca_subject:
          type: string
        root_ca_expiry:
          type: string
        node_count:
          type: string
        operational_cert_expiry:
          type: string
        created_at:
          type: string
    MatterFabricCreate:
      type: object
      properties:
        label:
          type: string
        fabric_id:
          type: string
    MatterCommissioningRequest:
      type: object
      properties:
        setup_payload:
          type: string
        fabric_id:
          type: string
        discovery_method:
          type: string
        wifi_credentials:
          type: string
        passcode:
          type: string
    MatterNode:
      type: object
      properties:
        node_id:
          type: string
        fabric_id:
          type: string
        vendor_id:
          type: string
        product_id:
          type: string
        software_version:
          type: string
        reachable:
          type: string
        state:
          type: string
        endpoints:
          type: string
        last_seen:
          type: string
        commissioned_at:
          type: string
    DPIConfig:
      type: object
      properties:
        enabled:
          type: string
        handlers:
          type: string
        signature_db_version:
          type: string
        sampling_rate:
          type: string
        excluded_devices:
          type: string
        nfqueue_config:
          type: string
        updated_at:
          type: string
    DPIConfigUpdate:
      type: object
      properties:
        enabled:
          type: string
        sampling_rate:
          type: string
        excluded_devices:
          type: string
    DPIPolicy:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        idx:
          type: string
        action:
          type: string
        apps:
          type: string
        categories:
          type: string
        fqdns:
          type: string
        macs:
          type: string
        risk_level:
          type: string
        next:
          type: string
        enabled:
          type: string
        created_at:
          type: string
    DPIPolicyCreate:
      type: object
      properties:
        name:
          type: string
        action:
          type: string
        idx:
          type: string
        apps:
          type: string
        categories:
          type: string
        fqdns:
          type: string
        macs:
          type: string
        risk_level:
          type: string
        next:
          type: string
    DPIPolicyUpdate:
      type: object
      properties:
        name:
          type: string
        idx:
          type: string
        action:
          type: string
        apps:
          type: string
        categories:
          type: string
        fqdns:
          type: string
        macs:
          type: string
        risk_level:
          type: string
        next:
          type: string
        enabled:
          type: string
    DPIFlow:
      type: object
      properties:
        src_mac:
          type: string
        app_id:
          type: string
        app_name:
          type: string
        category:
          type: string
        bytes_up:
          type: string
        bytes_down:
          type: string
        action:
          type: string
        risk_level:
          type: string
        first_seen:
          type: string
        last_seen:
          type: string
    DPIUsageReport:
      type: object
      properties:
        app_id:
          type: string
        app_name:
          type: string
        category:
          type: string
        bytes_up:
          type: string
        bytes_down:
          type: string
        pct_total:
          type: string
        period:
          type: string
    RewardGrant:
      type: object
      properties:
        type:
          type: string
        minutes:
          type: string
        reason:
          type: string
    ParentalUsageReport:
      type: object
      properties:
        profile_id:
          type: string
        profile_name:
          type: string
        device_mac:
          type: string
        device_nickname:
          type: string
        total_minutes:
          type: string
        remaining_minutes:
          type: string
        by_app:
          type: string
        by_category:
          type: string
        date:
          type: string
        granularity:
          type: string
    AlertRule:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        condition:
          type: string
        severity:
          type: string
        labels:
          type: string
        annotations:
          type: string
        runbook_url:
          type: string
        evaluation_interval_seconds:
          type: string
        notification_channels:
          type: string
        enabled:
          type: string
        created_at:
          type: string
    AlertRuleCreate:
      type: object
      properties:
        name:
          type: string
        condition:
          type: string
        severity:
          type: string
        labels:
          type: string
        annotations:
          type: string
        runbook_url:
          type: string
        evaluation_interval_seconds:
          type: string
        notification_channels:
          type: string
    AlertRuleUpdate:
      type: object
      properties:
        condition:
          type: string
        severity:
          type: string
        labels:
          type: string
        annotations:
          type: string
        runbook_url:
          type: string
        evaluation_interval_seconds:
          type: string
        notification_channels:
          type: string
        enabled:
          type: string
    Agent:
      type: object
      properties:
        id:
          type: string
          readOnly: true
        nodeId:
          type: string
          readOnly: true
        agentKey:
          type: string
        displayName:
          type: string
        category:
          type: string
        provider:
          type: string
        version:
          type: string
        enabled:
          type: boolean
        desiredState:
          type: object
        runtimeState:
          type: object
          readOnly: true
        config:
          type: object
        secretRefs:
          type: array
          readOnly: true
          items:
            type: string
        capabilities:
          type: array
          items:
            type: string
        health:
          type: string
          enum:
          - healthy
          - degraded
          - down
          - unknown
          readOnly: true
        lastHeartbeat:
          type: string
          format: date-time
          readOnly: true
        errorMessage:
          type: string
          readOnly: true
        firstSeenAt:
          type: string
          format: date-time
          readOnly: true
        configCreatedAt:
          type: string
          format: date-time
          readOnly: true
        configUpdatedAt:
          type: string
          format: date-time
          readOnly: true
    AgentCapability:
      type: object
      properties:
        agentKey:
          type: string
        displayName:
          type: string
        category:
          type: string
        providers:
          type: array
          items:
            type: string
        configSchema:
          type: object
        supportedActions:
          type: array
          items:
            type: string
        supportsStatus:
          type: boolean
        supportsMetrics:
          type: boolean
    AgentCapabilityList:
      type: object
      properties:
        capabilities:
          type: array
          items:
            $ref: '#/components/schemas/AgentCapability'
        nodeId:
          type: string
    AgentConfigUpdate:
      type: object
      properties:
        agentKey:
          type: string
        displayName:
          type: string
        config:
          type: object
        secrets:
          type: object
          writeOnly: true
        enabled:
          type: boolean
        desiredState:
          type: object
    AgentPolicy:
      type: object
      properties:
        id:
          type: string
          readOnly: true
        locationId:
          type: string
        agentKey:
          type: string
        provider:
          type: string
        enabled:
          type: boolean
        priority:
          type: integer
          description: Higher priority policies override lower priority ones
        config:
          type: object
        secretRefs:
          type: array
          readOnly: true
          items:
            type: string
        secrets:
          type: object
          writeOnly: true
        inheritable:
          type: boolean
          description: Whether this policy propagates to child scopes
        createdAt:
          type: string
          format: date-time
          readOnly: true
        updatedAt:
          type: string
          format: date-time
          readOnly: true
    AgentStatus:
      type: object
      properties:
        agentId:
          type: string
        agentKey:
          type: string
        status:
          type: string
          enum:
          - installed
          - running
          - stopped
          - error
          - unknown
        health:
          type: string
          enum:
          - healthy
          - degraded
          - down
          - unknown
        lastHeartbeat:
          type: string
          format: date-time
        errorMessage:
          type: string
        metrics:
          type: object
        uptimeSeconds:
          type: integer
    AgentBridgeConfig:
      type: object
      properties:
        enabled:
          type: boolean
          default: false
          description: Enable the MQTT bridge connection to the cloud broker
        brokerAddress:
          type: string
          description: Cloud MQTT broker hostname or IP address
        brokerPort:
          type: integer
          default: 8883
          minimum: 1
          maximum: 65535
          description: Cloud MQTT broker port for TLS connection
        keepaliveSeconds:
          type: integer
          default: 60
          minimum: 10
          maximum: 65535
          description: Bridge keepalive interval in seconds
        maxQueuedMessages:
          type: integer
          default: 10000
          minimum: 1000
          maximum: 50000
          description: Maximum messages buffered during WAN or cloud disconnection
        fallbackAddress:
          type: string
          description: Fallback cloud MQTT broker hostname for HA failover
        fallbackPort:
          type: integer
          default: 8883
          minimum: 1
          maximum: 65535
          description: Fallback cloud MQTT broker port
        autoFailback:
          type: boolean
          default: true
          description: Automatically migrate back to primary broker after it recovers
        tlsCaCert:
          type: string
          writeOnly: true
          description: Cloud broker CA certificate in PEM format
        tlsClientCert:
          type: string
          writeOnly: true
          description: Device client certificate in PEM format for mutual TLS authentication
        tlsClientKey:
          type: string
          writeOnly: true
          description: Device client private key in PEM format for mutual TLS authentication
    AgentBridgeStatus:
      type: object
      properties:
        connectionState:
          type: string
          enum:
          - connected
          - disconnected
          - connecting
          - reconnecting
          - unconfigured
          - emergency
          description: Current bridge connection state to the cloud MQTT broker
        cloudBroker:
          type: string
          description: Hostname and port of the currently connected cloud broker
        connectedSince:
          type: string
          format: date-time
          description: Timestamp of the last successful bridge connection
        lastDisconnectReason:
          type: string
          description: Reason for the last bridge disconnection (error code or message)
        queueDepth:
          type: integer
          description: Current number of messages queued for cloud delivery
        queueOldestAgeSeconds:
          type: integer
          description: Age in seconds of the oldest message waiting in the bridge queue
        agentsConnected:
          type: integer
          description: Number of registered agents currently connected to the local broker
        tlsVersion:
          type: string
          description: Negotiated TLS version for the bridge connection
        certExpiryDays:
          type: integer
          description: Number of days until the device TLS certificate expires
        bytesSent24h:
          type: integer
          format: int64
          description: Total bytes sent to cloud broker in the last 24 hours
        bytesReceived24h:
          type: integer
          format: int64
          description: Total bytes received from cloud broker in the last 24 hours
        emergencyMode:
          type: boolean
          readOnly: true
          description: Whether the bridge is in emergency mode due to extended cloud unreachability
        emergencySince:
          type: string
          format: date-time
          description: Timestamp when emergency mode was activated
    Alert:
      type: object
      properties:
        id:
          type: string
        rule_id:
          type: string
        rule_name:
          type: string
        status:
          type: string
        severity:
          type: string
        labels:
          type: string
        annotations:
          type: string
        values:
          type: string
        fired_at:
          type: string
        resolved_at:
          type: string
        silenced_until:
          type: string
        notification_log:
          type: string
    DynamicRoutingConfig:
      type: object
      properties:
        protocols:
          type: string
        updated_at:
          type: string
    DynamicRoutingConfigUpdate:
      type: object
      properties:
        protocols:
          type: string
    LearnedRoute:
      type: object
      properties:
        destination:
          type: string
        netmask:
          type: string
        next_hop:
          type: string
        metric:
          type: string
        protocol:
          type: string
        interface:
          type: string
        age_seconds:
          type: string
    VLANConfig:
      type: object
      properties:
        vlan_id:
          type: string
        name:
          type: string
        bridge:
          type: string
        description:
          type: string
        tagged_ports:
          type: string
        untagged_ports:
          type: string
        pvid:
          type: string
        ssid_bindings:
          type: string
        igmp_snooping:
          type: string
        enabled:
          type: string
        created_at:
          type: string
    VLANConfigCreate:
      type: object
      properties:
        vlan_id:
          type: string
        name:
          type: string
        bridge:
          type: string
        description:
          type: string
        tagged_ports:
          type: string
        untagged_ports:
          type: string
        pvid:
          type: string
        igmp_snooping:
          type: string
    VLANConfigUpdate:
      type: object
      properties:
        name:
          type: string
        bridge:
          type: string
        description:
          type: string
        tagged_ports:
          type: string
        untagged_ports:
          type: string
        pvid:
          type: string
        ssid_bindings:
          type: string
        igmp_snooping:
          type: string
        enabled:
          type: string
    PowerConfig:
      type: object
      properties:
        radios:
          type: string
        led:
          type: string
        eco_mode:
          type: string
        thermal:
          type: string
        poe_budget_w:
          type: string
        updated_at:
          type: string
    PowerConfigUpdate:
      type: object
      properties:
        radios:
          type: string
        led:
          type: string
        eco_mode:
          type: string
        thermal:
          type: string
    PowerSchedule:
      type: object
      properties:
        id:
          type: string
        timezone:
          type: string
        entries:
          type: string
        enabled:
          type: string
    PowerScheduleUpdate:
      type: object
      properties:
        timezone:
          type: string
        entries:
          type: string
        enabled:
          type: string
    ConfigSnapshot:
      type: object
      description: A point-in-time snapshot of location configuration for versioning
        and rollback.
      properties:
        snapshotId:
          type: string
          readOnly: true
        name:
          type: string
        createdAt:
          type: string
          format: date-time
          readOnly: true
        createdBy:
          type: string
          readOnly: true
        scope:
          type: string
          enum:
          - location
          - network
          - radio
          - security
          readOnly: true
        sizeBytes:
          type: integer
          readOnly: true
        tags:
          type: array
          items:
            type: string
        description:
          type: string
    ConfigRestore:
      type: object
      properties:
        snapshot_id:
          type: string
        dry_run:
          type: string
        auto_revert_seconds:
          type: string
    MaintenanceState:
      type: object
      properties:
        status:
          type: string
        banner_message:
          type: string
        scheduled_start:
          type: string
        scheduled_end:
          type: string
        activated_by:
          type: string
    MaintenanceStateUpdate:
      type: object
      properties:
        status:
          type: string
        banner_message:
          type: string
        scheduled_start:
          type: string
        scheduled_end:
          type: string
    ConfigDriftReport:
      type: object
      properties:
        node_id:
          type: string
        in_sync:
          type: string
        differences:
          type: string
        checked_at:
          type: string
    GRETunnel:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        if_type:
          type: string
        source:
          type: string
        destination:
          type: string
        key:
          type: string
        checksum:
          type: string
        sequence_numbers:
          type: string
        mtu:
          type: string
        keepalive_interval_seconds:
          type: string
        keepalive_retries:
          type: string
        ttl:
          type: string
        enabled:
          type: string
        state:
          type: string
        keepalive_status:
          type: string
        packets_tx:
          type: string
        packets_rx:
          type: string
        bytes_tx:
          type: string
        bytes_rx:
          type: string
        created_at:
          type: string
    GRETunnelCreate:
      type: object
      properties:
        name:
          type: string
        if_type:
          type: string
        source:
          type: string
        destination:
          type: string
        key:
          type: string
        checksum:
          type: string
        sequence_numbers:
          type: string
        mtu:
          type: string
        keepalive_interval_seconds:
          type: string
        keepalive_retries:
          type: string
        ttl:
          type: string
    GRETunnelUpdate:
      type: object
      properties:
        key:
          type: string
        checksum:
          type: string
        sequence_numbers:
          type: string
        mtu:
          type: string
        keepalive_interval_seconds:
          type: string
        keepalive_retries:
          type: string
        ttl:
          type: string
        enabled:
          type: string
    QoERule:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        target_type:
          type: string
        target_ids:
          type: string
        priority:
          type: string
        dscp_mark:
          type: string
        wme_access_category:
          type: string
        reserved_bandwidth_kbps:
          type: string
        schedule_enabled:
          type: string
        schedule_start:
          type: string
        schedule_end:
          type: string
        enabled:
          type: string
        created_at:
          type: string
    QoERuleCreate:
      type: object
      properties:
        name:
          type: string
        target_type:
          type: string
        target_ids:
          type: string
        priority:
          type: string
        dscp_mark:
          type: string
        wme_access_category:
          type: string
        reserved_bandwidth_kbps:
          type: string
        schedule_enabled:
          type: string
        schedule_start:
          type: string
        schedule_end:
          type: string
    QoERuleUpdate:
      type: object
      properties:
        name:
          type: string
        priority:
          type: string
        dscp_mark:
          type: string
        wme_access_category:
          type: string
        reserved_bandwidth_kbps:
          type: string
        schedule_enabled:
          type: string
        schedule_start:
          type: string
        schedule_end:
          type: string
        enabled:
          type: string
    QoOApplicationClass:
      type: object
      properties:
        id:
          type: string
          readOnly: true
        name:
          type: string
          description: Human-readable label (e.g. "4K Video Streaming")
        applicationClass:
          type: string
          enum:
          - streaming
          - gaming
          - voice
          - browsing
          - video_conf
          - iot
          - upload
          - download
          - custom
          description: Broad application category
        profile:
          type: string
          enum:
          - casual
          - standard
          - premium
          - ultra_low_latency
          description: Service tier profile affecting requirements stringency
        throughputReqMbps:
          type: number
          description: Minimum required throughput for acceptable performance
        latencyReqMs:
          type: number
          description: Maximum acceptable one-way latency
        packetLossReqPct:
          type: number
          description: Maximum acceptable packet loss percentage
        jitterReqMs:
          type: number
          description: Maximum acceptable jitter
        description:
          type: string
    QoOScore:
      type: object
      properties:
        id:
          type: string
          readOnly: true
        locationId:
          type: string
        timestamp:
          type: string
          format: date-time
          readOnly: true
        overallScore:
          type: integer
          minimum: 0
          maximum: 100
          description: QoO score 0-100, where 100 means all application classes meet
            their requirements for optimal performance
        applicationScores:
          type: array
          items:
            type: object
            properties:
              applicationClass:
                type: string
                enum:
                - streaming
                - gaming
                - voice
                - browsing
                - video_conf
                - iot
                - upload
                - download
              score:
                type: integer
                minimum: 0
                maximum: 100
              requirementsMet:
                type: boolean
              measuredThroughputMbps:
                type: number
              measuredLatencyMs:
                type: number
              measuredPacketLossPct:
                type: number
              measuredJitterMs:
                type: number
        networkSegmentBreakdown:
          type: array
          items:
            type: object
            properties:
              segment:
                type: string
                enum:
                - access
                - wifi
                - wan
                - isp_backbone
                - cdn_edge
              score:
                type: integer
                minimum: 0
                maximum: 100
              description:
                type: string
    QualityAttenuationReport:
      type: object
      properties:
        id:
          type: string
          readOnly: true
        nodeId:
          type: string
        observationPeriod:
          type: object
          properties:
            from:
              type: string
              format: date-time
            to:
              type: string
              format: date-time
        packetSizeBytes:
          type: integer
          description: Size of test packets used for the measurement
        loadMbps:
          type: number
          description: Offered load during the measurement period
        latencyPercentiles:
          type: array
          description: Latency distribution across key percentiles (ms)
          items:
            type: object
            properties:
              percentile:
                type: integer
                description: Percentile (0-100)
              latencyMs:
                type: number
        packetLossPct:
          type: number
        gComponent:
          type: object
          description: G component — minimum delay in seconds plus base loss
          properties:
            delaySec:
              type: number
            lossPct:
              type: number
        sComponent:
          type: object
          description: S component — serialization delay per bit plus loss increase
            per bit
          properties:
            delayPerBitSec:
              type: number
            lossIncreasePerBitPct:
              type: number
        sourceType:
          type: string
          enum:
          - twamp
          - stamp
          - l2_pm
          - synthetic_probe
          description: Measurement protocol or probe type used
    GuestInvitation:
      type: object
      properties:
        id:
          type: string
        token:
          type: string
        ssid:
          type: string
        status:
          type: string
        qr_url:
          type: string
        shareable_url:
          type: string
        valid_from:
          type: string
        valid_to:
          type: string
        max_devices:
          type: string
        bandwidth_limit_kbps_down:
          type: string
        bandwidth_limit_kbps_up:
          type: string
        devices_connected:
          type: string
        created_by:
          type: string
        created_at:
          type: string
    GuestInvitationCreate:
      type: object
      properties:
        ssid:
          type: string
        valid_from:
          type: string
        valid_to:
          type: string
        max_devices:
          type: string
        bandwidth_limit_kbps_down:
          type: string
        bandwidth_limit_kbps_up:
          type: string
        label:
          type: string
    GuestInvitationUpdate:
      type: object
      properties:
        valid_to:
          type: string
        max_devices:
          type: string
        bandwidth_limit_kbps_down:
          type: string
        bandwidth_limit_kbps_up:
          type: string
    RebootType:
      type: string
      description: Reboot reason classification from OpenSync `Reboot_Status.type`.
      enum:
      - UNKNOWN
      - COLD_BOOT
      - POWER_CYCLE
      - WATCHDOG
      - CRASH
      - USER
      - DEVICE
      - HEALTH_CHECK
      - UPGRADE
      - THERMAL
      - CLOUD
    RebootEventData:
      type: object
      description: Payload for `node.rebooted` events.
      properties:
        type:
          $ref: '#/components/schemas/RebootType'
        reason:
          type: string
          description: Free-text reboot reason (max 128 chars in upstream OVSDB).
          nullable: true
    CrashReport:
      type: object
      description: Process crash or OpenSync restart report published on the `Crash.Reports`
        MQTT topic.
      properties:
        nodeId:
          type: string
        locationId:
          type: string
        firmwareVersion:
          type: string
        model:
          type: string
        pid:
          type: string
        name:
          type: string
          description: Process name. `OpenSync` is used for full manager restarts.
        reason:
          type: string
        timestamp:
          type: string
          format: date-time
        backtrace:
          type: string
          nullable: true
    OptimizationStatus:
      type: object
      description: State of a mesh topology optimization job.
      properties:
        optimizationId:
          type: string
        locationId:
          type: string
        status:
          type: string
          enum:
          - pending
          - running
          - completed
          - failed
          - cancelled
        triggeredBy:
          type: string
          enum:
          - user
          - system
          - schedule
        dryRun:
          type: boolean
        autoApply:
          type: boolean
        createdAt:
          type: string
          format: date-time
        completedAt:
          type: string
          format: date-time
          nullable: true
        recommendations:
          type: array
          items:
            $ref: '#/components/schemas/OptimizationRecommendation'
        errors:
          type: array
          items:
            type: string
    OptimizationRecommendation:
      type: object
      description: A single topology change recommended by an optimization run.
      properties:
        nodeId:
          type: string
        action:
          type: string
          enum:
          - reparent
          - channel_change
          - band_switch
          - power_adjust
          - backhaul_switch
        targetParentNodeId:
          type: string
          nullable: true
        targetChannel:
          type: integer
          nullable: true
        targetBand:
          $ref: '#/components/schemas/Band'
          nullable: true
        reason:
          type: string
    RadiusStatsRecord:
      type: object
      description: RADIUS server statistics per VIF from `Wifi_Stats_Config` `radius_stats`
        stat type and upstream `RADIUS` table.
      properties:
        vifName:
          type: string
        vifRole:
          type: string
        serverAddress:
          type: string
        serverIndex:
          type: integer
        roundTripTimeMs:
          type: integer
        accessRequests:
          type: integer
        accessRetransmissions:
          type: integer
        accessAccepts:
          type: integer
        accessRejects:
          type: integer
        accessChallenges:
          type: integer
        malformedAccessResponses:
          type: integer
        badAuthenticators:
          type: integer
        pendingRequests:
          type: integer
        timeouts:
          type: integer
        unknownTypes:
          type: integer
        packetsDropped:
          type: integer
    ClientFreezeConfig:
      type: object
      description: Device connectivity freeze schedule from `Client_Freeze_Config`.
      properties:
        enabled:
          type: boolean
        source:
          type: string
          enum:
          - cloud
          - gw
          - init
        type:
          type: string
          enum:
          - schedule
          - always
        macs:
          type: array
          items:
            type: string
          description: Target device MAC addresses
        startTime:
          type: string
          format: date-time
        endTime:
          type: string
          format: date-time
    BluetoothConfig:
      type: object
      description: Bluetooth LE onboarding config from `AW_Bluetooth_Config`.
      properties:
        mode:
          type: string
          enum:
          - false
          - true
        command:
          type: string
          enum:
          - on_boarding
          - diagnostic
          - locate
    MAPConfig:
      type: object
      description: MAP-T (RFC 7597) or MAP-E (RFC 7599) IPv6 transition configuration
        for ISPs using IPv4-over-IPv6 tunneling.
      properties:
        enabled:
          type: boolean
          description: Enable MAP IPv6 transition on the WAN interface.
        mode:
          type: string
          enum:
          - MAP-T
          - MAP-E
          description: MAP translation mode. MAP-T uses stateless translation, MAP-E
            uses encapsulation.
        brAddress:
          type: string
          description: Border Relay IPv6 address.
        psidOffset:
          type: integer
          description: PSID offset bits (0-16).
        psidLength:
          type: integer
          description: PSID length in bits (0-16).
        eaLength:
          type: integer
          description: Embedded Address length in bits.
        ipv4Prefix:
          type: string
          description: Shared IPv4 prefix (CIDR).
        ipv6Prefix:
          type: string
          description: MAP IPv6 prefix (CIDR).
        dmrAddress:
          type: string
          description: Default Mapping Rule IPv6 prefix (MAP-T only).
    OpenVPNConfig:
      type: object
      description: OpenVPN tunnel configuration for site-to-site or client VPN connections.
      properties:
        enabled:
          type: boolean
          default: false
        mode:
          type: string
          enum:
          - client
          - server
          default: client
        protocol:
          type: string
          enum:
          - udp
          - tcp
          default: udp
        port:
          type: integer
          default: 1194
        authMethod:
          type: string
          enum:
          - tls
          - psk
          - password
          default: tls
        cipher:
          type: string
          enum:
          - AES-256-GCM
          - AES-128-GCM
          - CHACHA20-POLY1305
          default: AES-256-GCM
        compression:
          type: string
          enum:
          - none
          - lzo
          - lz4
          default: none
        topology:
          type: string
          enum:
          - subnet
          - net30
          default: subnet
        remote:
          type: string
          description: Remote OpenVPN server address (host:port) for client mode.
        redirectGateway:
          type: boolean
          description: Redirect all client traffic through the VPN tunnel.
          default: false
        tlsAuthKey:
          type: string
          writeOnly: true
          description: TLS authentication key for HMAC firewall.
        caCertificate:
          type: string
          writeOnly: true
          description: CA certificate for TLS verification.
        clientCertificate:
          type: string
          writeOnly: true
          description: Client certificate (TLS mode).
        clientKey:
          type: string
          writeOnly: true
          description: Client private key (TLS mode).
        mtu:
          type: integer
          default: 1500
        keepaliveInterval:
          type: integer
          default: 10
          description: Keepalive ping interval in seconds.
        keepaliveTimeout:
          type: integer
          default: 120
          description: Keepalive timeout in seconds.
    ZWaveConfig:
      type: object
      description: Z-Wave 800 series controller configuration. Manages the Z-Wave
        mesh network for smart home devices.
      properties:
        enabled:
          type: boolean
          default: false
        networkKey:
          type: string
          writeOnly: true
          description: Z-Wave network key for S0/S2 security.
        regionalFrequency:
          type: string
          enum:
          - EU
          - US
          - JP
          - AU
          - CN
          - KR
          - IN
          - RU
          - IL
          - custom
          default: EU
          description: Regional Z-Wave frequency band (determines 868/908/916/919/921
            MHz operation).
        s2SecurityClasses:
          type: array
          items:
            type: string
            enum:
            - S2_UNAUTHENTICATED
            - S2_AUTHENTICATED
            - S2_ACCESS
            - S2_LR
          description: Enabled S2 security classes for network inclusion.
        zwlrEnabled:
          type: boolean
          default: false
          description: Enable Z-Wave Long Range (ZWLR) for up to 1.6 km range.
        inclusionMode:
          type: boolean
          description: Put controller in inclusion mode to add new devices. Automatically
            returns to false after timeout.
          default: false
        maxDevices:
          type: integer
          default: 232
          description: Maximum number of Z-Wave devices on the network.
        deviceCount:
          type: integer
          readOnly: true
          description: Current number of paired Z-Wave devices.
    ZWaveDevice:
      type: object
      description: A Z-Wave device on the mesh network.
      properties:
        nodeId:
          type: integer
          readOnly: true
        deviceType:
          type: string
          readOnly: true
        manufacturer:
          type: string
          readOnly: true
        productName:
          type: string
          readOnly: true
        securityClass:
          type: string
          enum:
          - none
          - S0
          - S2_UNAUTHENTICATED
          - S2_AUTHENTICATED
          - S2_ACCESS
          readOnly: true
        batteryLevel:
          type: integer
          readOnly: true
        lastSeen:
          type: string
          format: date-time
          readOnly: true
        networkHealth:
          type: string
          enum:
          - good
          - fair
          - poor
          - unreachable
          readOnly: true
    DeviceFingerprint:
      type: object
      description: Device fingerprint data derived from network traffic analysis (DHCP,
        HTTP, mDNS, nDPI).
      properties:
        macOui:
          type: string
          readOnly: true
          description: MAC address OUI vendor name (e.g. "Apple, Inc.").
        dhcpFingerprint:
          type: string
          readOnly: true
          description: DHCP option fingerprint string.
        httpUserAgent:
          type: string
          readOnly: true
          description: HTTP User-Agent string observed from the device.
        mdnsName:
          type: string
          readOnly: true
          description: mDNS/Bonjour service name advertised by the device.
        hostname:
          type: string
          readOnly: true
          description: Device hostname from DHCP or NetBIOS.
        osGuess:
          type: string
          readOnly: true
          description: Operating system guess (e.g. "iOS 17", "Windows 11", "Android
            14").
        deviceCategory:
          type: string
          readOnly: true
          enum:
          - phone
          - tablet
          - laptop
          - desktop
          - tv
          - iot
          - printer
          - camera
          - speaker
          - gaming
          - wearable
          - appliance
          - other
          - unknown
          description: Device category classification.
        confidence:
          type: number
          readOnly: true
          description: Classification confidence score (0.0-1.0).
        detectionMethod:
          type: string
          readOnly: true
          description: Primary method used for identification (dhcp, nDPI, mDNS, user_agent,
            manual).
    TorConfig:
      type: object
      description: Tor (The Onion Router) client configuration for anonymous network
        access.
      properties:
        enabled:
          type: boolean
          default: false
        socksPort:
          type: integer
          default: 9050
          description: SOCKS5 proxy port on the router.
        transparentProxy:
          type: boolean
          default: false
          description: Route all client traffic through Tor transparently.
        streamIsolation:
          type: boolean
          default: true
          description: Use separate Tor circuits per destination for stream isolation.
        killSwitch:
          type: boolean
          default: true
          description: Block all non-Tor traffic when enabled.
        excludeNodes:
          type: array
          items:
            type: string
          description: Exclude Tor exit nodes from these country codes (ISO 3166-1
            alpha-2).
        bridgeAddress:
          type: string
          description: Tor bridge relay address for censored networks.
        circuitEstablished:
          type: boolean
          readOnly: true
          description: Whether a Tor circuit is currently established.
        guardNodeFingerprint:
          type: string
          readOnly: true
          description: Current guard node fingerprint.
    AttestationReport:
      type: object
      description: TPM 2.0 remote attestation report with measured boot PCR values.
      properties:
        attestationMethod:
          type: string
          enum:
          - TPM2
          - remote
          readOnly: true
        pcrValues:
          type: object
          readOnly: true
          description: PCR (Platform Configuration Register) index to SHA-256 hash
            mapping.
          additionalProperties:
            type: string
          example:
            '0': a1b2c3...
            '1': d4e5f6...
            '7': g7h8i9...
        pcrBank:
          type: string
          enum:
          - SHA1
          - SHA256
          default: SHA256
          readOnly: true
        attestationQuote:
          type: string
          readOnly: true
          description: TPM Quote (base64-encoded) signed by the attestation key.
        tpmNonce:
          type: string
          readOnly: true
          description: Nonce used in the attestation challenge.
        lastAttestation:
          type: string
          format: date-time
          readOnly: true
        verified:
          type: boolean
          readOnly: true
          description: Whether the last attestation passed verification against expected
            PCR values.
        expectedPcrPolicy:
          type: string
          readOnly: true
          description: Reference to the expected PCR policy bundle.
    SecurityAdvisory:
      type: object
      description: Firmware security advisory for a CVE or vendor vulnerability disclosure.
      properties:
        advisoryId:
          type: string
          readOnly: true
        cveId:
          type: string
          description: CVE identifier (e.g. CVE-2026-12345).
        title:
          type: string
        severity:
          type: string
          enum:
          - CRITICAL
          - HIGH
          - MEDIUM
          - LOW
          - INFORMATIONAL
        affectedVersions:
          type: array
          items:
            type: string
          description: Firmware version ranges affected by this advisory.
        patchedVersion:
          type: string
          description: First firmware version containing the fix.
        cweId:
          type: string
          description: CWE identifier for the weakness class.
        cvssScore:
          type: number
          description: CVSS 3.1 base score (0.0-10.0).
        description:
          type: string
          description: Human-readable description of the vulnerability.
        publishedAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        mitigationUrl:
          type: string
          description: URL to vendor mitigation guidance.
    TamperEvent:
      type: object
      description: Chassis intrusion or physical tamper detection event.
      properties:
        eventId:
          type: string
          readOnly: true
        sensorId:
          type: string
          readOnly: true
          description: Identifier of the tamper sensor.
        type:
          type: string
          enum:
          - chassis_open
          - chassis_close
          - seal_broken
          - voltage_tamper
          - physical_reset
          readOnly: true
        state:
          type: string
          enum:
          - open
          - closed
          - triggered
          readOnly: true
        timestamp:
          type: string
          format: date-time
          readOnly: true
        acknowledged:
          type: boolean
          default: false
        acknowledgedBy:
          type: string
          readOnly: true
          description: Operator who acknowledged the event.
    USPAgentConfig:
      type: object
      description: TR-369 USP (User Services Platform) agent configuration. The USP
        agent is a software agent on the CPE that communicates with a USP Controller
        via MQTT/WebSocket/STOMP. This schema manages the agent itself, not the USP
        protocol operations or TR-181 data model.
      properties:
        enabled:
          type: boolean
          default: false
          description: Enable the USP agent.
        controllerUrl:
          type: string
          description: USP Controller endpoint URL (MQTT broker, WebSocket, or STOMP
            server).
        controllerProtocol:
          type: string
          enum:
          - mqtt
          - websocket
          - stomp
          default: mqtt
          description: Transport protocol for USP communication.
        tlsEnabled:
          type: boolean
          default: true
        caCertificate:
          type: string
          writeOnly: true
          description: CA certificate for TLS verification of the USP controller.
        clientCertificate:
          type: string
          writeOnly: true
          description: Client certificate for mutual TLS.
        clientKey:
          type: string
          writeOnly: true
          description: Client private key for mutual TLS.
        endpointId:
          type: string
          description: USP Endpoint ID (unique identifier for this agent).
        periodicNotifIntervalSec:
          type: integer
          default: 300
          description: Periodic notification interval in seconds.
        maxRetryIntervalSec:
          type: integer
          default: 5120
          description: Maximum retry backoff interval for SARetry (TR-369 §3.6).
        subscriptions:
          type: array
          items:
            type: object
            properties:
              objectPath:
                type: string
                description: TR-181 object path to subscribe to.
              notifType:
                type: string
                enum:
                - ValueChange
                - ObjectCreation
                - ObjectDeletion
                - OperationComplete
                - OnBoardRequest
          description: USP subscription list for controller notifications.
    USPAgentStatus:
      type: object
      description: Read-only operational health of the TR-369 USP agent.
      properties:
        present:
          type: boolean
          readOnly: true
          description: Whether a USP agent is installed on the node.
        running:
          type: boolean
          readOnly: true
          description: Whether the USP agent process is currently running.
        connectionState:
          type: string
          enum:
          - connected
          - connecting
          - disconnected
          - error
          - unknown
          readOnly: true
          description: Connection state to the USP Controller.
        lastContact:
          type: string
          format: date-time
          readOnly: true
          description: Timestamp of last successful USP message exchange.
        protocolVersion:
          type: string
          readOnly: true
          description: USP protocol version (e.g. "1.3").
          example: '1.3'
        controllerUrl:
          type: string
          readOnly: true
          description: Currently connected USP Controller URL.
        endpointId:
          type: string
          readOnly: true
    ContainerStatus:
      type: object
      description: Read-only operational status of a container running on the node.
        Per the agent model (DESIGN.md Decision 5), the REST API does not manage container
        lifecycle (install, start, stop, remove) — that is the firmware designer's
        responsibility. This endpoint provides visibility into running containers.
      properties:
        containerId:
          type: string
          readOnly: true
        name:
          type: string
          readOnly: true
        image:
          type: string
          readOnly: true
        imageTag:
          type: string
          readOnly: true
        state:
          type: string
          enum:
          - running
          - paused
          - stopped
          - restarting
          - error
          - unknown
          readOnly: true
        uptimeSec:
          type: integer
          readOnly: true
        cpuUsagePct:
          type: number
          readOnly: true
        memoryUsageMb:
          type: number
          readOnly: true
        memoryLimitMb:
          type: number
          readOnly: true
        restartCount:
          type: integer
          readOnly: true
        ports:
          type: array
          items:
            type: object
            properties:
              hostPort:
                type: integer
              containerPort:
                type: integer
              protocol:
                type: string
                enum:
                - tcp
                - udp
          readOnly: true
        mounts:
          type: array
          items:
            type: object
            properties:
              source:
                type: string
              destination:
                type: string
          readOnly: true
        startedAt:
          type: string
          format: date-time
          readOnly: true
    HomeAssistantConfig:
      type: object
      description: Home Assistant integration configuration on the router.
      properties:
        enabled:
          type: boolean
          default: false
        apiUrl:
          type: string
          description: Home Assistant instance URL (e.g. http://homeassistant.local:8123).
        longLivedToken:
          type: string
          writeOnly: true
          description: Home Assistant long-lived access token.
        paired:
          type: boolean
          readOnly: true
          description: Whether the router is paired with a Home Assistant instance.
        entityPrefix:
          type: string
          default: router
          description: Prefix for auto-discovered entities in HA.
        mqttDiscoveryEnabled:
          type: boolean
          default: true
          description: Enable MQTT auto-discovery for device entities.
        entityWhitelist:
          type: array
          items:
            type: string
          description: List of entity types to expose (e.g. "sensor", "switch", "device_tracker").
            Empty means all.
        lastDiscovery:
          type: string
          format: date-time
          readOnly: true
        discoveredEntities:
          type: integer
          readOnly: true
          description: Count of entities currently exposed to Home Assistant.
    PoEPortConfig:
      type: object
      description: Per-port PoE (Power over Ethernet) configuration.
      properties:
        portId:
          type: integer
          readOnly: true
        label:
          type: string
        enabled:
          type: boolean
          default: true
          description: Enable PoE on this port.
        priority:
          type: string
          enum:
          - low
          - high
          - critical
          default: low
          description: PoE power allocation priority.
        powerLimitW:
          type: number
          description: Maximum power budget for this port in watts.
        currentPowerW:
          type: number
          readOnly: true
          description: Current power draw on this port in watts.
        pdClass:
          type: string
          enum:
          - class0
          - class1
          - class2
          - class3
          - class4
          - class5
          - class6
          - class7
          - class8
          readOnly: true
        pdStatus:
          type: string
          enum:
          - 'off'
          - 'on'
          - fault
          - searching
          readOnly: true
        totalBudgetW:
          type: number
          readOnly: true
          description: Total PoE power budget available across all ports.
        consumedW:
          type: number
          readOnly: true
          description: Total PoE power currently consumed across all ports.
    FanConfig:
      type: object
      description: Fan speed and thermal threshold configuration.
      properties:
        mode:
          type: string
          enum:
          - auto
          - manual
          - 'off'
          default: auto
        dutyCyclePct:
          type: integer
          minimum: 0
          maximum: 100
          description: Manual fan duty cycle percentage.
        minTempC:
          type: integer
          description: Temperature at which fan starts spinning (auto mode).
        maxTempC:
          type: integer
          description: Temperature at which fan reaches full speed (auto mode).
        currentSpeedRpm:
          type: integer
          readOnly: true
        currentDutyPct:
          type: integer
          readOnly: true
        fanCount:
          type: integer
          readOnly: true
          description: Number of fans installed on the device.
    CarbonConfig:
      type: object
      description: Carbon-aware computing configuration. Schedules non-critical workloads
        (firmware downloads, backups, analytics uploads) to low-carbon-intensity time
        windows.
      properties:
        enabled:
          type: boolean
          default: false
        region:
          type: string
          description: Electricity Maps region code (e.g. "DE", "FR", "US-CA").
        carbonThreshold:
          type: integer
          description: Maximum carbon intensity (gCO2eq/kWh) to allow non-critical
            workloads.
          default: 200
        deferrableWorkloads:
          type: array
          items:
            type: string
            enum:
            - firmware_download
            - analytics_upload
            - backup
            - report_generation
            - diagnostics_upload
          description: Workloads that can be shifted to low-carbon windows.
        currentCarbonIntensity:
          type: integer
          readOnly: true
          description: Current grid carbon intensity in gCO2eq/kWh.
        nextLowCarbonWindow:
          type: object
          readOnly: true
          properties:
            start:
              type: string
              format: date-time
            end:
              type: string
              format: date-time
            intensity:
              type: integer
        deferredCount:
          type: integer
          readOnly: true
          description: Number of workloads currently deferred.
        estimatedCO2SavedKg:
          type: number
          readOnly: true
    EnergyReport:
      type: object
      description: Energy consumption and efficiency report for EU Ecodesign compliance.
      properties:
        period:
          type: string
          enum:
          - 15min
          - hour
          - day
          - week
          - month
          readOnly: true
        totalKwh:
          type: number
          readOnly: true
          description: Total energy consumed in kWh.
        idlePowerW:
          type: number
          readOnly: true
        activePowerW:
          type: number
          readOnly: true
        deepSleepPowerW:
          type: number
          readOnly: true
        psuEfficiencyPct:
          type: number
          readOnly: true
        estimatedCO2Kg:
          type: number
          readOnly: true
        powerStateDistribution:
          type: object
          readOnly: true
          properties:
            idlePct:
              type: number
            activePct:
              type: number
            sleepPct:
              type: number
        timestamp:
          type: string
          format: date-time
          readOnly: true
    DigitalProductPassport:
      type: object
      description: EU ESPR Digital Product Passport (DPP) data for compliance with
        EU Regulation 2024/1781.
      properties:
        productId:
          type: string
          readOnly: true
        modelName:
          type: string
          readOnly: true
        manufacturer:
          type: string
          readOnly: true
        manufacturingDate:
          type: string
          format: date
          readOnly: true
        serialNumber:
          type: string
          readOnly: true
        recycledContentPct:
          type: number
          readOnly: true
          description: Percentage of recycled materials in the product.
        recyclabilityScore:
          type: number
          readOnly: true
          description: Product recyclability score (0-100).
        repairabilityScore:
          type: number
          readOnly: true
          description: Product repairability score (0-10) per EU energy label.
        carbonFootprintKg:
          type: number
          readOnly: true
          description: Product carbon footprint in kg CO2 equivalent.
        hazardousSubstances:
          type: array
          items:
            type: string
          readOnly: true
          description: REACH/RoHS substances of concern.
        expectedLifetimeYears:
          type: integer
          readOnly: true
        sparePartAvailabilityYears:
          type: integer
          readOnly: true
        firmwareUpdateCommitmentYears:
          type: integer
          readOnly: true
        dppDocumentUrl:
          type: string
          readOnly: true
          description: URL to the full DPP document.
    WatchdogConfig:
      type: object
      description: System watchdog and progressive escalation framework configuration.
      properties:
        hardwareWatchdogEnabled:
          type: boolean
          default: true
          description: External hardware watchdog (GPIO/PMIC).
        softwareWatchdogEnabled:
          type: boolean
          default: true
          description: Kernel softdog watchdog.
        services:
          type: array
          items:
            type: object
            properties:
              serviceName:
                type: string
                description: systemd/procd service name.
              enabled:
                type: boolean
                default: true
              maxRestarts:
                type: integer
                default: 5
                description: Maximum restarts before escalating.
              restartWindowSec:
                type: integer
                default: 300
                description: Window in seconds for counting restarts.
              escalationLevel:
                type: string
                enum:
                - LOG
                - RESTART
                - REBOOT
                - RECOVERY
                - HARD_RESET
                description: Current escalation level for this service.
              currentRestarts:
                type: integer
                readOnly: true
              lastEscalationAt:
                type: string
                format: date-time
                readOnly: true
        hardwareWatchdogTimeoutSec:
          type: integer
          default: 60
        pretimeoutDiagnostics:
          type: boolean
          default: true
          description: Capture diagnostic snapshot before watchdog timeout.
    BootIntegrityReport:
      type: object
      description: Results from the 10-stage boot-time integrity self-check.
      properties:
        overallStatus:
          type: string
          enum:
          - pass
          - fail
          - degraded
          readOnly: true
        lastBootAt:
          type: string
          format: date-time
          readOnly: true
        bootSlot:
          type: string
          enum:
          - A
          - B
          - recovery
          readOnly: true
        stages:
          type: array
          readOnly: true
          items:
            type: object
            properties:
              stage:
                type: integer
                minimum: 1
                maximum: 10
              name:
                type: string
                description: Stage name (e.g. "ROM_BOOT", "U_BOOT", "KERNEL_INTEGRITY",
                  "ROOTFS_VERIFY").
              status:
                type: string
                enum:
                - pass
                - fail
                - skipped
              durationMs:
                type: integer
              detail:
                type: string
                description: Failure detail if status is fail.
              pcrValue:
                type: string
                description: TPM PCR hash after this stage.
        firmwareVersion:
          type: string
          readOnly: true
        firmwareSigningKey:
          type: string
          readOnly: true
          description: Fingerprint of the key that signed the firmware.
    FirmwareSlotStatus:
      type: object
      description: A/B firmware slot and recovery partition health status.
      properties:
        activeSlot:
          type: string
          enum:
          - A
          - B
          - recovery
          readOnly: true
        nextBootSlot:
          type: string
          enum:
          - A
          - B
          - recovery
          readOnly: true
        slots:
          type: array
          readOnly: true
          items:
            type: object
            properties:
              slot:
                type: string
                enum:
                - A
                - B
                - recovery
              firmwareVersion:
                type: string
              buildDate:
                type: string
                format: date-time
              status:
                type: string
                enum:
                - valid
                - invalid
                - unbootable
                - pending
                - incomplete
              bootCount:
                type: integer
              lastBootResult:
                type: string
                enum:
                - success
                - failure
                - unknown
              sizeMb:
                type: integer
        uBootEnvHealthy:
          type: boolean
          readOnly: true
          description: Whether the dual-redundant U-Boot environment is healthy.
        uBootEnvChecksum:
          type: string
          readOnly: true
        recoveryModeAvailable:
          type: boolean
          readOnly: true
        gpiRecoveryTriggerEnabled:
          type: boolean
          readOnly: true
          description: Whether the hardware GPIO recovery escape hatch is enabled.
    TelemetryReportProfile:
      type: object
      description: Telemetry report profile defining what data to collect, at what
        interval, under what conditions. Inspired by RDK-B Telemetry 2.0.
      properties:
        profileId:
          type: string
          readOnly: true
        name:
          type: string
        enabled:
          type: boolean
          default: true
        metrics:
          type: array
          items:
            type: string
          description: Metric types to collect (e.g. "cpu", "memory", "wifi_clients",
            "wan_throughput", "temperature").
        intervalSec:
          type: integer
          default: 300
          description: Collection interval in seconds.
        conditions: &id001
          type: object
          description: Optional conditions that trigger collection (event-driven reporting).
          properties:
            minCpuPct:
              type: number
              description: Only collect when CPU exceeds this percentage.
            minMemoryPct:
              type: number
            minClientCount:
              type: integer
            timeOfDay:
              type: object
              properties:
                start:
                  type: string
                  pattern: ^[0-2]\d:[0-5]\d$
                  example: 08:00
                end:
                  type: string
                  pattern: ^[0-2]\d:[0-5]\d$
                  example: '22:00'
        encoding:
          type: string
          enum:
          - json
          - protobuf
          - avro
          default: json
          description: Data encoding format.
        destination: &id002
          type: object
          properties:
            type:
              type: string
              enum:
              - mqtt
              - http
              - kafka
              default: mqtt
            url:
              type: string
              description: MQTT topic, HTTP URL, or Kafka topic.
            auth:
              type: object
              properties:
                token:
                  type: string
                  writeOnly: true
              description: Authentication for the destination.
        retentionDays:
          type: integer
          default: 90
        createdAt:
          type: string
          format: date-time
          readOnly: true
        lastReportAt:
          type: string
          format: date-time
          readOnly: true
    TelemetryReportProfileCreate:
      type: object
      required:
      - name
      - metrics
      properties:
        name:
          type: string
        enabled:
          type: boolean
          default: true
        metrics:
          type: array
          items:
            type: string
        intervalSec:
          type: integer
          default: 300
        conditions: *id001
        encoding:
          type: string
          enum:
          - json
          - protobuf
          - avro
          default: json
        destination: *id002
        retentionDays:
          type: integer
          default: 90
    ScheduledChange:
      type: object
      description: A configuration change scheduled for future execution.
      properties:
        scheduleId:
          type: string
          readOnly: true
        name:
          type: string
        description:
          type: string
        operation:
          type: string
          enum:
          - firmware_upgrade
          - ssid_update
          - radio_config
          - wifi_schedule_toggle
          - wan_config
          - security_policy
          - reboot
        targetResource:
          type: string
        desiredState:
          type: object
        scheduledAt:
          type: string
          format: date-time
        recurring: *id003
        status:
          type: string
          enum:
          - pending
          - executing
          - completed
          - failed
          - cancelled
          readOnly: true
        createdAt:
          type: string
          format: date-time
          readOnly: true
        executedAt:
          type: string
          format: date-time
          readOnly: true
        resultMessage:
          type: string
          readOnly: true
    VoiceAssistantConfig:
      type: object
      description: Voice assistant platform integration (Alexa, Google Home, Apple
        HomeKit).
      properties:
        alexa:
          type: object
          properties:
            enabled:
              type: boolean
              default: false
            paired:
              type: boolean
              readOnly: true
            skillId:
              type: string
            accountLinked:
              type: boolean
              readOnly: true
            devicesDiscovered:
              type: integer
              readOnly: true
            lastSyncAt:
              type: string
              format: date-time
              readOnly: true
        googleHome:
          type: object
          properties:
            enabled:
              type: boolean
              default: false
            paired:
              type: boolean
              readOnly: true
            projectId:
              type: string
            localHomeEnabled:
              type: boolean
              default: true
            devicesDiscovered:
              type: integer
              readOnly: true
            lastSyncAt:
              type: string
              format: date-time
              readOnly: true
        appleHomeKit:
          type: object
          properties:
            enabled:
              type: boolean
              default: false
            paired:
              type: boolean
              readOnly: true
            setupCode:
              type: string
              writeOnly: true
            bridgeName:
              type: string
              default: Router Bridge
            pairedControllers:
              type: integer
              readOnly: true
            accessoryCount:
              type: integer
              readOnly: true
    AutomationRule:
      type: object
      description: Local automation rule with trigger-condition-action semantics.
      properties:
        ruleId:
          type: string
          readOnly: true
        name:
          type: string
        description:
          type: string
        enabled:
          type: boolean
          default: true
        trigger:
          type: object
          properties:
            type:
              type: string
              enum:
              - device_connected
              - device_disconnected
              - time
              - sensor_value
              - presence_changed
              - button_pressed
              - webhook
            deviceMac:
              type: string
            cronExpression:
              type: string
            sensorEntity:
              type: string
            sensorOperator:
              type: string
              enum:
              - gt
              - lt
              - eq
              - gte
              - lte
            sensorValue:
              type: number
        conditions:
          type: array
          items:
            type: object
            properties:
              type:
                type: string
                enum:
                - time_range
                - presence
                - device_count
              startTime:
                type: string
                pattern: ^[0-2]\d:[0-5]\d$
              endTime:
                type: string
                pattern: ^[0-2]\d:[0-5]\d$
              minClients:
                type: integer
        actions:
          type: array
          items:
            type: object
            properties:
              type:
                type: string
                enum:
                - notification
                - pause_device
                - unpause_device
                - set_wifi_schedule
                - reboot_node
                - webhook
                - mqtt_publish
              title:
                type: string
              message:
                type: string
              deviceMac:
                type: string
              url:
                type: string
              mqttTopic:
                type: string
              mqttPayload:
                type: string
        executionCount:
          type: integer
          readOnly: true
        lastExecutedAt:
          type: string
          format: date-time
          readOnly: true
        createdAt:
          type: string
          format: date-time
          readOnly: true
    CameraConfig:
      type: object
      description: IP camera integration on the router (RTSP, ONVIF, HomeKit Secure
        Video).
      properties:
        cameraId:
          type: string
          readOnly: true
        name:
          type: string
        enabled:
          type: boolean
          default: true
        rtspUrl:
          type: string
        onvifEnabled:
          type: boolean
          default: true
        onvifUsername:
          type: string
          writeOnly: true
        onvifPassword:
          type: string
          writeOnly: true
        homeKitSecureVideo:
          type: boolean
          default: false
        streamResolution:
          type: string
          enum:
          - 720p
          - 1080p
          - 4K
          default: 1080p
        recordOnMotion:
          type: boolean
          default: true
        recordRetentionDays:
          type: integer
          default: 10
        recordingStoragePath:
          type: string
        connectionStatus:
          type: string
          enum:
          - online
          - offline
          - error
          readOnly: true
        macAddress:
          type: string
          readOnly: true
        manufacturer:
          type: string
          readOnly: true
        lastSeen:
          type: string
          format: date-time
          readOnly: true
    SMSVerificationConfig:
      type: object
      description: SMS verification configuration for guest WiFi access.
      properties:
        enabled:
          type: boolean
          default: false
        provider:
          type: string
          enum:
          - twilio
          - messagebird
          - custom
        accountSid:
          type: string
          writeOnly: true
        authToken:
          type: string
          writeOnly: true
        senderNumber:
          type: string
        messageTemplate:
          type: string
          example: 'Your WiFi access code is: {{code}}. Valid for 10 minutes.'
        codeLength:
          type: integer
          default: 6
        codeExpirySec:
          type: integer
          default: 600
        maxAttempts:
          type: integer
          default: 3
        rateLimitPerNumber:
          type: integer
          default: 3
    SocialLoginConfig:
      type: object
      description: RADIUS-based social login configuration for guest WiFi.
      properties:
        enabled:
          type: boolean
          default: false
        providers:
          type: array
          items:
            type: object
            properties:
              provider:
                type: string
                enum:
                - facebook
                - google
                - apple
                - twitter
                - microsoft
              enabled:
                type: boolean
                default: false
              clientId:
                type: string
              clientSecret:
                type: string
                writeOnly: true
              redirectUri:
                type: string
        sessionTimeoutSec:
          type: integer
          default: 3600
        requireEmailVerification:
          type: boolean
          default: false
    PaidAccessConfig:
      type: object
      properties:
        enabled:
          type: boolean
          default: false
        paymentGateway:
          type: string
          enum:
          - stripe
          - paypal
          - custom
        gatewayApiKey:
          type: string
          writeOnly: true
        gatewaySecret:
          type: string
          writeOnly: true
        currency:
          type: string
          default: USD
        tiers:
          type: array
          items:
            type: object
            properties:
              tierId:
                type: string
                readOnly: true
              name:
                type: string
              price:
                type: number
              durationSec:
                type: integer
              bandwidthLimitMbps:
                type: integer
              dataCapMb:
                type: integer
              maxDevices:
                type: integer
        welcomeRedirectUrl:
          type: string
        requireEmailReceipt:
          type: boolean
          default: true
        totalRevenue:
          type: number
          readOnly: true
        activePaidSessions:
          type: integer
          readOnly: true
    PaidSession:
      type: object
      properties:
        sessionId:
          type: string
          readOnly: true
        tierId:
          type: string
        tierName:
          type: string
        paymentId:
          type: string
          readOnly: true
        amount:
          type: number
        currency:
          type: string
        deviceMac:
          type: string
          readOnly: true
        startedAt:
          type: string
          format: date-time
          readOnly: true
        expiresAt:
          type: string
          format: date-time
          readOnly: true
        dataUsedMb:
          type: number
          readOnly: true
        status:
          type: string
          enum:
          - active
          - expired
          - cancelled
          readOnly: true
    MotionZoneConfig:
      type: object
      properties:
        enabled:
          type: boolean
          default: false
        zones:
          type: array
          items:
            type: object
            properties:
              zoneId:
                type: string
                readOnly: true
              name:
                type: string
                example: Living Room
              sensitivity:
                type: string
                enum:
                - low
                - medium
                - high
                default: medium
              coverageArea:
                type: string
                description: Room/area identifier from floor plan.
              motionDetected:
                type: boolean
                readOnly: true
              lastMotionAt:
                type: string
                format: date-time
                readOnly: true
              occupancyCount:
                type: integer
                readOnly: true
                description: Estimated number of people in zone.
        globalSensitivity:
          type: string
          enum:
          - low
          - medium
          - high
          default: medium
        petImmune:
          type: boolean
          default: false
          description: Filter out small-animal motion.
        privacyMode:
          type: boolean
          default: true
          description: CSI data never leaves the device; only motion events are reported.
    MotionEvent:
      type: object
      properties:
        eventId:
          type: string
          readOnly: true
        zoneId:
          type: string
        zoneName:
          type: string
        eventType:
          type: string
          enum:
          - motion_start
          - motion_stop
          - occupancy_change
          readOnly: true
        confidence:
          type: number
          readOnly: true
        timestamp:
          type: string
          format: date-time
          readOnly: true
    AirtimeFairnessConfig:
      type: object
      properties:
        enabled:
          type: boolean
          default: true
        mode:
          type: string
          enum:
          - fair
          - weighted
          - disabled
          default: fair
        perSsid:
          type: boolean
          default: false
          description: Apply airtime fairness per-SSID rather than per-radio.
        minAirtimePct:
          type: integer
          default: 5
          description: Minimum airtime percentage per client.
        weights:
          type: object
          properties:
            highPriority:
              type: integer
              default: 3
            normalPriority:
              type: integer
              default: 2
            lowPriority:
              type: integer
              default: 1
    PHYDiagnostics:
      type: object
      properties:
        radioId:
          type: string
          readOnly: true
        band:
          type: string
          readOnly: true
        perChainRssi:
          type: array
          readOnly: true
          items:
            type: integer
          description: Per-chain RSSI in dBm.
        mcsDistribution:
          type: object
          readOnly: true
          description: MCS index usage distribution (MCS0-MCS13).
          additionalProperties:
            type: integer
          example:
            MCS0: 1500
            MCS7: 45000
            MCS11: 12000
        retryCount:
          type: integer
          readOnly: true
        ampduRetryCount:
          type: integer
          readOnly: true
        noiseFloorDbm:
          type: integer
          readOnly: true
        rxErrors:
          type: object
          readOnly: true
          properties:
            crcErrors:
              type: integer
            phyErrors:
              type: integer
            plcpErrors:
              type: integer
        txPowerDbm:
          type: integer
          readOnly: true
        channelUtilizationPct:
          type: integer
          readOnly: true
        nonWifiInterference:
          type: boolean
          readOnly: true
        antennaMask:
          type: string
          readOnly: true
          description: Active antenna bitmask.
        samplePeriodSec:
          type: integer
          default: 60
    FlowExportConfig:
      type: object
      properties:
        enabled:
          type: boolean
          default: false
        protocol:
          type: string
          enum:
          - netflow_v5
          - netflow_v9
          - ipfix
          default: ipfix
        collectorAddress:
          type: string
        collectorPort:
          type: integer
          default: 2055
        samplingRate:
          type: integer
          default: 1
          description: Packet sampling rate (1 = every packet, 100 = 1 in 100).
        activeTimeoutSec:
          type: integer
          default: 1800
          description: Flow active timeout.
        inactiveTimeoutSec:
          type: integer
          default: 15
          description: Flow inactive timeout.
        templateRefreshInterval:
          type: integer
          default: 600
          description: Template refresh interval for NetFlow v9/IPFIX.
        exportInterfaces:
          type: array
          items:
            type: string
          description: Interfaces to export flow data from (empty = all).
    GuestTier:
      type: object
      properties:
        tierId:
          type: string
          readOnly: true
        name:
          type: string
          example: Premium Guest
        priority:
          type: integer
          default: 0
        bandwidthLimitMbps:
          type: integer
        sessionDurationSec:
          type: integer
        maxDevices:
          type: integer
        requiresAuth:
          type: boolean
          default: true
        accessHours:
          type: array
          items:
            $ref: '#/components/schemas/AccessSchedule'
    SubnetPool:
      type: object
      properties:
        poolId:
          type: string
          readOnly: true
        name:
          type: string
        networkCidr:
          type: string
          description: Network CIDR (e.g. 10.0.0.0/16).
        subnetPrefixLen:
          type: integer
          default: 24
          description: Subnet prefix length for allocation (e.g. 24 = /24 subnets).
        gateway:
          type: string
          description: Default gateway address for allocated subnets.
        dnsServers:
          type: array
          items:
            type: string
        allocatedSubnets:
          type: array
          readOnly: true
          items:
            type: object
            properties:
              subnetCidr:
                type: string
              locationId:
                type: string
              locationName:
                type: string
              allocatedAt:
                type: string
                format: date-time
              utilizationPct:
                type: integer
        freeSubnets:
          type: integer
          readOnly: true
          description: Number of subnets still available in this pool.
        totalSubnets:
          type: integer
          readOnly: true
    SSOConfig:
      type: object
      properties:
        enabled:
          type: boolean
          default: false
        protocol:
          type: string
          enum:
          - saml
          - oidc
          default: oidc
        issuerUrl:
          type: string
          description: IdP issuer URL (e.g. https://accounts.google.com).
        clientId:
          type: string
        clientSecret:
          type: string
          writeOnly: true
        authorizationEndpoint:
          type: string
          description: OIDC authorization endpoint.
        tokenEndpoint:
          type: string
          description: OIDC token endpoint.
        userInfoEndpoint:
          type: string
          description: OIDC userinfo endpoint.
        jwksUri:
          type: string
          description: JWKS URI for token verification.
        samlMetadataUrl:
          type: string
          description: SAML IdP metadata XML URL (SAML only).
        spEntityId:
          type: string
          description: Service Provider entity ID.
        roleMapping:
          type: object
          description: Map IdP group claims to API RBAC roles.
          properties:
            adminGroup:
              type: string
            nocGroup:
              type: string
            supportGroup:
              type: string
            billingGroup:
              type: string
            readOnlyGroup:
              type: string
        autoProvisionUsers:
          type: boolean
          default: true
          description: Create operator accounts on first SSO login (Just-in-Time provisioning).
        defaultRole:
          type: string
          enum:
          - admin
          - read_only
          - noc
          - support_tier1
          - support_tier2
          - billing
          - provisioning
          default: read_only
          description: Default role for auto-provisioned users.
